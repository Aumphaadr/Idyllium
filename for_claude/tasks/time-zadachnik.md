# Задачник: Библиотека time

Урок №14 раздела «Консоль» (сразу после циклов — чтобы наиграться с паузами). Составлен 2026-07-31 по [лестнице навыков](../ladder/console-skill-ladder.md), [критериям заданий](../reports/task-design-criteria.md) и [методичке Real-IT](../reports/real-it-methodology-extract.md). Все решения прогнаны через компилятор. Образцы с текущим временем и случайностью помечены «например» (у вас выйдет своё); отсчёты и метки из чисел — точные.

**Для преподавателя.**
- *Доступно:* всё из уроков 1–13 — переменные, ввод, арифметика, `writeln` и цвета, `to_string/to_int/to_float`, `math.*`, `if`/`bool`, `random`, логические операции, **циклы** (`while`/`for`/`do…while`, `break`/`continue`); из новой темы — `use time;`, `time.sleep(s)` (секунды, целые и дробные), `time.now([зона])` → объект `time.stamp`, его свойства-через-точку (`.year .month .day .hour .minute .second .millisecond .week_day` [0=вс..6=сб] `.unix .timezone`), `time.create(год, месяц, день[, час, минута, секунда])`, `time.from_unix(n[, зона])`, метод `.in_timezone(зона)` (имена зон — IANA). Здесь **впервые** появляются точечные свойства объекта и вызов метода через точку.
- *Нельзя (рано):* массивов (№16), методов строк (№21), собственных функций (№25). Задания — время + циклы + условия, без коллекций.
- *Словарь:* пауза, временная метка, часовой пояс, UNIX-время, день недели, миллисекунда.

Библиотека `time` умеет две вещи: **ждать** (`sleep`) и **узнавать, который час** (`now` и метки). Владелец поставил тему сразу после циклов не зря — `sleep` внутри цикла даёт обратные отсчёты и «живой» вывод с задержкой, и с этого веселья мы и начнём. Потом — разбор меток на части, свои даты и часовые пояса. **Важно:** поломки (несуществующая дата, чужой пояс) ученик открывает сам.

---

# Блок 1. Паузы и обратные отсчёты: `time.sleep`

`time.sleep(s)` останавливает программу на `s` секунд (можно дробных). Само по себе — просто пауза. А внутри цикла — магия: числа и символы появляются по одному, как живые.

## Практика с преподавателем

### Задание 1. Одна пауза

Самая простая программа с паузой — две реплики и `sleep` между ними. Наберите и запустите: вторая строка появится **не сразу**.

```idyllium
use console;
use time;

main() {
    console.writeln("Внимание...");
    time.sleep(1.5);
    console.writeln("Прошло полторы секунды!");
}
```

```
Внимание...
Прошло полторы секунды!
```

Пока `sleep` отсчитывает свои полторы секунды, программа честно стоит на месте и только потом переходит к следующей строке. Всего одна новая команда — а вывод уже «дышит». Дальше соединим паузу с циклом, и начнётся настоящее веселье.

## Самостоятельная практика

### Задание 2. До Нового года

Программа спрашивает, с какого числа начать, и ведёт обратный отсчёт с паузой в секунду между числами, а в конце поздравляет.

```
С какого числа: 5
5
4
3
2
1
С Новым годом!
```

### Задание 3. Загрузка

Нарисуем «полосу загрузки». Программа спрашивает, сколько блоков грузить, печатает `Загрузка: `, а затем в цикле выводит столько символов `#` подряд, **без переносов** (через `console.write`), с короткой паузой (например, `0.2` секунды) между ними. В конце — `готово!`.

```
Сколько блоков загрузки: 15
Загрузка: ############### готово!
```

Полоса на экране растёт на глазах — вот зачем нужна пауза внутри цикла. Введите 40 — и полюбуйтесь долгой загрузкой во всей красе.

### Задание 4. Светофор

Программа изображает светофор: печатает «Красный» (красным цветом из урока о выводе), ждёт 2 секунды, потом «Жёлтый» (жёлтым), ждёт 1 секунду, потом «Зелёный» (зелёным) — «Поехали!». Цвета из урока №6, паузы — из этого.

```
Красный      (красными буквами)
Жёлтый       (жёлтыми буквами, через 2 секунды)
Зелёный — поехали!   (зелёными буквами, ещё через 1 секунду)
```

### Задание 5. Бросок кубика с дробью

Соединим паузу со случайностью. Программа «крутит» кубик: в цикле 8 раз печатает случайное число от 1 до 6 с короткой паузой (мелькание!), а потом объявляет финальный, «застывший» бросок.

```
Крутим... 4 2 6 1 5 3 2 4
Выпало: 5
```

Числа мелькают, как на настоящем кубике, — а последний бросок и есть результат. (Значения случайные, образец — «например».)

---

# Блок 2. Который сейчас час: `time.now`

`time.now()` возвращает временную метку — объект типа `time.stamp`. Из него можно достать любую часть даты и времени, обращаясь через точку **без скобок**: `t.year`, `t.hour` — мы ведь читаем свойство, а не приказываем что-то сделать.

## Практика с преподавателем

### Задание 6. Разбираем момент

Наберите (у вас, конечно, выйдут другие числа — время не стоит на месте):

```idyllium
use console;
use time;

main() {
    time.stamp t = time.now();
    console.writeln("Сейчас: ", t);
    console.writeln("Год: ", t.year, ", месяц: ", t.month, ", день: ", t.day);
    console.writeln("Время: ", t.hour, ":", t.minute, ":", t.second);
    console.writeln("День недели (0=вс): ", t.week_day);
}
```

```
Сейчас: 2026-07-31 19:37:03
Год: 2026, месяц: 7, день: 31
Время: 19:37:3
День недели (0=вс): 5
```

Одна метка — и в ней сразу всё: и дата, и время, и даже номер дня недели. Скобки после `t.year` не нужны: это свойство, а не команда.

## Самостоятельная практика

### Задание 7. Электронные часы

Программа берёт текущее время и выводит только часы, минуты и секунды в одну строку — как на электронном табло.

```
Сейчас 19:37:3
```

### Задание 8. Сегодня выходной?

Программа смотрит на текущий день недели (`t.week_day`) и сообщает, выходной сегодня или будни. Выходные — это воскресенье (0) **или** суббота (6); тут пригодится `or` из темы про логику.

```
Сегодня выходной, отдыхаем!
```

### Задание 9. Сколько секунд эпохе

Программа выводит текущее UNIX-время — число секунд, прошедших с 1 января 1970 года (свойство `.unix`). Почти два миллиарда — вот сколько «тикает» этот счётчик.

```
С начала эпохи UNIX прошло 1785440223 секунд.
```

---

# Блок 3. Свои метки и часовые пояса

Метку можно не только взять «сейчас», но и собрать из чисел (`time.create`) или из UNIX-времени (`time.from_unix`), а потом показать в любом часовом поясе.

## Практика с преподавателем

### Задание 10. Метка на заказ

Наберите:

```idyllium
use console;
use time;

main() {
    time.stamp tour = time.create(2026, 9, 24, 18, 3);
    console.writeln("Турнир: ", tour);
    console.writeln("День недели: ", tour.week_day);

    time.stamp epoch2000 = time.from_unix(946684800);
    console.writeln("Из UNIX-времени: ", epoch2000);
}
```

```
Турнир: 2026-09-24 18:03:00
День недели: 4
Из UNIX-времени: 2000-01-01 00:00:00
```

`time.create` собирает метку из года, месяца, дня (и по желанию — часа, минуты, секунды; что не указано — считается нулём). `time.from_unix` делает метку из одного большого числа.

## Самостоятельная практика

### Задание 11. В какой день недели

Программа спрашивает дату (год, месяц, день), собирает метку через `time.create` и говорит номер дня недели (0 — воскресенье, 6 — суббота). Удобно узнать, на какой день выпадает праздник!

```
Год: 2027
Месяц: 1
День: 1
День недели (0=вс): 5
```

### Задание 12. Несуществующая дата

`time.create` строго следит за календарём. Проверьте — попробуйте собрать 30 февраля:

```idyllium
    time.stamp broken = time.create(2026, 2, 30);
```

Запустите и прочитайте ошибку выполнения. В феврале не бывает 30-го числа, и `time.create` не станет выдумывать несуществующий день. (Проверять дату заранее мы бы могли условиями — но проще довериться самой функции: она честно остановится.)

### Задание 13. Машина времени

Программа спрашивает UNIX-время (одно большое число) и превращает его в настоящую дату через `time.from_unix`, а затем выводит её год отдельно. Попробуйте, например, `0` (самое начало эпохи) или `946684800`.

```
UNIX-время: 0
Дата: 1970-01-01 00:00:00
Это был 1970 год.
```

### Задание 14. Один миг в трёх городах

UNIX-время одинаково для всей планеты, а вот часы на стене — разные. Программа берёт один момент (`time.from_unix(0)`) и показывает его в трёх поясах через `.in_timezone(...)`: Лондон (`Europe/London`), Москва (`Europe/Moscow`), Нью-Йорк (`America/New_York`).

```
Лондон:    1970-01-01 01:00:00
Москва:    1970-01-01 03:00:00
Нью-Йорк:  1969-12-31 19:00:00
```

Тот же миг — а в Нью-Йорке ещё вчерашний вечер. Пояс не двигает время, лишь показывает его глазами разных городов.

---

# Блок 4. Со звёздочкой

### Задание 15 ⭐. Как в C++ заводят «настоящую» случайность

Вспомните урок про случайные числа: `random.set_seed(N)` делал результаты **воспроизводимыми** — один и тот же сид давал одни и те же числа. Удобно для проверки, но для игры-то нужна настоящая непредсказуемость, разная при каждом запуске. Откуда её взять?

Хитрость стара, как языки программирования. В **C++** пишут `srand(time(0))`: `time(0)` — это текущее UNIX-время (то самое число секунд), и им «заводят» генератор. Раз время при каждом запуске новое — и случайность новая.

В Idyllium тот же приём выглядит так:

```idyllium
use console;
use time;
use random;

main() {
    random.set_seed(time.now().unix);
    console.writeln("Бросок кубика: ", random.create_int(1, 6));
}
```

Запустите несколько раз — бросок каждый раз разный, ведь `time.now().unix` каждую секунду другой. Сделайте вывод: сид из времени = «настоящая» случайность, фиксированный сид = воспроизводимость для отладки. Два инструмента для двух разных задач.

### Задание 16 ⭐. Секундомер

Засечём, сколько длится пауза. Программа запоминает UNIX-время **до** (`time.now().unix`), делает `time.sleep(3)`, запоминает время **после** и выводит разницу — сколько секунд прошло.

```
Засекаю... подождите.
Прошло секунд: 3
```

Тот же приём измеряет, сколько работала любая часть программы: заметил время до, заметил после, вычел одно из другого.

### Задание 17 ⭐. Запуск ракеты «всё вместе»

Финальный номер — соберите всё в одну программу. По шагам: программа печатает текущее время старта (метка `time.now()`), ведёт обратный отсчёт от 5 с паузами (`sleep` в цикле), а на «Пуск!» объявляет случайный «номер миссии» (`random`, засеянный временем из задания 15). Три библиотеки — `time`, `random`, `console` — и циклы с условиями, всё разом.

```
Старт миссии в 2026-07-31 19:40:12
5
4
3
2
1
Пуск! Миссия №4718 пошла!
```

(Время и номер миссии — «например»: у вас выйдут свои.)

---

**Заметка для преподавателя (запас времени).** Прорешали всё — не открывайте новую тему, а развивайте сделанное:
- полосу загрузки (з.3) сделать процентной: рядом с каждым `#` печатать `i * 10` процентов (`to_string` из темы про преобразования);
- «выходной?» (з.8) довести до расписания: по `week_day` печатать разные сообщения несколькими независимыми `if` (пн — «снова на учёбу», пт — «почти отдых»…);
- секундомер (з.16) уточнить до миллисекунд через `.millisecond` — заодно повод обсудить, почему для коротких промежутков секунд мало.

«Битый код / эксперименты» здесь — задания 12 (несуществующая дата) и, в мягком виде, 5, 15, 16, где ученик наблюдает случайность и измеряет паузы. Красная нить блока 1 — **`sleep` в цикле оживляет вывод**: то, что раньше вылетало мгновенной простынёй, теперь идёт в ритме. Это самый наглядный способ почувствовать, что программа исполняется по шагам во времени, а не «вся сразу».
