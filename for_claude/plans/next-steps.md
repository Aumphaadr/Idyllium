# Возможные следующие шаги (черновик к обсуждению)

Составлено 2026-07-24 по итогам анализа документации; обновлено после редактуры «Консоли».

## Сделано 2026-07-24

- [x] Раздел «Консоль» (32 урока) отредактирован по критериям Т/АТ; все примеры прогнаны через компилятор; сайт пересобран — см. reports/console-lessons-edit-report.md.
- [x] Уроки консоли переехали в `docs/manual-content/console/` (единый источник правды), `docs-build-site.ts` обновлён, патчи интегрированы.

## Новые пункты (техдолг, найденный при редактуре)

- [ ] Баг CLI: буферизованный stdin теряет строки после первой (`printf "a\nb\n" | cli run` — второе значение не читается). `src/cli.ts`, readline.
- [ ] Пути в ошибках file.*: рантайм печатает абсолютный путь; рассмотреть проектно-относительные пути в диагностиках.
- [ ] Бесконечная рекурсия падает «сырым» `RangeError: Maximum call stack size exceeded` без `main.idyl:строка` — сделать читаемую runtime error.
- [ ] Синхронизировать формат диагностик в ai-reference: компиляция теперь `file:line:col:` (с колонкой).
- [ ] Решить судьбу `docs/manual-content/patches/console-*.html` (4 файла больше не используются — удалить?).
- [ ] Обсудить: разбиение types.html на 2–3 урока; страница-капстоун раздела; легализация секций «Попробуйте сами» (пока есть только в try-catch).
- [x] Ревью раздела «Виджеты» по критериям — выполнено 2026-07-24 (см. reports/widgets-lessons-review.md); редактура — следующий шаг по согласованию.
- [x] Редактура «Виджетов» выполнена 2026-07-24 (reports/widgets-lessons-edit-report.md): 21 урок, реальные тексты ошибок, демо синхронизированы, фантомный API Timer удалён (формулировки в отчёте — вернуть, если команда реализовала running/restart).
- [ ] Вопрос команде компиляции: `gui.Timer.running`/`restart()` — фантом урока или недоделанный API? (Компилятор: `type 'gui.Timer' has no member 'running'` / `no method 'restart'`; урок timer их документирует.) — в работе у команды (2026-07-24).
- [ ] БАГ компилятора: печать объекта класса не запрещена — `console.writeln(cat)` выводит `[object Object]` (контракт обещает ошибку `cannot print object of class ... directly`). Найден при ревью ООП.
- [ ] РЕГРЕСС диагностики: `destructor Hero() {}` даёт каскад сырых парсер-ошибок вместо прежнего `error: destructors are not supported yet` (было в инвентаризации 2026-06).
- [ ] Вопрос дизайна: `parent()` первым оператором конструктора — уроки подают как правило языка, компилятор не проверяет. Решить: проверка в компиляторе или «правило стиля» в уроках.
- [ ] Ревью ООП выполнено (reports/oop-lessons-review.md); редактура раздела — по согласованию. Приоритет №1 — arrays (неверный output из-за ссылочной семантики add) и errors (0/6 текстов, вредная секция про `;`).
- [x] Редактура Canvas выполнена 2026-07-24 (reports/canvas-lessons-edit-report.md); чекер команды доработан (GUI-skip + многофайловая конвенция).
- [x] Редактура JSON (8 правок: слепые примеры озвучены, врезка «Value(контейнер) — ссылка» в object-arrays, set() показан, +2 записи в errors) и SQLite (хвост достроен полными программами с выводами: null/reuse/transactions/keys/joins; defaults реран-безопасен и прогнан дважды; оговорка FOREIGN KEY; глоссы в scripts; финал учебной линии «Путь пройден» в errors) — 2026-07-24. Не сделано: урок «Калькулятор» (examples) — писать с нуля отдельной задачей.
- [ ] Ревью Canvas/JSON/SQLite/examples выполнено 2026-07-24 (reports/canvas-lessons-review.md, reports/json-sqlite-lessons-review.md) — ВСЕ 110 уроков учебника проверены. Приоритеты правок — в отчётах; сквозное: достроить хвост SQLite, финал учебной линии, урок «Калькулятор», опорные таблицы.
- [ ] Вопрос команде компиляции: включать ли `PRAGMA foreign_keys=ON` в sqlite-рантайме? (Сейчас FOREIGN KEY не проверяется — guild_id=999 вставляется молча; урок joins создаёт ложное ожидание защиты.)
- [x] Слияние веток сверено 2026-07-24 (см. reports/merge-verification-report.md): 8 исправлений подтверждены пробами; 239 блоков уроков сверены с новым рантаймом (225 дословных совпадений, 0 настоящих рассинхронов после 6 точечных правок: recursion, directories×2, files, math-basics-abs, timer-шпаргалка+millisecond в time).
- [ ] Оставшийся шум рантайма: перед читаемым сообщением бесконечной рекурсии всё ещё печатаются сырые «Exception in PromiseRejectCallback / RangeError» (дважды) — передать команде.
- [ ] При редактуре ООП: урок this.html снова корректен по сути (печать объекта — ошибка), но формат в нём старый (`main.idyl:10:` без колонки; реальный — `7:21:`); учесть.
- [ ] Проверить семантику `RadioButton.on_change`: срабатывает ли при снятии выбора (пример урока radiobutton может вести себя недетерминированно).
- [x] Битые медиа-ассеты учебника (cat.png, walk.gif, click.wav, theme.mp3) — восстановлены из git в `docs/manual-content/assets/`, пути сборки поправлены (2026-07-24).
- [x] Идемпотентность lessonFragment (задвоение стилей) — исправлена (2026-07-24).

## 1. Гигиена документации (быстрые победы)

- [ ] Синхронизировать **тексты ошибок в уроках** с фактическим выводом компилятора (inconsistencies.md, раздел C — ~12 устаревших формулировок). Идея: расширить `npm run docs:inventory` проверкой цитируемых в уроках диагностик.
- [ ] Исправить противоречия ядру языка в уроках: `N /= 4` для `int` (increment), печать массива строк без кавычек (dyn-array), «`;` после класса» и отсутствующие секции в oop/errors, заглушка «Калькулятор».
- [ ] Дополнить ai-reference: `has_next_line()`, дефолтные значения переменных, «порядковые сравнения только числовые», ссылочная семантика объектов.

## 2. Registry / api.json

- [ ] Выяснить по `src/core`, кто прав: уроки (`gui.Timer.running`/`restart()`, `math.atan`, `round(value, digits)`) или registry (их не знает). Затем править проигравшую сторону.
- [ ] Добавить `max/min/sum/avg` в раздел globals api.json.
- [ ] Выразить наследование (`extends`) в api.json, чтобы `image.Static`, `gui.Label` и др. машинно наследовали свойства базы.

## 3. Спецификация пробелов

- [ ] Задокументировать значения `KeyboardEvent.key` для спецклавиш и семантику `MouseScrollEvent.delta`.
- [ ] Зафиксировать дефолты виджетов (размеры, echo_mode, selected_index, min/max SpinBox, тексты кнопок Modal).
- [ ] Описать модель памяти (значения vs ссылки) единым разделом в ai-reference и уроком в учебнике.

## 4. Более крупные идеи (если захочется)

- [ ] Автопрогон всех код-блоков нового учебника компилятором в CI (расширение docs:inventory на docs/book + manual-content) — чтобы 42/221 старой миграции не повторились.
- [ ] Дописать урок-пример «Калькулятор» (examples).
- [ ] Единый стайл-гайд идентификаторов (snake_case) и зачистка camelCase в oop-уроках.
