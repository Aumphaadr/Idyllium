class Cow extends Animal {
    constructor Cow(string ex_name) {
        parent(ex_name);
    }
    // метод speak() не переопределён
}

main() {
    Cow burenka("Бурёнка");
    burenka.speak();   // выведет "..."
}