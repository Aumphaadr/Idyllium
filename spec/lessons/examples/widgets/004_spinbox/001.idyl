use gui;

main() {
    gui.Window win;
    win.width = 200;
    win.height = 100;
    win.title = "SpinBox";

    gui.SpinBox spin;
    spin.x = 20;
    spin.y = 20;
    spin.width = 100;
    spin.value = 5;

    win.add_child(spin);
    win.show();
}