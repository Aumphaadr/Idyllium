use gui;

main() {
    gui.Window win;
    win.width = 300;
    win.height = 200;
    win.title = "Кнопка-пустышка";

    gui.Button btn;
    btn.text = "Нажми меня";
    btn.x = 50;
    btn.y = 50;

    win.add_child(btn);
    win.show();
}