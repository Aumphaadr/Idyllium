use gui;

main() {
    gui.Window win;
    win.width = 250;
    win.height = 100;
    win.title = "CheckBox";

    gui.CheckBox cb;
    cb.x = 20;
    cb.y = 30;
    cb.text = "Я согласен";

    win.add_child(cb);
    win.show();
}