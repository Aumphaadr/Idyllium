use console;

main() {
    array<int, 7> numbers = [10, 20, 30, 40, 50, 60, 70];

    for (int i = 0; i < numbers.length(); i = i + 1) {
        console.writeln("numbers[", i, "] = ", numbers[i]);
    }
}