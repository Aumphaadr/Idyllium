use console;

main() {
    array<string, 3> raw = ["Кирка\n", "Топор\n", "Меч"];
    array<string, 3> clean;

    for (int i = 0; i < 3; i = i + 1) {
        clean[i] = raw[i].trim();
    }

    console.writeln(clean);
}