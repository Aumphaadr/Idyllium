use console;
use json;

main() {
    json.Object root;

    json.Value name("Mira");
    root.add("name", name);

    json.Value age;
    age.set_int(12);
    root.add("age", age);

    if (root.has("age")) {
        root.set("age", json.Value(13));
    }

    console.writeln(root.to_json());
}
