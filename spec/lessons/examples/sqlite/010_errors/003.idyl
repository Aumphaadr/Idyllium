use sqlite;

main() {
    sqlite.Database db = sqlite.open("error_column.db");
    db.execute("DROP TABLE IF EXISTS players");
    db.execute("CREATE TABLE players (name TEXT NOT NULL)");
    db.execute("INSERT INTO players VALUES ('Nika')");

    sqlite.Result rows = db.execute("SELECT name FROM players");
    rows.next();
    int score = rows.get_int("score");
}

// Expected:
// main.idyl:11: runtime error: sqlite result has no column 'score'
