# Idyllium

> **Гармония синтаксиса. Простота понимания. Надёжность обучения.**

Idyllium — учебный язык программирования, созданный специально для детей 10–15 лет.  
Он учит **думать, а не зубрить**, и избавляет от подводных камней, которые мешают начинающим в Pascal, C++ и Python.

## 🔍 Особенности

- ✅ **Предсказуемая арифметика**: `7 / 4 = 1.75`, а не `1`
- ✅ **Понятные ошибки**: "Неизвестный тип 'floot'. Возможно, вы имели в виду 'float'?"
- ✅ **Unicode "из коробки"**: `len("белый") == 5`
- ✅ **Безопасный ввод-вывод**: `console.get_int()` сам проверяет формат
- ✅ **Нет магии**: всё — явно, последовательно, без исключений

## ▶️ Быстрый старт

1. Установите Python 3.8+
2. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/Aumphaadr/Idyllium.git
   cd Idyllium
   ```
3. Напишите программу:
   ```idyllium
   // hello.idyl
   use console;
   main() {
       console.write("Введите имя: ");
       string name = console.get_string();
       console.write("Привет, ", name, "!\n");
   }
   ```
4. Скомпилируйте и запустите:
   ```bash
   python idylc.py hello.idyl --run
   ```

## 🛠 Архитектура компилятора

```
Idyllium → лексер → парсер → семантический анализ → генератор Python → выполнение
```

Компилятор написан на Python и генерирует читаемый Python-код, что упрощает отладку и обучение.

## 📚 Примеры

См. папку [`examples/`](examples/).

## 📚 Документация

Полное руководство по языку Idyllium доступно здесь:  
👉 [**Основы программирования на Idyllium**](https://aumphaadr.github.io/Idyllium/)

## 📜 Лицензия

Данный проект распространяется под лицензией MIT — см. файл [`LICENSE`](LICENSE).

---

Создано с ❤️ для будущих поколений программистов.
