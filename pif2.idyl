use console;
main() {
    console.write("Размер таблицы: ");
    int n = console.get_int();
    for (int i = 1; i <= n; i += 1) {
        for (int j = 1; j <= n; j += 1) {
            console.write(i * j, " ");
        }
        console.write("\n");
    }
}
