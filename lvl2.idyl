use console;
main() {
    console.write("Сколько опыта готов накопить: ");
    int cap = console.get_int();
    for (int i = 1; i <= 100000; i += 1) {
        if (i * i > cap) {
            console.writeln("Первый уровень дороже: ", i);
            break;
        }
    }
}
