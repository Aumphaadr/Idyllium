use console;
main() {
    console.write("Сколько ребят в кругу: ");
    int n = console.get_int();
    for (int i = 1; i <= n; i += 1) {
        if (mod(i, 3) == 0) { continue; }
        console.write(i, " ");
    }
    console.write("\n");
}
