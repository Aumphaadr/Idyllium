# Основы программирования на Idyllium

> **Цель**: обучать программированию без синтаксических ловушек  
> Подробнее о философии языка — на [официальном сайте](https://it-partizan.ru/misc/idyllium/index.html)

---

## Структура программы

Каждая программа начинается с функции `main()`:

```idyllium
use console;

main() {
    console.write("Hello, World!", '\n');
}
```

- Тип возвращаемого значения не указывается.
- Аргументы для `main` не требуются.

---

## Типы данных и переменные

- `int` — целое число  
- `float` — число с плавающей точкой  
- `char` — одиночный символ  
- `string` — строка  
- `bool` — логическое значение  
- `array<T, N>` — массив фиксированной длины  
- `dyn_array<T>` — динамический массив  
- `void` — для функций без возвращаемого значения  

Пример объявления переменных:

```idyllium
main() {
    int age = 18;
    float pi = 3.14;
    char letter = 'Q';
    string name = "Alice";
    bool is_active = true;
}
```

---

## Консольный ввод/вывод

Для работы с консолью используется встроенная библиотека `console`:

```idyllium
use console;

main() {
    int a;
    float b;
    string name;

    console.write("Введите имя");
    name = console.get_string();

    console.write("Введите целое число");
    a = console.get_int();

    console.write("Введите дробное число");
    b = console.get_float();

    console.write("Привет, ", name, "! Ваши числа: ", a, " и ", b, '\n');
}
```

- Чёткие типы и понятные функции: `console.get_int()`, `console.get_float()`, `console.get_string()`.
- Никаких `int(input(...))` или `Int32.TryParse()`.
- `console.write()` **не добавляет перенос строки** без явного `'\n'`.

---

## Арифметика

Все операции предсказуемы и строго типизированы:

```idyllium
main() {
    int a = 9;
    int b = 4;
    float d = 2.5;

    // базовые операции
    int r1 = a + b; // 13
    int r2 = a - b; // 5
    int r3 = a * b; // 36
    float r4 = a / b; // 2.25

    // взаимодействие 'int' и 'float' → всегда 'float'
    float r5 = a + d; // 11.5
    float r6 = b * d; // 10.0

    // неявное преобразование: только int → float
    float f = 3;     // 3.0
    int n = 7.5;     // TypeError

    // целочисленное деление — через функции
    int quotient = div(43, 2); // 21
    int remainder = mod(43, 2); // 1
}
```

> 💡 Оператор `%` **намеренно удалён**, чтобы дети не путали «остаток от деления» с «процентами».

---

## Явные преобразования типов

Используются функции:

```idyllium
main() {
    // числа → строки
    string s1 = to_string(25);   // "25"
    string s2 = to_string(4.7);  // "4.7"

    // строки → числа
    int n1 = to_int("1453");     // 1453
    float f1 = to_float("36.4"); // 36.4

    // между числовыми типами
    int n2 = to_int(9.63);       // 9
    float f2 = to_float(15);     // 15.0
}
```

---

## Конкатенация

Разрешена **только** в следующих случаях:

- `string + string`
- `string + char`
- `char + string`

```idyllium
use console;

main() {
    string greeting = "Hello, ";
    string name = "Alice";
    char mark = '!';

    string r1 = greeting + name; // OK
    string r2 = r1 + mark;       // OK

    console.write(r1, '\n');     // "Hello, Alice"
    console.write(r2, '\n');     // "Hello, Alice!"
}
```

Конкатенация с числами **запрещена**:

```idyllium
string bad = "Year: " + 2025;           // TypeError
string good = "Year: " + to_string(2025); // OK
```

---

## Условный оператор

Стандартный синтаксис, как в C-подобных языках:

```idyllium
use console;

main() {
    int a = console.get_int();
    int b = console.get_int();

    if (a > b) {
        console.write(a, " больше, чем ", b, '\n');
    }
    else if (a == b) {
        console.write(a, " равно ", b, '\n');
    }
    else {
        console.write(a, " меньше, чем ", b, '\n');
    }
}
```

---

## Циклы

### `for`
```idyllium
for (int i = 0; i < 20; i = i + 1) {
    console.write("i = ", i, '\n');
}
```

### `while`
```idyllium
int q = 1;
while (q < 500) {
    console.write(q, '\n');
    q = q * 2;
}
```

### `do-while`
```idyllium
int right_answer = 71;
int your_answer;
do {
    console.write("27 + 44 = ");
    your_answer = console.get_int();
} while (your_answer != right_answer);
```

---

## Псевдослучайные числа

Используется библиотека `random`:

```idyllium
use console;
use random;

main() {
    int n = random.create_int(10, 99);     // [10, 99] включительно
    float d = random.create_float(2.5, 4); // от 2.5 до 4.0

    string alphabet = "abcdefghijklmn";
    char ch = random.choose_from(alphabet); // случайный символ
}
```

> ✨ Без `rand() % N`, `srand(time(0))` и других «цыганских фокусов».

---

## Массивы

### Статические
```idyllium
array<int, 5> small = [11, 22, 33, 44, 55];
array<int, 50> big; // автоматически инициализируется нулями

// Ошибка при выходе за границы:
console.write(big[60], '\n'); // IndexError
```

### Динамические
```idyllium
dyn_array<int> nums;
nums.add(42);
nums.add(74);
nums.remove_at(1);
int L = nums.length(); // 4
```

- Нумерация с нуля.
- Ячейки строго однотипны.
- Безопасные операции с проверкой границ.

---

## Библиотеки

Вызов функций — по схеме `библиотека.функция()`:

```idyllium
use console;
use math;
use time;

main() {
    console.write("Подождите, округляю число...", '\n');
    time.sleep(2.5);
    float pi4 = math.round(3.1415926535, 4);
    console.write("Округлено: ", pi4, '\n'); // 3.1416
}
```

> 💡 Нет «пространств имён» — только понятная иерархия.

---

## Функции

### Процедура (`void`)
```idyllium
void function draw_line(int L) {
    string res = "";
    for (int i = 0; i < L; i = i + 1) {
        res = res + '-';
    }
    console.write(res, '\n');
}

main() {
    draw_line(5);  // -----
}
```

### Функция с возвратом
```idyllium
int function cube(int N) {
    return N * N * N;
}

main() {
    int a = cube(2); // 8
}
```

---

## Работа с файлами

### Запись
```idyllium
use file;

main() {
    file fout = file.open("output.txt", "write");
    fout.write_line("Привет, мир!", '\n');
    fout.write_line("Сейчас ", 2025, " год", '\n');
    fout.close();
}
```

### Чтение
```idyllium
use console;
use file;

main() {
    file fin = file.open("input.txt", "read");
    while (fin.has_next_line()) {
        string line = fin.read_line();
        console.write(line, '\n');
    }
    fin.close();
}
```

---

> 💬 **Idyllium создан, чтобы дети учились думать — а не бояться ошибок.**  
> Сайт проекта: [https://it-partizan.ru/misc/idyllium/index.html](https://it-partizan.ru/misc/idyllium/index.html)
