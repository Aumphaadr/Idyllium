/* Generated by tools/build-web-ide.js */
(function (global) {
  'use strict';

  var modules = {
"src/browser.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.IdylliumProject = exports.formatIdyllium = exports.compileIdyllium = void 0;
exports.runIdylliumInBrowser = runIdylliumInBrowser;
exports.prepareIdylliumBrowserProgram = prepareIdylliumBrowserProgram;
const run_1 = require("./runtime/run");
Object.defineProperty(exports, "compileIdyllium", { enumerable: true, get: function () { return run_1.compileIdyllium; } });
const formatter_1 = require("./language/formatter");
Object.defineProperty(exports, "formatIdyllium", { enumerable: true, get: function () { return formatter_1.formatIdyllium; } });
const project_1 = require("./language/project");
Object.defineProperty(exports, "IdylliumProject", { enumerable: true, get: function () { return project_1.IdylliumProject; } });
const runtime_1 = require("./runtime/runtime");
async function runIdylliumInBrowser(options) {
    const prepared = await prepareIdylliumBrowserProgram(options);
    if (!prepared.compilation.success || !prepared.runtime) {
        return {
            success: false,
            output: '',
            runtimeError: null,
            compilation: prepared.compilation,
            runtime: null,
            files: prepared.fileSystemSnapshot(),
            writtenFiles: prepared.writtenFilesSnapshot(),
            audio: [],
            windows: [],
            canvases: [],
            modals: [],
        };
    }
    try {
        await prepared.run();
        return browserRunSuccess(prepared.runtime, prepared.compilation, prepared.fileSystemSnapshot(), prepared.writtenFilesSnapshot());
    }
    catch (error) {
        return {
            success: false,
            output: prepared.runtime.getOutput(),
            runtimeError: error instanceof Error ? error.message : String(error),
            compilation: prepared.compilation,
            runtime: prepared.runtime,
            files: prepared.fileSystemSnapshot(),
            writtenFiles: prepared.writtenFilesSnapshot(),
            audio: prepared.runtime.getAudio(),
            windows: prepared.runtime.getWindows(),
            canvases: prepared.runtime.getCanvases(),
            modals: prepared.runtime.getModals(),
        };
    }
}
async function prepareIdylliumBrowserProgram(options) {
    const files = normalizeBrowserFiles(options.files);
    const entryFile = normalizeBrowserPath(options.entryFile ?? '/workspace/main.idyl');
    const source = browserFileText(files[entryFile]);
    const fileSystem = (0, runtime_1.createMemoryRuntimeFileSystem)(files);
    const fileSystemSnapshot = () => fileSystem.snapshot?.() ?? files;
    const writtenFilesSnapshot = () => fileSystem.writtenFilesSnapshot?.() ?? {};
    const compilation = (0, run_1.compileIdyllium)(source, {
        file: entryFile,
        sources: browserSources(files),
        resolveModule(moduleName, fromFile) {
            const candidate = resolveBrowserModule(moduleName, fromFile, files);
            return candidate ? { file: candidate, source: browserFileText(files[candidate]) } : null;
        },
    });
    if (!compilation.success || !compilation.jsCode) {
        return {
            compilation,
            runtime: null,
            fileSystemSnapshot,
            writtenFilesSnapshot,
            async run() {
                // The compilation result already contains the diagnostics.
            },
        };
    }
    const runtime = createMemoryRuntime(options, fileSystem);
    const AsyncFunction = Object.getPrototypeOf(async function idle() { }).constructor;
    const factory = new AsyncFunction(compilation.jsCode);
    const program = await factory();
    return {
        compilation,
        runtime,
        fileSystemSnapshot,
        writtenFilesSnapshot,
        async run() {
            await program(runtime);
        },
    };
}
function createMemoryRuntime(options, fileSystem) {
    return (0, runtime_1.createRuntime)({
        console: options.console,
        input: options.input,
        fileSystem,
    });
}
function browserRunSuccess(runtime, compilation, files, writtenFiles) {
    return {
        success: true,
        output: runtime.getOutput(),
        runtimeError: null,
        compilation,
        runtime,
        files,
        writtenFiles,
        audio: runtime.getAudio(),
        windows: runtime.getWindows(),
        canvases: runtime.getCanvases(),
        modals: runtime.getModals(),
    };
}
function normalizeBrowserFiles(files) {
    const normalized = {};
    for (const [file, source] of Object.entries(files)) {
        normalized[normalizeBrowserPath(file)] = source;
    }
    return normalized;
}
function browserSources(files) {
    const sources = {};
    for (const [file, source] of Object.entries(files)) {
        if (file.endsWith('.idyl'))
            sources[file] = browserFileText(source);
    }
    return sources;
}
function browserFileText(file) {
    if (typeof file === 'string')
        return file;
    return file?.content ?? '';
}
function resolveBrowserModule(moduleName, fromFile, files) {
    const candidates = [
        normalizeBrowserPath(moduleName),
        normalizeBrowserPath(`${moduleName}.idyl`),
        normalizeBrowserPath(`${browserDirname(fromFile)}/${moduleName}.idyl`),
    ];
    return candidates.find((candidate) => Object.prototype.hasOwnProperty.call(files, candidate)) ?? null;
}
function normalizeBrowserPath(value, base = '/workspace') {
    const raw = value.replace(/\\/gu, '/');
    const parts = (raw.startsWith('/') ? raw : `${base}/${raw}`).split('/');
    const normalized = [];
    for (const part of parts) {
        if (part === '' || part === '.')
            continue;
        if (part === '..') {
            normalized.pop();
            continue;
        }
        normalized.push(part);
    }
    return `/${normalized.join('/')}`;
}
function browserDirname(filePath) {
    const normalized = normalizeBrowserPath(filePath);
    if (normalized === '/')
        return '/';
    const index = normalized.lastIndexOf('/');
    if (index <= 0)
        return '/';
    return normalized.slice(0, index);
}
//# sourceMappingURL=browser.js.map
},
"src/core/codegen.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.JavaScriptGenerator = void 0;
const types_1 = require("./types");
const registry_1 = require("./stdlib/registry");
class JavaScriptGenerator {
    importedModules = new Set();
    userClassNames = new Set();
    classFields = new Map();
    functionParameters = new Map();
    classMethodParameters = new Map();
    classConstructorParameters = new Map();
    moduleFunctionParameters = new Map();
    moduleClassMethodParameters = new Map();
    moduleClassConstructorParameters = new Map();
    scopes = [new Map()];
    currentClassNames = [];
    returnTypes = [];
    userModuleNames;
    stdlib = (0, registry_1.createDefaultStandardLibrary)();
    constructor(options = {}) {
        this.userModuleNames = options.userModuleNames ?? new Set();
    }
    generate(program, options = {}) {
        const modules = options.modules ?? [];
        this.prepareModuleSignatures(modules);
        this.importedModules = new Set(program.imports.map((item) => item.moduleName));
        this.userClassNames = new Set(program.declarations
            .filter((item) => item.kind === 'ClassDeclaration')
            .map((item) => item.name));
        const lines = [];
        lines.push('return async function __idylliumProgram($rt) {');
        for (const module of modules) {
            this.emitModule(module, lines, 1);
        }
        this.prepareProgramState(program);
        this.emitProgramDeclarations(program, lines, 1);
        if (program.main) {
            lines.push('  async function main() {');
            this.returnTypes.push(program.main.returnType);
            this.emitBlock(program.main.body, lines, 2);
            this.returnTypes.pop();
            lines.push('  }');
            lines.push('  await main();');
        }
        lines.push('};');
        return { jsCode: lines.join('\n') };
    }
    emitModule(module, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}$rt.modules.${module.name} = await (async function() {`);
        this.prepareProgramState(module.program);
        this.emitProgramDeclarations(module.program, lines, indent + 1);
        lines.push(`${pad}  return {`);
        for (const declaration of module.program.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                lines.push(`${pad}    ${JSON.stringify(declaration.name)}: ${declaration.name},`);
            }
            if (declaration.kind === 'VariableDeclaration') {
                lines.push(`${pad}    ${JSON.stringify(declaration.name)}: ${declaration.name},`);
            }
            if (declaration.kind === 'ClassDeclaration') {
                lines.push(`${pad}    ${JSON.stringify(declaration.name)}: ${this.classObjectName(declaration.name)},`);
                lines.push(`${pad}    ${JSON.stringify(this.exportedClassCreateName(declaration.name))}: ${this.classCreateFactoryName(declaration.name)},`);
                lines.push(`${pad}    ${JSON.stringify(this.exportedClassDefaultName(declaration.name))}: ${this.classDefaultFactoryName(declaration.name)},`);
            }
        }
        lines.push(`${pad}  };`);
        lines.push(`${pad}})();`);
    }
    prepareProgramState(program) {
        this.importedModules = new Set(program.imports.map((item) => item.moduleName));
        this.userClassNames = new Set(program.declarations
            .filter((item) => item.kind === 'ClassDeclaration')
            .map((item) => item.name));
        this.classFields = new Map();
        this.functionParameters = new Map();
        this.classMethodParameters = new Map();
        this.classConstructorParameters = new Map();
        const ownClassFields = new Map();
        for (const declaration of program.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                this.functionParameters.set(declaration.name, declaration.parameters);
            }
            if (declaration.kind !== 'ClassDeclaration')
                continue;
            const fields = new Map();
            for (const member of declaration.members) {
                if (member.kind === 'ClassFieldDeclaration') {
                    for (const field of member.fields)
                        fields.set(field.name, member.declaredType);
                }
                if (member.kind === 'ClassMethodDeclaration') {
                    this.classMethodParameters.set(this.classMemberKey(declaration.name, member.name), member.parameters);
                }
                if (member.kind === 'ConstructorDeclaration') {
                    this.classConstructorParameters.set(declaration.name, member.parameters);
                }
            }
            ownClassFields.set(declaration.name, fields);
        }
        const collectFields = (className, seen = new Set()) => {
            if (this.classFields.has(className))
                return this.classFields.get(className) ?? new Map();
            if (seen.has(className))
                return ownClassFields.get(className) ?? new Map();
            seen.add(className);
            const declaration = program.declarations.find((item) => (item.kind === 'ClassDeclaration' && item.name === className));
            const fields = new Map();
            if (declaration?.baseName) {
                for (const [name, type] of collectFields(declaration.baseName, seen)) {
                    fields.set(name, type);
                }
            }
            for (const [name, type] of ownClassFields.get(className) ?? new Map()) {
                fields.set(name, type);
            }
            this.classFields.set(className, fields);
            return fields;
        };
        for (const className of ownClassFields.keys()) {
            collectFields(className);
        }
        this.scopes = [new Map()];
        this.currentClassNames = [];
        this.returnTypes = [];
    }
    prepareModuleSignatures(modules) {
        this.moduleFunctionParameters = new Map();
        this.moduleClassMethodParameters = new Map();
        this.moduleClassConstructorParameters = new Map();
        for (const module of modules) {
            for (const declaration of module.program.declarations) {
                if (declaration.kind === 'FunctionDeclaration') {
                    this.moduleFunctionParameters.set(`${module.name}.${declaration.name}`, declaration.parameters);
                    continue;
                }
                if (declaration.kind !== 'ClassDeclaration')
                    continue;
                for (const member of declaration.members) {
                    if (member.kind === 'ClassMethodDeclaration') {
                        this.moduleClassMethodParameters.set(`${module.name}.${this.classMemberKey(declaration.name, member.name)}`, member.parameters);
                    }
                    if (member.kind === 'ConstructorDeclaration') {
                        this.moduleClassConstructorParameters.set(`${module.name}.${declaration.name}`, member.parameters);
                    }
                }
            }
        }
    }
    emitProgramDeclarations(program, lines, indent) {
        for (const declaration of program.declarations) {
            if (declaration.kind === 'ClassDeclaration') {
                this.emitClassDeclaration(declaration, lines, indent);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'VariableDeclaration') {
                this.emitVariableDeclaration(declaration, lines, indent);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                this.emitFunctionDeclaration(declaration, lines, indent);
            }
        }
    }
    emitBlock(block, lines, indent) {
        this.pushScope();
        for (const statement of block.statements) {
            this.emitStatement(statement, lines, indent);
        }
        this.popScope();
    }
    emitStatement(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        switch (statement.kind) {
            case 'BlockStatement':
                lines.push(`${pad}{`);
                this.emitBlock(statement, lines, indent + 1);
                lines.push(`${pad}}`);
                return;
            case 'IfStatement':
                lines.push(`${pad}if (${this.expression(statement.condition)}) {`);
                this.emitStatementBody(statement.thenBranch, lines, indent + 1);
                if (statement.elseBranch) {
                    lines.push(`${pad}} else {`);
                    this.emitStatementBody(statement.elseBranch, lines, indent + 1);
                }
                lines.push(`${pad}}`);
                return;
            case 'WhileStatement':
                this.emitWhileStatement(statement, lines, indent);
                return;
            case 'DoWhileStatement':
                this.emitDoWhileStatement(statement, lines, indent);
                return;
            case 'ForStatement':
                this.emitForStatement(statement, lines, indent);
                return;
            case 'BreakStatement':
                this.emitBreakStatement(statement, lines, indent);
                return;
            case 'ContinueStatement':
                this.emitContinueStatement(statement, lines, indent);
                return;
            case 'ReturnStatement':
                this.emitReturnStatement(statement, lines, indent);
                return;
            case 'VariableDeclaration':
                this.emitVariableDeclaration(statement, lines, indent);
                return;
            case 'AssignmentStatement':
                this.emitAssignment(statement, lines, indent);
                return;
            case 'ExpressionStatement':
                lines.push(`${pad}${this.maybeAwait(statement.expression)};`);
                return;
        }
    }
    emitStatementBody(statement, lines, indent) {
        if (statement.kind === 'BlockStatement') {
            this.emitBlock(statement, lines, indent);
            return;
        }
        this.emitStatement(statement, lines, indent);
    }
    emitVariableDeclaration(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}${this.variableDeclarationCode(statement)};`);
    }
    emitFunctionDeclaration(declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        const params = declaration.parameters.map((parameter) => parameter.name).join(', ');
        lines.push(`${pad}async function ${declaration.name}(${params}) {`);
        this.pushScope();
        this.returnTypes.push(declaration.returnType);
        this.emitParameterDefaults(declaration.parameters, lines, indent + 1);
        this.emitParameterCasts(declaration.parameters, lines, indent + 1);
        this.emitBlock(declaration.body, lines, indent + 1);
        this.returnTypes.pop();
        this.popScope();
        lines.push(`${pad}}`);
    }
    emitClassDeclaration(declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        const classObjectName = this.classObjectName(declaration.name);
        lines.push(`${pad}const ${classObjectName} = {};`);
        lines.push(`${pad}function ${this.classDefaultFactoryName(declaration.name)}() {`);
        if (declaration.baseName) {
            lines.push(`${pad}  const self = ${this.classDefaultFactoryName(declaration.baseName)}();`);
            lines.push(`${pad}  self.__idylliumType = ${JSON.stringify(declaration.name)};`);
        }
        else {
            lines.push(`${pad}  const self = { __idylliumType: ${JSON.stringify(declaration.name)} };`);
        }
        for (const member of declaration.members) {
            if (member.kind === 'ClassFieldDeclaration') {
                this.emitClassFieldDefaults(member, lines, indent + 1);
            }
        }
        for (const member of declaration.members) {
            if (member.kind === 'ClassMethodDeclaration' && !member.isStatic) {
                this.emitInstanceMethod(declaration.name, member, lines, indent + 1);
            }
        }
        lines.push(`${pad}  return self;`);
        lines.push(`${pad}}`);
        const constructor = declaration.members.find((member) => member.kind === 'ConstructorDeclaration');
        lines.push(`${pad}async function ${this.classInitFunctionName(declaration.name)}(self, ...__args) {`);
        if (constructor) {
            if (declaration.baseName) {
                lines.push(`${pad}  const parent = async (...__parentArgs) => {`);
                lines.push(`${pad}    await ${this.classInitFunctionName(declaration.baseName)}(self, ...__parentArgs);`);
                lines.push(`${pad}  };`);
            }
            this.emitConstructorCall(declaration.name, constructor, lines, indent + 1);
        }
        lines.push(`${pad}}`);
        lines.push(`${pad}async function ${this.classCreateFactoryName(declaration.name)}(...__args) {`);
        lines.push(`${pad}  const self = ${this.classDefaultFactoryName(declaration.name)}();`);
        lines.push(`${pad}  await ${this.classInitFunctionName(declaration.name)}(self, ...__args);`);
        lines.push(`${pad}  return self;`);
        lines.push(`${pad}}`);
        for (const member of declaration.members) {
            if (member.kind === 'ClassMethodDeclaration' && member.isStatic) {
                this.emitStaticMethod(declaration.name, member, lines, indent);
            }
        }
    }
    emitClassFieldDefaults(declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        for (const field of declaration.fields) {
            const rawValue = field.initializer
                ? this.expression(field.initializer)
                : this.defaultValue(declaration.declaredType, false);
            const value = this.castForType(rawValue, declaration.declaredType);
            lines.push(`${pad}self.${field.name} = ${value};`);
        }
    }
    emitInstanceMethod(className, declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        const params = declaration.parameters.map((parameter) => parameter.name).join(', ');
        lines.push(`${pad}self.${declaration.name} = async function(${params}) {`);
        this.pushScope();
        this.currentClassNames.push(className);
        this.returnTypes.push(declaration.returnType);
        this.emitParameterDefaults(declaration.parameters, lines, indent + 1);
        this.emitParameterCasts(declaration.parameters, lines, indent + 1);
        this.emitBlock(declaration.body, lines, indent + 1);
        this.returnTypes.pop();
        this.currentClassNames.pop();
        this.popScope();
        lines.push(`${pad}};`);
    }
    emitStaticMethod(className, declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        const params = declaration.parameters.map((parameter) => parameter.name).join(', ');
        lines.push(`${pad}${this.classObjectName(className)}.${declaration.name} = async function(${params}) {`);
        this.pushScope();
        this.currentClassNames.push(className);
        this.returnTypes.push(declaration.returnType);
        this.emitParameterDefaults(declaration.parameters, lines, indent + 1);
        this.emitParameterCasts(declaration.parameters, lines, indent + 1);
        this.emitBlock(declaration.body, lines, indent + 1);
        this.returnTypes.pop();
        this.currentClassNames.pop();
        this.popScope();
        lines.push(`${pad}};`);
    }
    emitConstructorCall(className, declaration, lines, indent) {
        const pad = '  '.repeat(indent);
        const params = declaration.parameters.map((parameter) => parameter.name).join(', ');
        lines.push(`${pad}await (async function(${params}) {`);
        this.pushScope();
        this.currentClassNames.push(className);
        this.emitParameterDefaults(declaration.parameters, lines, indent + 1);
        this.emitParameterCasts(declaration.parameters, lines, indent + 1);
        this.emitBlock(declaration.body, lines, indent + 1);
        this.currentClassNames.pop();
        this.popScope();
        lines.push(`${pad}}).apply(self, __args);`);
    }
    emitAssignment(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}${this.assignmentCode(statement)};`);
    }
    emitWhileStatement(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}while (${this.expression(statement.condition)}) {`);
        this.emitStatementBody(statement.body, lines, indent + 1);
        lines.push(`${pad}}`);
    }
    emitDoWhileStatement(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}do {`);
        this.emitStatementBody(statement.body, lines, indent + 1);
        lines.push(`${pad}} while (${this.expression(statement.condition)});`);
    }
    emitForStatement(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        this.pushScope();
        const initializer = statement.initializer ? this.forClauseCode(statement.initializer) : '';
        const condition = statement.condition ? this.expression(statement.condition) : '';
        const increment = statement.increment ? this.forClauseCode(statement.increment) : '';
        lines.push(`${pad}for (${initializer}; ${condition}; ${increment}) {`);
        this.emitStatementBody(statement.body, lines, indent + 1);
        lines.push(`${pad}}`);
        this.popScope();
    }
    emitBreakStatement(_statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}break;`);
    }
    emitContinueStatement(_statement, lines, indent) {
        const pad = '  '.repeat(indent);
        lines.push(`${pad}continue;`);
    }
    emitReturnStatement(statement, lines, indent) {
        const pad = '  '.repeat(indent);
        const rawValue = statement.value ? this.expression(statement.value) : '';
        const returnType = this.returnTypes[this.returnTypes.length - 1] ?? null;
        const value = statement.value
            ? ` ${returnType ? this.castForType(rawValue, returnType) : rawValue}`
            : '';
        lines.push(`${pad}return${value};`);
    }
    forClauseCode(statement) {
        switch (statement.kind) {
            case 'VariableDeclaration':
                return this.variableDeclarationCode(statement);
            case 'AssignmentStatement':
                return this.assignmentCode(statement);
            case 'ExpressionStatement':
                return this.expression(statement.expression);
        }
    }
    variableDeclarationCode(statement) {
        const rawValue = statement.initializer
            ? this.initializerExpression(statement.initializer, statement.declaredType)
            : statement.constructorArgs
                ? this.constructorInitializer(statement)
                : this.defaultValue(statement.declaredType);
        const value = this.castForType(rawValue, statement.declaredType);
        this.declareType(statement.name, statement.declaredType);
        return `let ${statement.name} = ${value}`;
    }
    assignmentCode(statement) {
        const targetType = this.targetTypeName(statement.target);
        if (statement.operator === '=') {
            if (statement.target.kind === 'IndexExpression') {
                const value = this.castForType(this.expression(statement.value), targetType);
                return `$rt.array.set(${this.expression(statement.target.object)}, ${this.expression(statement.target.index)}, ${value}, ${JSON.stringify(statement.target.range.start.file)}, ${statement.target.range.start.line})`;
            }
            if (statement.target.kind === 'MemberExpression') {
                const value = this.castForType(this.expression(statement.value), targetType);
                return `$rt.setProperty(${this.expression(statement.target.object)}, ${JSON.stringify(statement.target.name)}, ${value}, ${JSON.stringify(statement.target.range.start.file)}, ${statement.target.range.start.line})`;
            }
            return `${this.expression(statement.target)} = ${this.castForType(this.expression(statement.value), targetType)}`;
        }
        if (statement.target.kind === 'IndexExpression') {
            const object = this.expression(statement.target.object);
            const index = this.expression(statement.target.index);
            const current = `$rt.array.get(${object}, ${index}, ${JSON.stringify(statement.target.range.start.file)}, ${statement.target.range.start.line})`;
            const rawValue = this.compoundAssignmentValue(statement.operator, current, this.expression(statement.value), statement.range);
            const value = this.castForType(rawValue, targetType);
            return `$rt.array.set(${object}, ${index}, ${value}, ${JSON.stringify(statement.target.range.start.file)}, ${statement.target.range.start.line})`;
        }
        const target = this.expression(statement.target);
        const value = this.expression(statement.value);
        const rawAssignedValue = this.compoundAssignmentValue(statement.operator, target, value, statement.range);
        if (statement.target.kind === 'MemberExpression') {
            const assignedValue = this.castForType(rawAssignedValue, targetType);
            return `$rt.setProperty(${this.expression(statement.target.object)}, ${JSON.stringify(statement.target.name)}, ${assignedValue}, ${JSON.stringify(statement.target.range.start.file)}, ${statement.target.range.start.line})`;
        }
        return `${target} = ${this.castForType(rawAssignedValue, targetType)}`;
    }
    compoundAssignmentValue(operator, target, value, range) {
        const binaryOperator = operator.slice(0, 1);
        if (binaryOperator === '/') {
            return `$rt.core.divide(${target}, ${value}, ${JSON.stringify(range.start.file)}, ${range.start.line})`;
        }
        return `(${target} ${binaryOperator} ${value})`;
    }
    maybeAwait(expression) {
        return this.expression(expression);
    }
    expression(expression) {
        switch (expression.kind) {
            case 'LiteralExpression':
                return JSON.stringify(expression.value);
            case 'IdentifierExpression':
                if (expression.name === 'this')
                    return 'this';
                if (this.userClassNames.has(expression.name))
                    return this.classObjectName(expression.name);
                return expression.name;
            case 'UnaryExpression':
                return expression.operator === 'not'
                    ? `(!${this.expression(expression.operand)})`
                    : `(-${this.expression(expression.operand)})`;
            case 'BinaryExpression':
                return this.binaryExpression(expression);
            case 'ArrayLiteralExpression':
                return this.arrayLiteralExpression(expression, true, null, '() => 0');
            case 'IndexExpression':
                return `$rt.array.get(${this.expression(expression.object)}, ${this.expression(expression.index)}, ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
            case 'FunctionExpression':
                return this.functionExpression(expression);
            case 'CallExpression':
                return `(await ${this.callExpression(expression)})`;
            case 'MemberExpression':
                return this.memberExpression(expression);
        }
    }
    functionExpression(expression) {
        const params = expression.parameters.map((parameter) => parameter.name).join(', ');
        const lines = [`(async function(${params}) {`];
        this.pushScope();
        this.returnTypes.push(expression.returnType);
        this.emitParameterDefaults(expression.parameters, lines, 1);
        this.emitParameterCasts(expression.parameters, lines, 1);
        this.emitBlock(expression.body, lines, 1);
        this.returnTypes.pop();
        this.popScope();
        lines.push('})');
        return lines.join('\n');
    }
    binaryExpression(expression) {
        const left = this.expression(expression.left);
        const right = this.expression(expression.right);
        if (expression.operator === '/') {
            return `$rt.core.divide(${left}, ${right}, ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
        }
        if (expression.operator === 'and') {
            return `(${left} && ${right})`;
        }
        if (expression.operator === 'or') {
            return `(${left} || ${right})`;
        }
        return `(${left} ${expression.operator} ${right})`;
    }
    callExpression(expression) {
        const callee = expression.callee;
        if (callee.kind === 'IdentifierExpression') {
            if (callee.name === 'max' || callee.name === 'min' || callee.name === 'sum' || callee.name === 'avg') {
                const args = this.callArgumentValues(expression.args, ['array']).join(', ');
                return `$rt.array.${callee.name}(${args}, ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
            }
            if (callee.name === 'div' || callee.name === 'mod' || callee.name === 'to_int' || callee.name === 'to_float') {
                const args = this.callArgumentValues(expression.args, this.stdlib.getGlobalFunction(callee.name)?.parameters.map((parameter) => parameter.name)).join(', ');
                return `$rt.core.${callee.name}(${args}, ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
            }
            if (callee.name === 'to_string') {
                const args = this.callArgumentValues(expression.args, this.stdlib.getGlobalFunction(callee.name)?.parameters.map((parameter) => parameter.name)).join(', ');
                return `$rt.core.to_string(${args})`;
            }
        }
        if (callee.kind === 'MemberExpression' && callee.object.kind === 'IdentifierExpression') {
            const moduleName = callee.object.name;
            if (this.importedModules.has(moduleName) && moduleName === 'console') {
                const args = this.callArgumentValues(expression.args, this.stdlib.getModuleFunction(moduleName, callee.name)?.parameters.map((parameter) => parameter.name)).join(', ');
                if (callee.name === 'get_int' || callee.name === 'get_float') {
                    return `$rt.console.${callee.name}(${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
                }
                if (callee.name === 'set_precision') {
                    return `$rt.console.set_precision(${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line}, ${args})`;
                }
                return `$rt.console.${callee.name}(${args})`;
            }
            if (this.importedModules.has(moduleName)) {
                const parameterNames = this.moduleFunctionParameterNames(moduleName, callee.name);
                const args = this.callArgumentValues(expression.args, parameterNames).join(', ');
                if (!this.userModuleNames.has(moduleName)) {
                    return `$rt.callModuleFunction(${JSON.stringify(moduleName)}, ${JSON.stringify(callee.name)}, [${args}], ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
                }
                return `$rt.modules.${moduleName}.${callee.name}(${args})`;
            }
        }
        if (callee.kind === 'MemberExpression') {
            const typeName = this.expressionTypeName(callee.object);
            const typesRuntimeName = this.typesRuntimeName(typeName);
            if (typesRuntimeName && (callee.name === 'to_bin' || callee.name === 'to_hex')) {
                return `$rt.types.${callee.name}(${this.expression(callee.object)}, ${JSON.stringify(typesRuntimeName)})`;
            }
            const args = this.methodCallArgs(callee.name, expression.args, typeName).join(', ');
            return `$rt.callMethod(${this.expression(callee.object)}, ${JSON.stringify(callee.name)}, [${args}], ${JSON.stringify(expression.range.start.file)}, ${expression.range.start.line})`;
        }
        return `${this.expression(callee)}(${this.callArgumentValues(expression.args, this.callableParameterNames(callee)).join(', ')})`;
    }
    memberExpression(expression) {
        if (expression.object.kind === 'IdentifierExpression') {
            const moduleName = expression.object.name;
            if (this.importedModules.has(moduleName)) {
                return `$rt.modules.${moduleName}.${expression.name}`;
            }
            if (this.userClassNames.has(moduleName)) {
                return `${this.classObjectName(moduleName)}.${expression.name}`;
            }
        }
        return `${this.expression(expression.object)}.${expression.name}`;
    }
    initializerExpression(expression, declaredType) {
        if (expression.kind === 'ArrayLiteralExpression' && declaredType.kind === 'ArrayTypeName') {
            return this.arrayLiteralExpression(expression, declaredType.dynamic, declaredType.dynamic ? null : declaredType.size, `() => ${this.defaultValue(declaredType.elementType, false)}`, declaredType.elementType);
        }
        return this.expression(expression);
    }
    constructorInitializer(statement) {
        if (statement.declaredType.kind === 'QualifiedTypeName' && this.userModuleNames.has(statement.declaredType.moduleName)) {
            const args = this.callArgumentValues(statement.constructorArgs ?? [], this.constructorParameterNames(statement.declaredType)).join(', ');
            return `await $rt.modules.${statement.declaredType.moduleName}.${this.exportedClassCreateName(statement.declaredType.name)}(${args})`;
        }
        if (statement.declaredType.kind === 'QualifiedTypeName'
            && statement.declaredType.moduleName === 'json'
            && statement.declaredType.name === 'Value') {
            const args = this.callArgumentValues(statement.constructorArgs ?? [], this.stdlib.getModuleFunction('json', 'Value')?.parameters.map((parameter) => parameter.name)).join(', ');
            return `$rt.callModuleFunction("json", "Value", [${args}], ${JSON.stringify(statement.range.start.file)}, ${statement.range.start.line})`;
        }
        if (statement.declaredType.kind !== 'ClassTypeName')
            return this.defaultValue(statement.declaredType);
        const args = this.callArgumentValues(statement.constructorArgs ?? [], this.constructorParameterNames(statement.declaredType)).join(', ');
        return `await ${this.classCreateFactoryName(statement.declaredType.name)}(${args})`;
    }
    arrayLiteralExpression(expression, dynamic, staticSize, defaultFactory, elementType = null) {
        const values = expression.elements
            .map((element) => this.castForType(this.expression(element), elementType))
            .join(', ');
        const size = staticSize === null ? 'null' : String(staticSize);
        return `$rt.array.from([${values}], ${dynamic ? 'true' : 'false'}, ${size}, ${defaultFactory})`;
    }
    defaultValue(type, allowAwait = true) {
        const runtimeTypeName = this.typesRuntimeName(type);
        if (runtimeTypeName)
            return `$rt.types.cast(0, ${JSON.stringify(runtimeTypeName)})`;
        if (type.kind === 'ArrayTypeName') {
            const size = type.dynamic ? 0 : type.size ?? 0;
            return `$rt.array.create(${size}, () => ${this.defaultValue(type.elementType, false)}, ${type.dynamic ? 'true' : 'false'})`;
        }
        if (type.kind === 'ClassTypeName') {
            return allowAwait
                ? `await ${this.classCreateFactoryName(type.name)}()`
                : `${this.classDefaultFactoryName(type.name)}()`;
        }
        if (type.kind === 'QualifiedTypeName') {
            if (this.userModuleNames.has(type.moduleName)) {
                return allowAwait
                    ? `await $rt.modules.${type.moduleName}.${this.exportedClassCreateName(type.name)}()`
                    : `$rt.modules.${type.moduleName}.${this.exportedClassDefaultName(type.name)}()`;
            }
            if (type.moduleName === 'colors' && type.name === 'Color') {
                return '$rt.modules.colors.TRANSPARENT';
            }
            return `$rt.createObject(${JSON.stringify(type.moduleName)}, ${JSON.stringify(type.name)})`;
        }
        switch (type.name) {
            case 'string':
                return JSON.stringify('');
            case 'char':
                return JSON.stringify('\0');
            case 'bool':
                return 'false';
            default:
                return '0';
        }
    }
    classDefaultFactoryName(className) {
        return `__idyl_default_${className}`;
    }
    classCreateFactoryName(className) {
        return `__idyl_create_${className}`;
    }
    classInitFunctionName(className) {
        return `__idyl_init_${className}`;
    }
    classObjectName(className) {
        return `__idyl_class_${className}`;
    }
    exportedClassCreateName(className) {
        return `__create_${className}`;
    }
    exportedClassDefaultName(className) {
        return `__default_${className}`;
    }
    emitParameterCasts(parameters, lines, indent) {
        const pad = '  '.repeat(indent);
        for (const parameter of parameters) {
            this.declareType(parameter.name, parameter.paramType);
            const value = this.castForType(parameter.name, parameter.paramType);
            if (value !== parameter.name) {
                lines.push(`${pad}${parameter.name} = ${value};`);
            }
        }
    }
    emitParameterDefaults(parameters, lines, indent) {
        const pad = '  '.repeat(indent);
        for (const parameter of parameters) {
            if (!parameter.defaultValue)
                continue;
            lines.push(`${pad}if (${parameter.name} === undefined) {`);
            lines.push(`${pad}  ${parameter.name} = ${this.expression(parameter.defaultValue)};`);
            lines.push(`${pad}}`);
        }
    }
    callArgumentValues(args, parameterNames) {
        return this.orderedCallArguments(args, parameterNames).map((arg) => (arg ? this.expression(arg.value) : 'undefined'));
    }
    orderedCallArguments(args, parameterNames) {
        if (!args.some((arg) => arg.name !== null) || !parameterNames)
            return [...args];
        const ordered = new Array(parameterNames.length).fill(null);
        let positionalIndex = 0;
        let lastProvidedIndex = -1;
        for (const arg of args) {
            if (arg.name !== null) {
                const parameterIndex = parameterNames.indexOf(arg.name);
                if (parameterIndex >= 0) {
                    ordered[parameterIndex] = arg;
                    lastProvidedIndex = Math.max(lastProvidedIndex, parameterIndex);
                }
                continue;
            }
            if (positionalIndex < ordered.length) {
                ordered[positionalIndex] = arg;
                lastProvidedIndex = Math.max(lastProvidedIndex, positionalIndex);
            }
            positionalIndex += 1;
        }
        if (lastProvidedIndex < 0)
            return [];
        return ordered.slice(0, lastProvidedIndex + 1);
    }
    callableParameterNames(callee) {
        if (callee.kind === 'IdentifierExpression') {
            return this.functionParameters.get(callee.name)?.map((parameter) => parameter.name)
                ?? this.stdlib.getGlobalFunction(callee.name)?.parameters.map((parameter) => parameter.name);
        }
        return undefined;
    }
    moduleFunctionParameterNames(moduleName, functionName) {
        if (this.userModuleNames.has(moduleName)) {
            return this.moduleFunctionParameters.get(`${moduleName}.${functionName}`)?.map((parameter) => parameter.name);
        }
        return this.stdlib.getModuleFunction(moduleName, functionName)?.parameters.map((parameter) => parameter.name);
    }
    constructorParameterNames(type) {
        if (type.kind === 'ClassTypeName') {
            return this.classConstructorParameters.get(type.name)?.map((parameter) => parameter.name);
        }
        if (type.kind === 'QualifiedTypeName' && this.userModuleNames.has(type.moduleName)) {
            return this.moduleClassConstructorParameters.get(`${type.moduleName}.${type.name}`)?.map((parameter) => parameter.name);
        }
        return undefined;
    }
    methodParameterNames(methodName, receiverType) {
        if (!receiverType)
            return undefined;
        if (receiverType.kind === 'ClassTypeName') {
            return this.classMethodParameters.get(this.classMemberKey(receiverType.name, methodName))?.map((parameter) => parameter.name);
        }
        if (receiverType.kind === 'QualifiedTypeName' && this.userModuleNames.has(receiverType.moduleName)) {
            return this.moduleClassMethodParameters.get(`${receiverType.moduleName}.${this.classMemberKey(receiverType.name, methodName)}`)?.map((parameter) => parameter.name);
        }
        if (receiverType.kind === 'ArrayTypeName') {
            return this.arrayMethodParameterNames(methodName);
        }
        const typeRef = this.typeRefFromTypeName(receiverType);
        if (!typeRef)
            return this.stringMethodParameterNames(receiverType, methodName);
        return this.stdlib.getTypeMethod(typeRef, methodName)?.parameters.map((parameter) => parameter.name)
            ?? this.stringMethodParameterNames(receiverType, methodName);
    }
    arrayMethodParameterNames(methodName) {
        switch (methodName) {
            case 'contains':
            case 'find':
            case 'count':
            case 'add':
                return ['value'];
            case 'remove_at':
                return ['index'];
            case 'resize':
                return ['size'];
            case 'insert':
                return ['index', 'value'];
            case 'join':
                return ['other'];
            default:
                return [];
        }
    }
    stringMethodParameterNames(receiverType, methodName) {
        if (receiverType.kind !== 'PrimitiveTypeName' || receiverType.name !== 'string')
            return undefined;
        switch (methodName) {
            case 'contains':
            case 'find':
            case 'count':
                return ['text'];
            case 'substring':
                return ['start', 'length'];
            case 'replace':
                return ['old_text', 'new_text'];
            case 'split':
                return ['separator'];
            default:
                return [];
        }
    }
    typeRefFromTypeName(type) {
        if (type.kind === 'PrimitiveTypeName')
            return (0, types_1.primitive)(type.name);
        if (type.kind === 'QualifiedTypeName')
            return (0, types_1.qualified)(type.moduleName, type.name);
        if (type.kind === 'ArrayTypeName') {
            const elementType = this.typeRefFromTypeName(type.elementType);
            return elementType ? (0, types_1.arrayType)(elementType, type.size, type.dynamic) : null;
        }
        return null;
    }
    methodCallArgs(methodName, args, receiverType) {
        const orderedArgs = this.orderedCallArguments(args, this.methodParameterNames(methodName, receiverType));
        if (receiverType?.kind !== 'ArrayTypeName') {
            return orderedArgs.map((arg) => (arg ? this.expression(arg.value) : 'undefined'));
        }
        if (methodName === 'add' || methodName === 'contains' || methodName === 'find' || methodName === 'count') {
            return orderedArgs.map((arg, index) => (index === 0 && arg
                ? this.castForType(this.expression(arg.value), receiverType.elementType)
                : arg ? this.expression(arg.value) : 'undefined'));
        }
        if (methodName === 'insert') {
            return orderedArgs.map((arg, index) => (index === 1 && arg
                ? this.castForType(this.expression(arg.value), receiverType.elementType)
                : arg ? this.expression(arg.value) : 'undefined'));
        }
        return orderedArgs.map((arg) => (arg ? this.expression(arg.value) : 'undefined'));
    }
    classMemberKey(className, memberName) {
        return `${className}.${memberName}`;
    }
    castForType(value, type) {
        const runtimeName = this.typesRuntimeName(type);
        if (!runtimeName)
            return value;
        return `$rt.types.cast(${value}, ${JSON.stringify(runtimeName)})`;
    }
    typesRuntimeName(type) {
        if (type?.kind !== 'QualifiedTypeName')
            return null;
        if (type.moduleName !== 'types')
            return null;
        return TYPE_RUNTIME_NAMES.has(type.name) ? type.name : null;
    }
    targetTypeName(expression) {
        if (expression.kind === 'IndexExpression') {
            const objectType = this.expressionTypeName(expression.object);
            return objectType?.kind === 'ArrayTypeName' ? objectType.elementType : null;
        }
        return this.expressionTypeName(expression);
    }
    expressionTypeName(expression) {
        switch (expression.kind) {
            case 'IdentifierExpression':
                return this.lookupType(expression.name);
            case 'MemberExpression':
                return this.memberTypeName(expression);
            case 'IndexExpression': {
                const objectType = this.expressionTypeName(expression.object);
                return objectType?.kind === 'ArrayTypeName' ? objectType.elementType : null;
            }
            default:
                return null;
        }
    }
    memberTypeName(expression) {
        if (expression.object.kind === 'IdentifierExpression' && expression.object.name === 'this') {
            const className = this.currentClassNames[this.currentClassNames.length - 1] ?? null;
            return className ? this.classFields.get(className)?.get(expression.name) ?? null : null;
        }
        const objectType = this.expressionTypeName(expression.object);
        if (objectType?.kind !== 'ClassTypeName')
            return null;
        return this.classFields.get(objectType.name)?.get(expression.name) ?? null;
    }
    declareType(name, type) {
        this.scopes[this.scopes.length - 1].set(name, type);
    }
    lookupType(name) {
        for (let i = this.scopes.length - 1; i >= 0; i--) {
            const type = this.scopes[i].get(name);
            if (type)
                return type;
        }
        return null;
    }
    pushScope() {
        this.scopes.push(new Map());
    }
    popScope() {
        this.scopes.pop();
    }
}
exports.JavaScriptGenerator = JavaScriptGenerator;
const TYPE_RUNTIME_NAMES = new Set([
    'int8',
    'uint8',
    'int16',
    'uint16',
    'int32',
    'uint32',
    'float32',
    'float64',
]);
//# sourceMappingURL=codegen.js.map
},
"src/core/diagnostics.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DiagnosticBag = void 0;
exports.formatDiagnostic = formatDiagnostic;
exports.formatDiagnostics = formatDiagnostics;
exports.pointRange = pointRange;
class DiagnosticBag {
    diagnostics = [];
    error(range, message, code) {
        this.add('error', range, message, code);
    }
    warning(range, message, code) {
        this.add('warning', range, message, code);
    }
    info(range, message, code) {
        this.add('info', range, message, code);
    }
    add(severity, range, message, code) {
        this.diagnostics.push({ severity, range, message, code });
    }
    hasErrors() {
        return this.diagnostics.some((diagnostic) => diagnostic.severity === 'error');
    }
    all() {
        return [...this.diagnostics];
    }
    errors() {
        return this.diagnostics.filter((diagnostic) => diagnostic.severity === 'error');
    }
}
exports.DiagnosticBag = DiagnosticBag;
function formatDiagnostic(diagnostic) {
    const { start } = diagnostic.range;
    const code = diagnostic.code ? ` ${diagnostic.code}` : '';
    return `${start.file}:${start.line}:${start.column}: ${diagnostic.severity}${code}: ${diagnostic.message}`;
}
function formatDiagnostics(diagnostics) {
    return diagnostics.map(formatDiagnostic).join('\n');
}
function pointRange(location) {
    return { start: location, end: location };
}
//# sourceMappingURL=diagnostics.js.map
},
"src/core/lexer.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Lexer = void 0;
const diagnostics_1 = require("./diagnostics");
const tokens_1 = require("./tokens");
class Lexer {
    source;
    file;
    tokens = [];
    diagnostics = new diagnostics_1.DiagnosticBag();
    index = 0;
    line = 1;
    column = 1;
    constructor(source, file = 'main.idyl') {
        this.source = source;
        this.file = file;
    }
    tokenize() {
        while (!this.isAtEnd()) {
            this.scanToken();
        }
        const location = this.location();
        this.tokens.push({
            kind: tokens_1.TokenKind.EndOfFile,
            lexeme: '',
            literal: null,
            range: { start: location, end: location },
        });
        return {
            tokens: this.tokens,
            diagnostics: this.diagnostics,
        };
    }
    scanToken() {
        const start = this.location();
        const char = this.advance();
        switch (char) {
            case '(':
                this.addSimple(tokens_1.TokenKind.LeftParen, start);
                return;
            case ')':
                this.addSimple(tokens_1.TokenKind.RightParen, start);
                return;
            case '{':
                this.addSimple(tokens_1.TokenKind.LeftBrace, start);
                return;
            case '}':
                this.addSimple(tokens_1.TokenKind.RightBrace, start);
                return;
            case '[':
                this.addSimple(tokens_1.TokenKind.LeftBracket, start);
                return;
            case ']':
                this.addSimple(tokens_1.TokenKind.RightBracket, start);
                return;
            case ',':
                this.addSimple(tokens_1.TokenKind.Comma, start);
                return;
            case ';':
                this.addSimple(tokens_1.TokenKind.Semicolon, start);
                return;
            case '.':
                this.addSimple(tokens_1.TokenKind.Dot, start);
                return;
            case ':':
                this.addSimple(tokens_1.TokenKind.Colon, start);
                return;
            case '~':
                this.addSimple(tokens_1.TokenKind.Tilde, start);
                return;
            case '+':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.PlusEqual : tokens_1.TokenKind.Plus, start);
                return;
            case '-':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.MinusEqual : tokens_1.TokenKind.Minus, start);
                return;
            case '*':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.StarEqual : tokens_1.TokenKind.Star, start);
                return;
            case '/':
                if (this.match('/')) {
                    this.skipLineComment();
                    return;
                }
                if (this.match('*')) {
                    this.skipBlockComment(start);
                    return;
                }
                this.addSimple(this.match('=') ? tokens_1.TokenKind.SlashEqual : tokens_1.TokenKind.Slash, start);
                return;
            case '=':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.EqualEqual : tokens_1.TokenKind.Equal, start);
                return;
            case '!':
                if (this.match('=')) {
                    this.addSimple(tokens_1.TokenKind.BangEqual, start);
                    return;
                }
                this.bad(start, "unexpected character '!'");
                return;
            case '<':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.LessEqual : tokens_1.TokenKind.Less, start);
                return;
            case '>':
                this.addSimple(this.match('=') ? tokens_1.TokenKind.GreaterEqual : tokens_1.TokenKind.Greater, start);
                return;
            case '"':
                this.scanString(start);
                return;
            case "'":
                this.scanChar(start);
                return;
            case ' ':
            case '\r':
            case '\t':
                return;
            case '\n':
                return;
            default:
                if (this.isDigit(char)) {
                    this.scanNumber(start, char);
                    return;
                }
                if (this.isIdentifierStart(char)) {
                    this.scanIdentifier(start, char);
                    return;
                }
                this.bad(start, `unexpected character '${char}'`);
        }
    }
    scanNumber(start, first) {
        let text = first;
        while (this.isDigit(this.peek())) {
            text += this.advance();
        }
        let isFloat = false;
        if (this.peek() === '.' && this.isDigit(this.peekNext())) {
            isFloat = true;
            text += this.advance();
            while (this.isDigit(this.peek())) {
                text += this.advance();
            }
        }
        this.tokens.push({
            kind: isFloat ? tokens_1.TokenKind.FloatLiteral : tokens_1.TokenKind.IntLiteral,
            lexeme: text,
            literal: isFloat ? Number.parseFloat(text) : Number.parseInt(text, 10),
            range: this.rangeFrom(start),
        });
    }
    scanIdentifier(start, first) {
        let text = first;
        while (this.isIdentifierPart(this.peek())) {
            text += this.advance();
        }
        const kind = tokens_1.KEYWORDS[text] ?? tokens_1.TokenKind.Identifier;
        let literal = null;
        if (kind === tokens_1.TokenKind.KwTrue)
            literal = true;
        if (kind === tokens_1.TokenKind.KwFalse)
            literal = false;
        this.tokens.push({
            kind,
            lexeme: text,
            literal,
            range: this.rangeFrom(start),
        });
    }
    scanString(start) {
        let value = '';
        while (!this.isAtEnd() && this.peek() !== '"') {
            if (this.peek() === '\n') {
                this.bad(start, 'string literal cannot contain a raw newline');
                return;
            }
            value += this.scanEscapedCharacter(start);
        }
        if (this.isAtEnd()) {
            this.bad(start, 'unterminated string literal');
            return;
        }
        this.advance();
        this.tokens.push({
            kind: tokens_1.TokenKind.StringLiteral,
            lexeme: this.sourceSlice(start),
            literal: value,
            range: this.rangeFrom(start),
        });
    }
    scanChar(start) {
        let value = '';
        while (!this.isAtEnd() && this.peek() !== "'") {
            if (this.peek() === '\n') {
                this.bad(start, 'char literal cannot contain a raw newline');
                return;
            }
            value += this.scanEscapedCharacter(start);
        }
        if (this.isAtEnd()) {
            this.bad(start, 'unterminated char literal');
            return;
        }
        this.advance();
        if (Array.from(value).length !== 1) {
            this.diagnostics.error(this.rangeFrom(start), 'char literal must contain exactly one character');
        }
        this.tokens.push({
            kind: tokens_1.TokenKind.CharLiteral,
            lexeme: this.sourceSlice(start),
            literal: value,
            range: this.rangeFrom(start),
        });
    }
    scanEscapedCharacter(start) {
        const char = this.advance();
        if (char !== '\\') {
            return char;
        }
        if (this.isAtEnd()) {
            this.bad(start, 'unfinished escape sequence');
            return '';
        }
        const escaped = this.advance();
        switch (escaped) {
            case 'n':
                return '\n';
            case 't':
                return '\t';
            case 'r':
                return '\r';
            case 'e':
                return '\x1b';
            case '\\':
                return '\\';
            case '"':
                return '"';
            case "'":
                return "'";
            case '0':
                return '\0';
            default:
                this.diagnostics.error(this.rangeFrom(start), `unknown escape sequence '\\${escaped}'`);
                return escaped;
        }
    }
    skipLineComment() {
        while (!this.isAtEnd() && this.peek() !== '\n') {
            this.advance();
        }
    }
    skipBlockComment(start) {
        while (!this.isAtEnd()) {
            if (this.peek() === '*' && this.peekNext() === '/') {
                this.advance();
                this.advance();
                return;
            }
            this.advance();
        }
        this.diagnostics.error(this.rangeFrom(start), 'unterminated block comment');
    }
    addSimple(kind, start) {
        this.tokens.push({
            kind,
            lexeme: this.sourceSlice(start),
            literal: null,
            range: this.rangeFrom(start),
        });
    }
    bad(start, message) {
        const range = this.rangeFrom(start);
        this.diagnostics.error(range, message);
        this.tokens.push({
            kind: tokens_1.TokenKind.Bad,
            lexeme: this.sourceSlice(start),
            literal: null,
            range,
        });
    }
    advance() {
        const char = this.source[this.index] ?? '\0';
        this.index++;
        if (char === '\n') {
            this.line++;
            this.column = 1;
        }
        else {
            this.column++;
        }
        return char;
    }
    match(expected) {
        if (this.peek() !== expected)
            return false;
        this.advance();
        return true;
    }
    peek() {
        return this.source[this.index] ?? '\0';
    }
    peekNext() {
        return this.source[this.index + 1] ?? '\0';
    }
    isAtEnd() {
        return this.index >= this.source.length;
    }
    location() {
        return {
            file: this.file,
            line: this.line,
            column: this.column,
        };
    }
    rangeFrom(start) {
        return {
            start,
            end: this.location(),
        };
    }
    sourceSlice(start) {
        const absoluteStart = this.absoluteIndexFor(start);
        return this.source.slice(absoluteStart, this.index);
    }
    absoluteIndexFor(location) {
        let line = 1;
        let column = 1;
        for (let i = 0; i < this.source.length; i++) {
            if (line === location.line && column === location.column)
                return i;
            if (this.source[i] === '\n') {
                line++;
                column = 1;
            }
            else {
                column++;
            }
        }
        return this.source.length;
    }
    isDigit(char) {
        return char >= '0' && char <= '9';
    }
    isIdentifierStart(char) {
        return char === '_' || /^\p{L}$/u.test(char);
    }
    isIdentifierPart(char) {
        return this.isIdentifierStart(char) || /^\p{N}$/u.test(char);
    }
}
exports.Lexer = Lexer;
//# sourceMappingURL=lexer.js.map
},
"src/core/modules.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.UserModuleRegistry = void 0;
exports.qualifiedUserClassName = qualifiedUserClassName;
class UserModuleRegistry {
    modules = new Map();
    register(module) {
        this.modules.set(module.name, module);
    }
    hasModule(name) {
        return this.modules.has(name);
    }
    getModule(name) {
        return this.modules.get(name);
    }
    listModules() {
        return [...this.modules.values()];
    }
}
exports.UserModuleRegistry = UserModuleRegistry;
function qualifiedUserClassName(moduleName, className) {
    return `${moduleName}.${className}`;
}
//# sourceMappingURL=modules.js.map
},
"src/core/parser.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Parser = void 0;
const diagnostics_1 = require("./diagnostics");
const tokens_1 = require("./tokens");
class Parser {
    tokens;
    diagnostics = new diagnostics_1.DiagnosticBag();
    current = 0;
    constructor(tokens) {
        this.tokens = tokens;
    }
    parseProgram() {
        const start = this.peek().range.start;
        const imports = [];
        const topLevelDeclarations = [];
        let main = null;
        while (this.match(tokens_1.TokenKind.KwUse)) {
            imports.push(this.parseImport(this.previous()));
        }
        while (!this.isAtEnd()) {
            if (this.check(tokens_1.TokenKind.KwClass)) {
                topLevelDeclarations.push(this.parseClassDeclaration());
                continue;
            }
            if (this.check(tokens_1.TokenKind.KwMain)) {
                if (main !== null) {
                    this.error(this.peek().range, "entry point 'main' is already declared");
                }
                main = this.parseMainFunction();
                continue;
            }
            if (this.checkTypeStart()) {
                const declaration = this.parseTopLevelDeclaration();
                if (declaration.kind === 'FunctionDeclaration' && declaration.name === 'main') {
                    if (main !== null) {
                        this.error(declaration.range, "entry point 'main' is already declared");
                    }
                    main = this.mainFromFunctionDeclaration(declaration);
                    continue;
                }
                topLevelDeclarations.push(declaration);
                continue;
            }
            this.error(this.peek().range, `unexpected token ${(0, tokens_1.tokenDisplay)(this.peek().kind)} at top level`);
            this.advance();
        }
        const end = main?.range.end
            ?? topLevelDeclarations[topLevelDeclarations.length - 1]?.range.end
            ?? imports[imports.length - 1]?.range.end
            ?? this.peek().range.end;
        return {
            program: {
                kind: 'Program',
                imports,
                declarations: topLevelDeclarations,
                main,
                range: { start, end },
            },
            diagnostics: this.diagnostics,
        };
    }
    parseImport(useToken) {
        const moduleToken = this.consume(tokens_1.TokenKind.Identifier, 'expected library name after use');
        this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after use declaration");
        return {
            kind: 'ImportDeclaration',
            moduleName: moduleToken.lexeme,
            range: { start: useToken.range.start, end: this.previous().range.end },
        };
    }
    parseMainFunction() {
        const main = this.consume(tokens_1.TokenKind.KwMain, "expected 'main' entry point");
        if (main.kind !== tokens_1.TokenKind.KwMain)
            return null;
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after main");
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after main");
        const body = this.parseBlock();
        return {
            kind: 'MainFunction',
            returnType: this.voidTypeName(main.range),
            parameters: [],
            body,
            range: { start: main.range.start, end: body.range.end },
        };
    }
    mainFromFunctionDeclaration(declaration) {
        return {
            kind: 'MainFunction',
            returnType: declaration.returnType,
            parameters: declaration.parameters,
            body: declaration.body,
            range: declaration.range,
        };
    }
    parseTopLevelDeclaration() {
        const declaredType = this.parseTypeName();
        if (this.match(tokens_1.TokenKind.KwFunction)) {
            return this.finishFunctionDeclaration(declaredType);
        }
        return this.finishVariableDeclaration(declaredType);
    }
    finishFunctionDeclaration(returnType) {
        const name = this.consumeFunctionName();
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after function name");
        const parameters = this.parseParameterList();
        const body = this.parseBlock();
        return {
            kind: 'FunctionDeclaration',
            returnType,
            name: name.lexeme,
            parameters,
            body,
            range: { start: returnType.range.start, end: body.range.end },
        };
    }
    consumeFunctionName() {
        if (this.check(tokens_1.TokenKind.Identifier, tokens_1.TokenKind.KwMain))
            return this.advance();
        this.error(this.peek().range, 'expected function name');
        return {
            kind: tokens_1.TokenKind.Identifier,
            lexeme: '',
            literal: null,
            range: this.peek().range,
        };
    }
    voidTypeName(range) {
        return {
            kind: 'PrimitiveTypeName',
            name: 'void',
            range,
        };
    }
    parseBlock() {
        const leftBrace = this.consume(tokens_1.TokenKind.LeftBrace, "expected '{' to start a block");
        const statements = [];
        while (!this.check(tokens_1.TokenKind.RightBrace) && !this.isAtEnd()) {
            statements.push(this.parseStatement());
        }
        const rightBrace = this.consume(tokens_1.TokenKind.RightBrace, "expected '}' to close a block");
        return {
            kind: 'BlockStatement',
            statements,
            range: { start: leftBrace.range.start, end: rightBrace.range.end },
        };
    }
    parseStatement() {
        if (this.checkTypeStart()) {
            return this.parseVariableDeclaration();
        }
        if (this.check(tokens_1.TokenKind.KwIf)) {
            return this.parseIfStatement();
        }
        if (this.check(tokens_1.TokenKind.KwWhile)) {
            return this.parseWhileStatement();
        }
        if (this.check(tokens_1.TokenKind.KwDo)) {
            return this.parseDoWhileStatement();
        }
        if (this.check(tokens_1.TokenKind.KwFor)) {
            return this.parseForStatement();
        }
        if (this.check(tokens_1.TokenKind.KwBreak)) {
            return this.parseBreakStatement();
        }
        if (this.check(tokens_1.TokenKind.KwContinue)) {
            return this.parseContinueStatement();
        }
        if (this.check(tokens_1.TokenKind.KwReturn)) {
            return this.parseReturnStatement();
        }
        if (this.check(tokens_1.TokenKind.LeftBrace)) {
            return this.parseBlock();
        }
        return this.parseExpressionStatement();
    }
    parseVariableDeclaration() {
        const declaredType = this.parseTypeName();
        return this.finishVariableDeclaration(declaredType);
    }
    finishVariableDeclaration(declaredType) {
        const name = this.consume(tokens_1.TokenKind.Identifier, 'expected variable name');
        let initializer = null;
        let constructorArgs = null;
        if (this.match(tokens_1.TokenKind.Equal)) {
            initializer = this.parseExpression();
        }
        else if (this.match(tokens_1.TokenKind.LeftParen)) {
            constructorArgs = this.parseArgumentListAfterLeftParen();
        }
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after variable declaration");
        return {
            kind: 'VariableDeclaration',
            declaredType,
            name: name.lexeme,
            initializer,
            constructorArgs,
            range: { start: declaredType.range.start, end: semicolon.range.end },
        };
    }
    parseClassDeclaration() {
        const classToken = this.consume(tokens_1.TokenKind.KwClass, "expected 'class'");
        const name = this.consume(tokens_1.TokenKind.Identifier, 'expected class name');
        let baseName = null;
        if (this.match(tokens_1.TokenKind.KwExtends)) {
            const base = this.consume(tokens_1.TokenKind.Identifier, 'expected base class name after extends');
            baseName = base.lexeme;
        }
        const leftBrace = this.consume(tokens_1.TokenKind.LeftBrace, "expected '{' to start class body");
        const members = [];
        let currentAccess = 'public';
        while (!this.check(tokens_1.TokenKind.RightBrace) && !this.isAtEnd()) {
            if (this.check(tokens_1.TokenKind.KwPrivate, tokens_1.TokenKind.KwPublic)) {
                const access = this.advance();
                this.consume(tokens_1.TokenKind.Colon, "expected ':' after access modifier");
                currentAccess = access.kind === tokens_1.TokenKind.KwPrivate ? 'private' : 'public';
                continue;
            }
            if (this.check(tokens_1.TokenKind.KwDestructor)) {
                const destructor = this.advance();
                this.error(destructor.range, 'destructors are not supported yet');
                this.synchronizeClassMember();
                continue;
            }
            const isStatic = this.match(tokens_1.TokenKind.KwStatic);
            if (this.check(tokens_1.TokenKind.KwConstructor)) {
                if (isStatic) {
                    this.error(this.previous().range, 'constructors cannot be static');
                }
                members.push(this.parseConstructorDeclaration(currentAccess));
                continue;
            }
            if (this.checkTypeStart()) {
                const declaredType = this.parseTypeName();
                if (this.match(tokens_1.TokenKind.KwFunction)) {
                    members.push(this.finishClassMethodDeclaration(declaredType, isStatic, currentAccess));
                }
                else {
                    if (isStatic) {
                        this.error(declaredType.range, 'static fields are not supported yet');
                    }
                    members.push(this.finishClassFieldDeclaration(declaredType, currentAccess));
                }
                continue;
            }
            this.error(this.peek().range, `unexpected token ${(0, tokens_1.tokenDisplay)(this.peek().kind)} in class body`);
            this.advance();
        }
        const rightBrace = this.consume(tokens_1.TokenKind.RightBrace, "expected '}' to close class body");
        return {
            kind: 'ClassDeclaration',
            name: name.lexeme,
            baseName,
            members,
            range: { start: classToken.range.start, end: rightBrace.range.end },
        };
    }
    finishClassFieldDeclaration(declaredType, access) {
        const fields = [];
        do {
            const name = this.consume(tokens_1.TokenKind.Identifier, 'expected field name');
            const initializer = this.match(tokens_1.TokenKind.Equal) ? this.parseExpression() : null;
            fields.push({
                kind: 'FieldDeclarator',
                name: name.lexeme,
                initializer,
                range: { start: name.range.start, end: (initializer ?? name).range.end },
            });
        } while (this.match(tokens_1.TokenKind.Comma));
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after field declaration");
        return {
            kind: 'ClassFieldDeclaration',
            declaredType,
            fields,
            access,
            range: { start: declaredType.range.start, end: semicolon.range.end },
        };
    }
    finishClassMethodDeclaration(returnType, isStatic, access) {
        const name = this.consume(tokens_1.TokenKind.Identifier, 'expected method name');
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after method name");
        const parameters = this.parseParameterList();
        const body = this.parseBlock();
        return {
            kind: 'ClassMethodDeclaration',
            returnType,
            name: name.lexeme,
            parameters,
            body,
            isStatic,
            access,
            range: { start: returnType.range.start, end: body.range.end },
        };
    }
    parseConstructorDeclaration(access) {
        const constructorToken = this.consume(tokens_1.TokenKind.KwConstructor, "expected 'constructor'");
        const name = this.consume(tokens_1.TokenKind.Identifier, 'expected constructor class name');
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after constructor name");
        const parameters = this.parseParameterList();
        const body = this.parseBlock();
        return {
            kind: 'ConstructorDeclaration',
            name: name.lexeme,
            parameters,
            body,
            access,
            range: { start: constructorToken.range.start, end: body.range.end },
        };
    }
    parseParameterList() {
        const parameters = [];
        if (!this.check(tokens_1.TokenKind.RightParen)) {
            do {
                parameters.push(this.parseParameterDeclaration());
            } while (this.match(tokens_1.TokenKind.Comma));
        }
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after parameters");
        return parameters;
    }
    parseParameterDeclaration() {
        const paramType = this.parseTypeName();
        const paramName = this.consume(tokens_1.TokenKind.Identifier, 'expected parameter name');
        const defaultValue = this.match(tokens_1.TokenKind.Equal) ? this.parseExpression() : null;
        return {
            kind: 'ParameterDeclaration',
            paramType,
            name: paramName.lexeme,
            defaultValue,
            range: { start: paramType.range.start, end: (defaultValue ?? paramName).range.end },
        };
    }
    synchronizeClassMember() {
        while (!this.isAtEnd() && !this.check(tokens_1.TokenKind.Semicolon, tokens_1.TokenKind.RightBrace)) {
            this.advance();
        }
        if (this.check(tokens_1.TokenKind.Semicolon))
            this.advance();
    }
    parseExpressionStatement() {
        return this.parseAssignmentOrExpressionStatement(true);
    }
    parseAssignmentOrExpressionStatement(consumeSemicolon) {
        const expression = this.parseExpression();
        const assignmentOperator = this.matchAssignmentOperator();
        if (assignmentOperator) {
            const value = this.parseExpression();
            const end = consumeSemicolon
                ? this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after assignment").range.end
                : value.range.end;
            return {
                kind: 'AssignmentStatement',
                target: expression,
                operator: this.assignmentOperatorText(assignmentOperator.kind),
                value,
                range: { start: expression.range.start, end },
            };
        }
        const end = consumeSemicolon
            ? this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after expression").range.end
            : expression.range.end;
        return {
            kind: 'ExpressionStatement',
            expression,
            range: { start: expression.range.start, end },
        };
    }
    matchAssignmentOperator() {
        if (this.match(tokens_1.TokenKind.Equal, tokens_1.TokenKind.PlusEqual, tokens_1.TokenKind.MinusEqual, tokens_1.TokenKind.StarEqual, tokens_1.TokenKind.SlashEqual)) {
            return this.previous();
        }
        return null;
    }
    assignmentOperatorText(kind) {
        switch (kind) {
            case tokens_1.TokenKind.PlusEqual:
                return '+=';
            case tokens_1.TokenKind.MinusEqual:
                return '-=';
            case tokens_1.TokenKind.StarEqual:
                return '*=';
            case tokens_1.TokenKind.SlashEqual:
                return '/=';
            default:
                return '=';
        }
    }
    parseIfStatement() {
        const ifToken = this.consume(tokens_1.TokenKind.KwIf, "expected 'if'");
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after if");
        const condition = this.parseExpression();
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after if condition");
        const thenBranch = this.parseStatement();
        const elseBranch = this.match(tokens_1.TokenKind.KwElse)
            ? this.parseStatement()
            : null;
        return {
            kind: 'IfStatement',
            condition,
            thenBranch,
            elseBranch,
            range: { start: ifToken.range.start, end: (elseBranch ?? thenBranch).range.end },
        };
    }
    parseWhileStatement() {
        const whileToken = this.consume(tokens_1.TokenKind.KwWhile, "expected 'while'");
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after while");
        const condition = this.parseExpression();
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after while condition");
        const body = this.parseStatement();
        return {
            kind: 'WhileStatement',
            condition,
            body,
            range: { start: whileToken.range.start, end: body.range.end },
        };
    }
    parseDoWhileStatement() {
        const doToken = this.consume(tokens_1.TokenKind.KwDo, "expected 'do'");
        const body = this.parseStatement();
        this.consume(tokens_1.TokenKind.KwWhile, "expected 'while' after do body");
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after while");
        const condition = this.parseExpression();
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after do-while condition");
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after do-while");
        return {
            kind: 'DoWhileStatement',
            body,
            condition,
            range: { start: doToken.range.start, end: semicolon.range.end },
        };
    }
    parseForStatement() {
        const forToken = this.consume(tokens_1.TokenKind.KwFor, "expected 'for'");
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after for");
        const initializer = this.parseForInitializer();
        let condition = null;
        if (!this.check(tokens_1.TokenKind.Semicolon)) {
            condition = this.parseExpression();
        }
        this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after for condition");
        let increment = null;
        if (!this.check(tokens_1.TokenKind.RightParen)) {
            increment = this.parseAssignmentOrExpressionStatement(false);
        }
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after for clauses");
        const body = this.parseStatement();
        return {
            kind: 'ForStatement',
            initializer,
            condition,
            increment,
            body,
            range: { start: forToken.range.start, end: body.range.end },
        };
    }
    parseForInitializer() {
        if (this.match(tokens_1.TokenKind.Semicolon)) {
            return null;
        }
        if (this.checkTypeStart()) {
            return this.parseVariableDeclaration();
        }
        return this.parseAssignmentOrExpressionStatement(true);
    }
    parseBreakStatement() {
        const token = this.consume(tokens_1.TokenKind.KwBreak, "expected 'break'");
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after break");
        return {
            kind: 'BreakStatement',
            range: { start: token.range.start, end: semicolon.range.end },
        };
    }
    parseContinueStatement() {
        const token = this.consume(tokens_1.TokenKind.KwContinue, "expected 'continue'");
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after continue");
        return {
            kind: 'ContinueStatement',
            range: { start: token.range.start, end: semicolon.range.end },
        };
    }
    parseReturnStatement() {
        const token = this.consume(tokens_1.TokenKind.KwReturn, "expected 'return'");
        const value = this.check(tokens_1.TokenKind.Semicolon) ? null : this.parseExpression();
        const semicolon = this.consume(tokens_1.TokenKind.Semicolon, "expected ';' after return");
        return {
            kind: 'ReturnStatement',
            value,
            range: { start: token.range.start, end: semicolon.range.end },
        };
    }
    parseExpression() {
        return this.parseOr();
    }
    parseOr() {
        let expression = this.parseAnd();
        while (this.match(tokens_1.TokenKind.KwOr)) {
            const operator = this.previous();
            const right = this.parseAnd();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseAnd() {
        let expression = this.parseEquality();
        while (this.match(tokens_1.TokenKind.KwAnd)) {
            const operator = this.previous();
            const right = this.parseEquality();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseEquality() {
        let expression = this.parseComparison();
        while (this.match(tokens_1.TokenKind.EqualEqual, tokens_1.TokenKind.BangEqual)) {
            const operator = this.previous();
            const right = this.parseComparison();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseComparison() {
        let expression = this.parseTerm();
        while (this.match(tokens_1.TokenKind.Less, tokens_1.TokenKind.LessEqual, tokens_1.TokenKind.Greater, tokens_1.TokenKind.GreaterEqual)) {
            const operator = this.previous();
            const right = this.parseTerm();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseTerm() {
        let expression = this.parseFactor();
        while (this.match(tokens_1.TokenKind.Plus, tokens_1.TokenKind.Minus)) {
            const operator = this.previous();
            const right = this.parseFactor();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseFactor() {
        let expression = this.parseUnary();
        while (this.match(tokens_1.TokenKind.Star, tokens_1.TokenKind.Slash)) {
            const operator = this.previous();
            const right = this.parseUnary();
            expression = this.binary(expression, operator, right);
        }
        return expression;
    }
    parseUnary() {
        if (this.match(tokens_1.TokenKind.Minus, tokens_1.TokenKind.KwNot)) {
            const operator = this.previous();
            const operand = this.parseUnary();
            return {
                kind: 'UnaryExpression',
                operator: operator.kind === tokens_1.TokenKind.KwNot ? 'not' : '-',
                operand,
                range: { start: operator.range.start, end: operand.range.end },
            };
        }
        return this.parseCall();
    }
    parseCall() {
        let expression = this.parsePrimary();
        while (true) {
            if (this.match(tokens_1.TokenKind.Dot)) {
                const name = this.consume(tokens_1.TokenKind.Identifier, "expected member name after '.'");
                expression = {
                    kind: 'MemberExpression',
                    object: expression,
                    name: name.lexeme,
                    range: { start: expression.range.start, end: name.range.end },
                };
                continue;
            }
            if (this.match(tokens_1.TokenKind.LeftParen)) {
                const args = [];
                if (!this.check(tokens_1.TokenKind.RightParen)) {
                    do {
                        args.push(this.parseCallArgument());
                    } while (this.match(tokens_1.TokenKind.Comma));
                }
                const rightParen = this.consume(tokens_1.TokenKind.RightParen, "expected ')' after arguments");
                expression = {
                    kind: 'CallExpression',
                    callee: expression,
                    args,
                    range: { start: expression.range.start, end: rightParen.range.end },
                };
                continue;
            }
            if (this.match(tokens_1.TokenKind.LeftBracket)) {
                const index = this.parseExpression();
                const rightBracket = this.consume(tokens_1.TokenKind.RightBracket, "expected ']' after array index");
                expression = {
                    kind: 'IndexExpression',
                    object: expression,
                    index,
                    range: { start: expression.range.start, end: rightBracket.range.end },
                };
                continue;
            }
            break;
        }
        return expression;
    }
    parsePrimary() {
        if (this.checkTypeStart()) {
            const returnType = this.parseTypeName();
            if (!this.match(tokens_1.TokenKind.KwFunction)) {
                this.error(returnType.range, "expected 'function' after callback return type");
                return {
                    kind: 'LiteralExpression',
                    value: 0,
                    valueType: 'int',
                    range: returnType.range,
                };
            }
            return this.finishFunctionExpression(returnType);
        }
        if (this.match(tokens_1.TokenKind.IntLiteral)) {
            return this.literal(this.previous(), 'int');
        }
        if (this.match(tokens_1.TokenKind.FloatLiteral)) {
            return this.literal(this.previous(), 'float');
        }
        if (this.match(tokens_1.TokenKind.StringLiteral)) {
            return this.literal(this.previous(), 'string');
        }
        if (this.match(tokens_1.TokenKind.CharLiteral)) {
            return this.literal(this.previous(), 'char');
        }
        if (this.match(tokens_1.TokenKind.KwTrue, tokens_1.TokenKind.KwFalse)) {
            return this.literal(this.previous(), 'bool');
        }
        if (this.match(tokens_1.TokenKind.LeftBracket)) {
            const leftBracket = this.previous();
            const elements = [];
            if (!this.check(tokens_1.TokenKind.RightBracket)) {
                do {
                    elements.push(this.parseExpression());
                } while (this.match(tokens_1.TokenKind.Comma));
            }
            const rightBracket = this.consume(tokens_1.TokenKind.RightBracket, "expected ']' after array literal");
            return {
                kind: 'ArrayLiteralExpression',
                elements,
                range: { start: leftBracket.range.start, end: rightBracket.range.end },
            };
        }
        if (this.match(tokens_1.TokenKind.Identifier, tokens_1.TokenKind.KwDiv, tokens_1.TokenKind.KwMod, tokens_1.TokenKind.KwThis)) {
            const token = this.previous();
            return {
                kind: 'IdentifierExpression',
                name: token.lexeme,
                range: token.range,
            };
        }
        if (this.match(tokens_1.TokenKind.LeftParen)) {
            const expression = this.parseExpression();
            const rightParen = this.consume(tokens_1.TokenKind.RightParen, "expected ')' after expression");
            return {
                ...expression,
                range: { start: expression.range.start, end: rightParen.range.end },
            };
        }
        const token = this.advance();
        this.error(token.range, `expected expression, got ${(0, tokens_1.tokenDisplay)(token.kind)}`);
        return {
            kind: 'LiteralExpression',
            value: 0,
            valueType: 'int',
            range: token.range,
        };
    }
    finishFunctionExpression(returnType) {
        this.consume(tokens_1.TokenKind.LeftParen, "expected '(' after function");
        const parameters = this.parseParameterList();
        const body = this.parseBlock();
        return {
            kind: 'FunctionExpression',
            returnType,
            parameters,
            body,
            range: { start: returnType.range.start, end: body.range.end },
        };
    }
    literal(token, valueType) {
        return {
            kind: 'LiteralExpression',
            value: token.literal,
            valueType,
            range: token.range,
        };
    }
    binary(left, operator, right) {
        return {
            kind: 'BinaryExpression',
            operator: this.operatorText(operator.kind),
            left,
            right,
            range: { start: left.range.start, end: right.range.end },
        };
    }
    operatorText(kind) {
        switch (kind) {
            case tokens_1.TokenKind.Plus:
                return '+';
            case tokens_1.TokenKind.Minus:
                return '-';
            case tokens_1.TokenKind.Star:
                return '*';
            case tokens_1.TokenKind.Slash:
                return '/';
            case tokens_1.TokenKind.EqualEqual:
                return '==';
            case tokens_1.TokenKind.BangEqual:
                return '!=';
            case tokens_1.TokenKind.Less:
                return '<';
            case tokens_1.TokenKind.LessEqual:
                return '<=';
            case tokens_1.TokenKind.Greater:
                return '>';
            case tokens_1.TokenKind.GreaterEqual:
                return '>=';
            case tokens_1.TokenKind.KwAnd:
                return 'and';
            case tokens_1.TokenKind.KwOr:
                return 'or';
            default:
                return '+';
        }
    }
    parseTypeName() {
        const start = this.peek();
        if (this.match(tokens_1.TokenKind.KwArray, tokens_1.TokenKind.KwDynArray)) {
            const keyword = this.previous();
            const dynamic = keyword.kind === tokens_1.TokenKind.KwDynArray;
            this.consume(tokens_1.TokenKind.Less, "expected '<' after array type name");
            const elementType = this.parseTypeName();
            let size = null;
            if (dynamic) {
                this.consume(tokens_1.TokenKind.Greater, "expected '>' after dyn_array element type");
            }
            else {
                this.consume(tokens_1.TokenKind.Comma, "expected ',' after array element type");
                const sizeToken = this.consume(tokens_1.TokenKind.IntLiteral, 'expected array size');
                size = typeof sizeToken.literal === 'number' ? sizeToken.literal : 0;
                this.consume(tokens_1.TokenKind.Greater, "expected '>' after array size");
            }
            return {
                kind: 'ArrayTypeName',
                elementType,
                size,
                dynamic,
                range: { start: start.range.start, end: this.previous().range.end },
            };
        }
        if (this.checkTypeKeyword()) {
            const token = this.advance();
            return {
                kind: 'PrimitiveTypeName',
                name: this.typeNameFromToken(token.kind),
                range: token.range,
            };
        }
        const moduleToken = this.consume(tokens_1.TokenKind.Identifier, 'expected type name');
        if (!this.match(tokens_1.TokenKind.Dot)) {
            return {
                kind: 'ClassTypeName',
                name: moduleToken.lexeme,
                range: moduleToken.range,
            };
        }
        const nameToken = this.consume(tokens_1.TokenKind.Identifier, 'expected type name after module name');
        return {
            kind: 'QualifiedTypeName',
            moduleName: moduleToken.lexeme,
            name: nameToken.lexeme,
            range: { start: start.range.start, end: nameToken.range.end },
        };
    }
    typeNameFromToken(kind) {
        switch (kind) {
            case tokens_1.TokenKind.KwInt:
                return 'int';
            case tokens_1.TokenKind.KwFloat:
                return 'float';
            case tokens_1.TokenKind.KwString:
                return 'string';
            case tokens_1.TokenKind.KwChar:
                return 'char';
            case tokens_1.TokenKind.KwBool:
                return 'bool';
            case tokens_1.TokenKind.KwVoid:
                return 'void';
            default:
                return 'void';
        }
    }
    checkTypeKeyword() {
        return this.check(tokens_1.TokenKind.KwInt, tokens_1.TokenKind.KwFloat, tokens_1.TokenKind.KwString, tokens_1.TokenKind.KwChar, tokens_1.TokenKind.KwBool, tokens_1.TokenKind.KwVoid);
    }
    checkTypeStart() {
        return this.checkTypeKeyword()
            || this.check(tokens_1.TokenKind.KwArray, tokens_1.TokenKind.KwDynArray)
            || (this.check(tokens_1.TokenKind.Identifier)
                && this.checkNext(tokens_1.TokenKind.Dot)
                && this.checkAhead(2, tokens_1.TokenKind.Identifier)
                && (this.checkAhead(3, tokens_1.TokenKind.Identifier) || this.checkAhead(3, tokens_1.TokenKind.KwFunction)))
            || (this.check(tokens_1.TokenKind.Identifier)
                && (this.checkNext(tokens_1.TokenKind.Identifier) || this.checkNext(tokens_1.TokenKind.KwFunction)));
    }
    parseArgumentListAfterLeftParen() {
        const args = [];
        if (!this.check(tokens_1.TokenKind.RightParen)) {
            do {
                args.push(this.parseCallArgument());
            } while (this.match(tokens_1.TokenKind.Comma));
        }
        this.consume(tokens_1.TokenKind.RightParen, "expected ')' after arguments");
        return args;
    }
    parseCallArgument() {
        if (this.check(tokens_1.TokenKind.Identifier) && this.checkNext(tokens_1.TokenKind.Equal)) {
            const name = this.advance();
            this.consume(tokens_1.TokenKind.Equal, "expected '=' after argument name");
            const value = this.parseExpression();
            return {
                kind: 'CallArgument',
                name: name.lexeme,
                value,
                range: { start: name.range.start, end: value.range.end },
            };
        }
        const value = this.parseExpression();
        return {
            kind: 'CallArgument',
            name: null,
            value,
            range: value.range,
        };
    }
    match(...kinds) {
        if (!this.check(...kinds))
            return false;
        this.advance();
        return true;
    }
    consume(kind, message) {
        if (this.check(kind))
            return this.advance();
        this.error(this.peek().range, message);
        return {
            kind,
            lexeme: '',
            literal: null,
            range: this.peek().range,
        };
    }
    check(...kinds) {
        if (this.isAtEnd())
            return kinds.includes(tokens_1.TokenKind.EndOfFile);
        return kinds.includes(this.peek().kind);
    }
    checkNext(kind) {
        return this.checkAhead(1, kind);
    }
    checkAhead(offset, kind) {
        if (this.current + offset >= this.tokens.length)
            return false;
        return this.tokens[this.current + offset].kind === kind;
    }
    advance() {
        if (!this.isAtEnd())
            this.current++;
        return this.previous();
    }
    isAtEnd() {
        return this.peek().kind === tokens_1.TokenKind.EndOfFile;
    }
    peek() {
        return this.tokens[this.current];
    }
    previous() {
        return this.tokens[this.current - 1];
    }
    error(range, message) {
        this.diagnostics.error(range, message);
    }
}
exports.Parser = Parser;
//# sourceMappingURL=parser.js.map
},
"src/core/project.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseSource = parseSource;
exports.addDiagnostics = addDiagnostics;
exports.loadUserModules = loadUserModules;
exports.resolveUserModule = resolveUserModule;
exports.buildUserModuleRegistry = buildUserModuleRegistry;
exports.collectModuleExports = collectModuleExports;
const lexer_1 = require("./lexer");
const modules_1 = require("./modules");
const parser_1 = require("./parser");
const types_1 = require("./types");
const fs = require('fs');
const path = require('path');
function parseSource(moduleName, file, source, diagnostics) {
    const lexed = new lexer_1.Lexer(source, file).tokenize();
    addDiagnostics(diagnostics, lexed.diagnostics);
    const parsed = new parser_1.Parser(lexed.tokens).parseProgram();
    addDiagnostics(diagnostics, parsed.diagnostics);
    return {
        moduleName,
        file,
        source,
        tokens: lexed.tokens,
        ast: parsed.program,
    };
}
function addDiagnostics(target, source) {
    for (const diagnostic of source.all()) {
        target.add(diagnostic.severity, diagnostic.range, diagnostic.message, diagnostic.code);
    }
}
function loadUserModules(root, rootFile, options, stdlib, diagnostics, output) {
    const loaded = new Map();
    const loading = [];
    const loadImports = (program, fromFile) => {
        for (const importDecl of program.imports) {
            const moduleName = importDecl.moduleName;
            if (stdlib.hasModule(moduleName))
                continue;
            loadModule(moduleName, fromFile, importDecl.range);
        }
    };
    const loadModule = (moduleName, fromFile, range) => {
        if (loaded.has(moduleName))
            return;
        const cycleStart = loading.indexOf(moduleName);
        if (cycleStart >= 0) {
            const cycle = [...loading.slice(cycleStart), moduleName].join(' -> ');
            diagnostics.error(range, `module import cycle detected: ${cycle}`);
            return;
        }
        const resolved = resolveUserModule(moduleName, fromFile, options);
        if (!resolved) {
            diagnostics.error(range, `module '${moduleName}' was not found`);
            return;
        }
        loading.push(moduleName);
        const parsed = parseSource(moduleName, resolved.file, resolved.source, diagnostics);
        if (parsed.ast) {
            loadImports(parsed.ast, resolved.file);
            const loadedModule = {
                name: moduleName,
                file: resolved.file,
                source: resolved.source,
                tokens: parsed.tokens,
                ast: parsed.ast,
            };
            loaded.set(moduleName, loadedModule);
            output.push(loadedModule);
        }
        loading.pop();
    };
    loadImports(root, rootFile);
}
function resolveUserModule(moduleName, fromFile, options) {
    const custom = options.resolveModule?.(moduleName, fromFile);
    if (custom)
        return custom;
    const sourceFromMap = resolveUserModuleFromSources(moduleName, fromFile, options.sources);
    if (sourceFromMap)
        return sourceFromMap;
    const file = resolveUserModuleFile(moduleName, fromFile);
    if (!file)
        return null;
    return { file, source: fs.readFileSync(file, 'utf8') };
}
function buildUserModuleRegistry(modules, stdlib, diagnostics) {
    const userModuleRegistry = new modules_1.UserModuleRegistry();
    for (const module of modules) {
        userModuleRegistry.register(collectModuleExports(module, stdlib, userModuleRegistry, diagnostics));
    }
    return userModuleRegistry;
}
function collectModuleExports(module, stdlib, userModules, diagnostics) {
    const functions = new Map();
    const classes = new Map();
    const localClasses = new Set();
    for (const declaration of module.ast.declarations) {
        if (declaration.kind === 'ClassDeclaration') {
            localClasses.add(declaration.name);
        }
    }
    for (const declaration of module.ast.declarations) {
        if (declaration.kind === 'FunctionDeclaration') {
            functions.set(declaration.name, functionSpecFromDeclaration(declaration, module.name, module.ast, localClasses, stdlib, userModules, diagnostics));
        }
        if (declaration.kind === 'ClassDeclaration') {
            classes.set(declaration.name, classSpecFromDeclaration(declaration, module.name, module.ast, localClasses, stdlib, userModules, diagnostics));
        }
    }
    return {
        name: module.name,
        file: module.file,
        functions,
        classes,
    };
}
function resolveUserModuleFromSources(moduleName, fromFile, sources) {
    if (!sources)
        return null;
    const candidates = [
        moduleName,
        `${moduleName}.idyl`,
        path.join(path.dirname(fromFile), `${moduleName}.idyl`),
    ];
    for (const candidate of candidates) {
        if (Object.prototype.hasOwnProperty.call(sources, candidate)) {
            return { file: candidate, source: sources[candidate] };
        }
    }
    return null;
}
function resolveUserModuleFile(moduleName, fromFile) {
    if (fromFile === 'main.idyl' || fromFile.trim() === '')
        return null;
    const candidate = path.join(path.dirname(fromFile), `${moduleName}.idyl`);
    if (!fs.existsSync(candidate))
        return null;
    return candidate;
}
function functionSpecFromDeclaration(declaration, moduleName, program, localClasses, stdlib, userModules, diagnostics) {
    const parameters = declaration.parameters.map((parameter) => ({
        name: parameter.name,
        type: resolveModuleExportType(parameter.paramType, moduleName, program, localClasses, stdlib, userModules, diagnostics),
    }));
    const returnType = resolveModuleExportType(declaration.returnType, moduleName, program, localClasses, stdlib, userModules, diagnostics);
    return {
        name: declaration.name,
        parameters,
        returnType,
        minArguments: requiredParameterCount(declaration.parameters),
    };
}
function classSpecFromDeclaration(declaration, moduleName, program, localClasses, stdlib, userModules, diagnostics) {
    const qualifiedName = (0, modules_1.qualifiedUserClassName)(moduleName, declaration.name);
    const fields = [];
    const methods = [];
    let constructorSpec = null;
    let constructorAccess = 'public';
    for (const member of declaration.members) {
        if (member.kind === 'ClassFieldDeclaration') {
            const type = resolveModuleExportType(member.declaredType, moduleName, program, localClasses, stdlib, userModules, diagnostics);
            for (const field of member.fields) {
                fields.push({
                    name: field.name,
                    type,
                    owner: qualifiedName,
                    access: member.access,
                    range: field.range,
                });
            }
        }
        if (member.kind === 'ClassMethodDeclaration') {
            const spec = functionSpecFromDeclaration(member, moduleName, program, localClasses, stdlib, userModules, diagnostics);
            methods.push({
                name: member.name,
                spec,
                owner: qualifiedName,
                access: member.access,
                isStatic: member.isStatic,
                range: member.range,
            });
        }
        if (member.kind === 'ConstructorDeclaration') {
            constructorSpec = {
                name: member.name,
                parameters: member.parameters.map((parameter) => ({
                    name: parameter.name,
                    type: resolveModuleExportType(parameter.paramType, moduleName, program, localClasses, stdlib, userModules, diagnostics),
                })),
                returnType: types_1.VOID,
                minArguments: requiredParameterCount(member.parameters),
            };
            constructorAccess = member.access;
        }
    }
    return {
        name: declaration.name,
        qualifiedName,
        baseName: declaration.baseName ? (0, modules_1.qualifiedUserClassName)(moduleName, declaration.baseName) : null,
        fields,
        methods,
        constructorSpec,
        constructorAccess,
        range: declaration.range,
    };
}
function requiredParameterCount(parameters) {
    const firstDefault = parameters.findIndex((parameter) => parameter.defaultValue !== null);
    return firstDefault < 0 ? parameters.length : firstDefault;
}
function resolveModuleExportType(typeName, moduleName, program, localClasses, stdlib, userModules, diagnostics) {
    if (typeName.kind === 'PrimitiveTypeName') {
        return (0, types_1.primitive)(typeName.name);
    }
    if (typeName.kind === 'ArrayTypeName') {
        return resolveModuleArrayType(typeName, moduleName, program, localClasses, stdlib, userModules, diagnostics);
    }
    if (typeName.kind === 'ClassTypeName') {
        return localClasses.has(typeName.name)
            ? (0, types_1.classType)((0, modules_1.qualifiedUserClassName)(moduleName, typeName.name))
            : (0, types_1.classType)(typeName.name);
    }
    if (!program.imports.some((item) => item.moduleName === typeName.moduleName)) {
        diagnostics.error(typeName.range, `'${typeName.moduleName}' is not imported (use 'use ${typeName.moduleName};')`);
        return types_1.ERROR_TYPE;
    }
    if (stdlib.hasModule(typeName.moduleName)) {
        return (0, types_1.qualified)(typeName.moduleName, typeName.name);
    }
    const importedModule = userModules.getModule(typeName.moduleName);
    const importedClass = importedModule?.classes.get(typeName.name);
    if (importedClass)
        return (0, types_1.classType)(importedClass.qualifiedName);
    diagnostics.error(typeName.range, `module '${typeName.moduleName}' has no type '${typeName.name}'`);
    return types_1.ERROR_TYPE;
}
function resolveModuleArrayType(typeName, moduleName, program, localClasses, stdlib, userModules, diagnostics) {
    return (0, types_1.arrayType)(resolveModuleExportType(typeName.elementType, moduleName, program, localClasses, stdlib, userModules, diagnostics), typeName.size, typeName.dynamic);
}
//# sourceMappingURL=project.js.map
},
"src/core/semantics.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SemanticAnalyzer = void 0;
const diagnostics_1 = require("./diagnostics");
const modules_1 = require("./modules");
const registry_1 = require("./stdlib/registry");
const types_1 = require("./types");
class SemanticAnalyzer {
    stdlib;
    userModuleRegistry;
    diagnostics = new diagnostics_1.DiagnosticBag();
    imports = new Set();
    userModules = new Set();
    scopes = [new Map()];
    functions = new Map();
    classes = new Map();
    returnTypes = [];
    classContexts = [];
    loopDepth = 0;
    constructor(stdlib = (0, registry_1.createDefaultStandardLibrary)(), userModuleRegistry = new modules_1.UserModuleRegistry()) {
        this.stdlib = stdlib;
        this.userModuleRegistry = userModuleRegistry;
    }
    analyze(program) {
        for (const importDecl of program.imports) {
            this.imports.add(importDecl.moduleName);
            if (!this.stdlib.hasModule(importDecl.moduleName)) {
                if (this.userModuleRegistry.hasModule(importDecl.moduleName)) {
                    this.registerImportedModuleClasses(importDecl.moduleName);
                }
                else {
                    this.userModules.add(importDecl.moduleName);
                }
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'ClassDeclaration') {
                this.registerClass(declaration);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'ClassDeclaration') {
                this.registerClassMembers(declaration);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                this.registerFunction(declaration);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'ClassDeclaration') {
                this.analyzeClassDeclaration(declaration);
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'VariableDeclaration') {
                this.analyzeVariableDeclaration(declaration, 'variable');
            }
        }
        for (const declaration of program.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                this.analyzeFunctionDeclaration(declaration);
            }
        }
        if (program.main) {
            this.analyzeMainFunction(program.main);
        }
        return {
            success: !this.diagnostics.hasErrors(),
            diagnostics: this.diagnostics,
        };
    }
    registerImportedModuleClasses(moduleName) {
        const module = this.userModuleRegistry.getModule(moduleName);
        if (!module)
            return;
        for (const classSpec of module.classes.values()) {
            if (this.classes.has(classSpec.qualifiedName))
                continue;
            const fields = new Map();
            const methods = new Map();
            const methodAccess = new Map();
            for (const field of classSpec.fields) {
                fields.set(field.name, {
                    name: field.name,
                    type: field.type,
                    range: field.range,
                    owner: field.owner,
                    access: field.access,
                });
            }
            for (const method of classSpec.methods) {
                methods.set(method.name, method.spec);
                methodAccess.set(method.name, {
                    name: method.name,
                    owner: method.owner,
                    access: method.access,
                    isStatic: method.isStatic,
                    range: method.range,
                });
            }
            this.classes.set(classSpec.qualifiedName, {
                declaration: {
                    kind: 'ClassDeclaration',
                    name: classSpec.qualifiedName,
                    baseName: classSpec.baseName,
                    members: [],
                    range: classSpec.range,
                },
                fields,
                methods,
                methodDeclarations: new Map(),
                methodAccess,
                ownFields: new Set(fields.keys()),
                ownMethods: new Set(methods.keys()),
                constructorSpec: classSpec.constructorSpec,
                constructorDeclaration: null,
                membersRegistered: true,
                membersRegistering: false,
            });
        }
    }
    registerFunction(declaration) {
        if (this.functions.has(declaration.name)) {
            this.diagnostics.error(declaration.range, `function '${declaration.name}' is already declared`);
            return;
        }
        this.functions.set(declaration.name, declaration);
        const parameters = declaration.parameters.map((parameter) => this.resolveTypeName(parameter.paramType));
        const returnType = this.resolveTypeName(declaration.returnType);
        this.declare(declaration.name, (0, types_1.functionType)(parameters, returnType, requiredParameterCount(declaration.parameters)), 'function', declaration.range);
    }
    analyzeFunctionDeclaration(declaration) {
        const returnType = this.resolveTypeName(declaration.returnType);
        this.returnTypes.push(returnType);
        this.pushScope();
        this.analyzeParameters(declaration.parameters);
        this.analyzeStatement(declaration.body);
        this.reportMissingReturn(returnType, declaration.body, declaration.range);
        this.popScope();
        this.returnTypes.pop();
    }
    analyzeParameters(parameters) {
        const resolved = [];
        let sawDefault = false;
        for (const parameter of parameters) {
            const parameterType = this.resolveTypeName(parameter.paramType);
            resolved.push(parameterType);
            if (parameter.defaultValue) {
                sawDefault = true;
                const defaultType = this.expressionType(parameter.defaultValue);
                if (!this.canAssign(parameterType, defaultType)) {
                    this.diagnostics.error(parameter.defaultValue.range, `default value for parameter '${parameter.name}' expects '${(0, types_1.typeToString)(parameterType)}', got '${(0, types_1.typeToString)(defaultType)}'`);
                }
            }
            else if (sawDefault) {
                this.diagnostics.error(parameter.range, `parameter '${parameter.name}' without default value cannot follow a parameter with default value`);
            }
            this.declare(parameter.name, parameterType, 'parameter', parameter.range);
        }
        return resolved;
    }
    analyzeMainFunction(declaration) {
        const returnType = this.resolveTypeName(declaration.returnType);
        if (declaration.parameters.length > 0) {
            this.diagnostics.error(declaration.parameters[0].range, "entry point 'main' cannot have parameters");
        }
        this.returnTypes.push(returnType);
        this.pushScope();
        this.analyzeParameters(declaration.parameters);
        this.analyzeStatement(declaration.body);
        this.reportMissingReturn(returnType, declaration.body, declaration.range);
        this.popScope();
        this.returnTypes.pop();
    }
    registerClass(declaration) {
        if (this.classes.has(declaration.name)) {
            this.diagnostics.error(declaration.range, `class '${declaration.name}' is already declared`);
            return;
        }
        if (this.stdlib.hasModule(declaration.name)) {
            this.diagnostics.error(declaration.range, `class '${declaration.name}' conflicts with a standard library module`);
            return;
        }
        this.classes.set(declaration.name, {
            declaration,
            fields: new Map(),
            methods: new Map(),
            methodDeclarations: new Map(),
            methodAccess: new Map(),
            ownFields: new Set(),
            ownMethods: new Set(),
            constructorSpec: null,
            constructorDeclaration: null,
            membersRegistered: false,
            membersRegistering: false,
        });
    }
    registerClassMembers(declaration) {
        const info = this.classes.get(declaration.name);
        if (!info)
            return;
        if (info.membersRegistered)
            return;
        if (info.membersRegistering) {
            this.diagnostics.error(declaration.range, `class inheritance cycle involving '${declaration.name}'`);
            return;
        }
        info.membersRegistering = true;
        if (declaration.baseName) {
            const baseInfo = this.classes.get(declaration.baseName);
            if (!baseInfo) {
                this.diagnostics.error(declaration.range, `unknown base class '${declaration.baseName}'`);
            }
            else {
                this.registerClassMembers(baseInfo.declaration);
                this.inheritClassMembers(info, baseInfo);
            }
        }
        for (const member of declaration.members) {
            switch (member.kind) {
                case 'ClassFieldDeclaration':
                    this.registerClassFields(info, member);
                    break;
                case 'ClassMethodDeclaration':
                    this.registerClassMethod(info, member);
                    break;
                case 'ConstructorDeclaration':
                    this.registerClassConstructor(info, member);
                    break;
            }
        }
        info.membersRegistering = false;
        info.membersRegistered = true;
    }
    inheritClassMembers(info, baseInfo) {
        for (const [name, field] of baseInfo.fields) {
            info.fields.set(name, field);
        }
        for (const [name, method] of baseInfo.methods) {
            info.methods.set(name, method);
            const declaration = baseInfo.methodDeclarations.get(name);
            if (declaration)
                info.methodDeclarations.set(name, declaration);
            const access = baseInfo.methodAccess.get(name);
            if (access)
                info.methodAccess.set(name, access);
        }
    }
    methodSignatureCanOverride(baseMethod, declaration) {
        const parameters = declaration.parameters.map((parameter) => this.resolveTypeName(parameter.paramType));
        const returnType = this.resolveTypeName(declaration.returnType);
        if (parameters.length !== baseMethod.parameters.length)
            return false;
        if (!(0, types_1.sameType)(returnType, baseMethod.returnType))
            return false;
        return parameters.every((type, index) => (0, types_1.sameType)(type, baseMethod.parameters[index].type));
    }
    registerClassFields(info, declaration) {
        const fieldType = this.resolveTypeName(declaration.declaredType);
        if ((0, types_1.sameType)(fieldType, types_1.VOID)) {
            this.diagnostics.error(declaration.range, "class field type cannot be 'void'");
        }
        for (const field of declaration.fields) {
            if (info.fields.has(field.name) || info.methods.has(field.name)) {
                this.diagnostics.error(field.range, `class '${info.declaration.name}' already has member '${field.name}'`);
                continue;
            }
            info.fields.set(field.name, {
                name: field.name,
                type: fieldType,
                range: field.range,
                owner: info.declaration.name,
                access: declaration.access,
            });
            info.ownFields.add(field.name);
        }
    }
    registerClassMethod(info, declaration) {
        const inheritedField = info.fields.get(declaration.name);
        if (inheritedField && inheritedField.owner !== info.declaration.name) {
            this.diagnostics.error(declaration.range, `method '${declaration.name}' conflicts with inherited field '${inheritedField.owner}.${declaration.name}'`);
            return;
        }
        const inheritedMethod = info.methods.get(declaration.name);
        if (inheritedMethod && !this.methodSignatureCanOverride(inheritedMethod, declaration)) {
            this.diagnostics.error(declaration.range, `method '${info.declaration.name}.${declaration.name}' must match inherited method signature`);
            return;
        }
        if ((info.fields.has(declaration.name) && info.ownFields.has(declaration.name)) || (info.methods.has(declaration.name) && info.ownMethods.has(declaration.name))) {
            this.diagnostics.error(declaration.range, `class '${info.declaration.name}' already has member '${declaration.name}'`);
            return;
        }
        const parameters = declaration.parameters.map((parameter) => this.resolveTypeName(parameter.paramType));
        const returnType = this.resolveTypeName(declaration.returnType);
        info.methods.set(declaration.name, {
            name: declaration.name,
            parameters: parameters.map((type, index) => ({ name: declaration.parameters[index].name, type })),
            returnType,
            minArguments: requiredParameterCount(declaration.parameters),
        });
        info.methodDeclarations.set(declaration.name, declaration);
        info.methodAccess.set(declaration.name, {
            name: declaration.name,
            owner: info.declaration.name,
            access: declaration.access,
            isStatic: declaration.isStatic,
            range: declaration.range,
        });
        info.ownMethods.add(declaration.name);
    }
    registerClassConstructor(info, declaration) {
        if (declaration.name !== info.declaration.name) {
            this.diagnostics.error(declaration.range, `constructor name '${declaration.name}' must match class '${info.declaration.name}'`);
        }
        if (info.constructorSpec) {
            this.diagnostics.error(declaration.range, `class '${info.declaration.name}' already has a constructor`);
            return;
        }
        const parameters = declaration.parameters.map((parameter) => this.resolveTypeName(parameter.paramType));
        info.constructorSpec = {
            name: declaration.name,
            parameters: parameters.map((type, index) => ({ name: declaration.parameters[index].name, type })),
            returnType: types_1.VOID,
            minArguments: requiredParameterCount(declaration.parameters),
        };
        info.constructorDeclaration = declaration;
    }
    analyzeClassDeclaration(declaration) {
        const info = this.classes.get(declaration.name);
        if (!info)
            return;
        for (const member of declaration.members) {
            if (member.kind === 'ClassFieldDeclaration') {
                this.analyzeClassFieldInitializers(info, member);
            }
            if (member.kind === 'ClassMethodDeclaration') {
                this.analyzeClassMethod(info, member);
            }
            if (member.kind === 'ConstructorDeclaration') {
                this.analyzeClassConstructor(info, member);
            }
        }
    }
    analyzeClassFieldInitializers(info, declaration) {
        const fieldType = this.resolveTypeName(declaration.declaredType);
        for (const field of declaration.fields) {
            if (!field.initializer)
                continue;
            this.pushClassContext(info.declaration.name, false);
            this.pushScope();
            this.declare('this', (0, types_1.classType)(info.declaration.name), 'parameter', info.declaration.range);
            const initializerType = this.expressionType(field.initializer);
            if (!this.canAssign(fieldType, initializerType)) {
                this.diagnostics.error(field.initializer.range, `cannot assign '${(0, types_1.typeToString)(initializerType)}' value to '${(0, types_1.typeToString)(fieldType)}' field`);
            }
            this.popScope();
            this.popClassContext();
        }
    }
    analyzeClassMethod(info, declaration) {
        const returnType = this.resolveTypeName(declaration.returnType);
        this.returnTypes.push(returnType);
        this.pushClassContext(info.declaration.name, declaration.isStatic);
        this.pushScope();
        if (!declaration.isStatic) {
            this.declare('this', (0, types_1.classType)(info.declaration.name), 'parameter', declaration.range);
        }
        this.analyzeParameters(declaration.parameters);
        this.analyzeStatement(declaration.body);
        this.reportMissingReturn(returnType, declaration.body, declaration.range);
        this.popScope();
        this.popClassContext();
        this.returnTypes.pop();
    }
    analyzeClassConstructor(info, declaration) {
        this.returnTypes.push(types_1.VOID);
        this.pushClassContext(info.declaration.name, false);
        this.pushScope();
        this.declare('this', (0, types_1.classType)(info.declaration.name), 'parameter', declaration.range);
        if (info.declaration.baseName) {
            const baseInfo = this.classes.get(info.declaration.baseName);
            const baseConstructor = baseInfo?.constructorSpec ?? {
                name: 'parent',
                parameters: [],
                returnType: types_1.VOID,
            };
            this.declare('parent', (0, types_1.functionType)(baseConstructor.parameters.map((parameter) => parameter.type), types_1.VOID, baseConstructor.minArguments), 'function', declaration.range);
        }
        this.analyzeParameters(declaration.parameters);
        this.analyzeStatement(declaration.body);
        this.popScope();
        this.popClassContext();
        this.returnTypes.pop();
    }
    analyzeStatement(statement) {
        switch (statement.kind) {
            case 'BlockStatement':
                this.pushScope();
                for (const child of statement.statements) {
                    this.analyzeStatement(child);
                }
                this.popScope();
                return;
            case 'IfStatement':
                this.analyzeIfStatement(statement);
                return;
            case 'WhileStatement':
                this.analyzeWhileStatement(statement);
                return;
            case 'DoWhileStatement':
                this.analyzeDoWhileStatement(statement);
                return;
            case 'ForStatement':
                this.analyzeForStatement(statement);
                return;
            case 'BreakStatement':
                this.analyzeBreakStatement(statement);
                return;
            case 'ContinueStatement':
                this.analyzeContinueStatement(statement);
                return;
            case 'ReturnStatement':
                this.analyzeReturnStatement(statement);
                return;
            case 'VariableDeclaration':
                this.analyzeVariableDeclaration(statement, 'variable');
                return;
            case 'AssignmentStatement':
                this.analyzeAssignment(statement);
                return;
            case 'ExpressionStatement':
                this.expressionType(statement.expression);
                return;
        }
    }
    analyzeIfStatement(statement) {
        this.expectBoolCondition(statement.condition, 'if condition');
        this.analyzeStatement(statement.thenBranch);
        if (statement.elseBranch) {
            this.analyzeStatement(statement.elseBranch);
        }
    }
    analyzeWhileStatement(statement) {
        this.expectBoolCondition(statement.condition, 'while condition');
        this.loopDepth++;
        this.analyzeStatement(statement.body);
        this.loopDepth--;
    }
    analyzeDoWhileStatement(statement) {
        this.loopDepth++;
        this.analyzeStatement(statement.body);
        this.loopDepth--;
        this.expectBoolCondition(statement.condition, 'do-while condition');
    }
    analyzeForStatement(statement) {
        this.pushScope();
        if (statement.initializer) {
            this.analyzeForClause(statement.initializer);
        }
        if (statement.condition) {
            this.expectBoolCondition(statement.condition, 'for condition');
        }
        this.loopDepth++;
        this.analyzeStatement(statement.body);
        this.loopDepth--;
        if (statement.increment) {
            this.analyzeForClause(statement.increment);
        }
        this.popScope();
    }
    analyzeForClause(statement) {
        switch (statement.kind) {
            case 'VariableDeclaration':
                this.analyzeVariableDeclaration(statement, 'variable');
                return;
            case 'AssignmentStatement':
                this.analyzeAssignment(statement);
                return;
            case 'ExpressionStatement':
                this.expressionType(statement.expression);
                return;
        }
    }
    analyzeBreakStatement(statement) {
        if (this.loopDepth === 0) {
            this.diagnostics.error(statement.range, "'break' is only valid inside a loop");
        }
    }
    analyzeContinueStatement(statement) {
        if (this.loopDepth === 0) {
            this.diagnostics.error(statement.range, "'continue' is only valid inside a loop");
        }
    }
    analyzeReturnStatement(statement) {
        const expected = this.returnTypes[this.returnTypes.length - 1] ?? types_1.VOID;
        if (!statement.value) {
            if (!(0, types_1.sameType)(expected, types_1.VOID)) {
                this.diagnostics.error(statement.range, `return value of type '${(0, types_1.typeToString)(expected)}' is required`);
            }
            return;
        }
        const valueType = this.expressionType(statement.value);
        if ((0, types_1.sameType)(expected, types_1.VOID)) {
            this.diagnostics.error(statement.value.range, "void function cannot return a value");
            return;
        }
        if (!this.canAssign(expected, valueType)) {
            this.diagnostics.error(statement.value.range, `cannot return '${(0, types_1.typeToString)(valueType)}' value from '${(0, types_1.typeToString)(expected)}' function`);
        }
    }
    expectBoolCondition(expression, context) {
        const conditionType = this.expressionType(expression);
        if (!(0, types_1.sameType)(conditionType, types_1.BOOL)) {
            this.diagnostics.error(expression.range, `${context} must be 'bool', got '${(0, types_1.typeToString)(conditionType)}'`);
        }
    }
    analyzeVariableDeclaration(statement, kind) {
        const declaredType = this.resolveTypeName(statement.declaredType);
        if (declaredType.kind === 'primitive' && declaredType.name === 'void') {
            this.diagnostics.error(statement.range, "cannot declare variable of type 'void'");
            return;
        }
        if (statement.initializer) {
            const initializerType = this.expressionType(statement.initializer);
            if (declaredType.kind === 'array'
                && initializerType.kind === 'array'
                && !declaredType.dynamic
                && !initializerType.dynamic
                && declaredType.size !== initializerType.size) {
                this.diagnostics.error(statement.initializer.range, `array initializer has ${initializerType.size ?? 0} elements, but '${(0, types_1.typeToString)(declaredType)}' requires ${declaredType.size ?? 0}`);
            }
            if (!this.canAssign(declaredType, initializerType)) {
                this.diagnostics.error(statement.initializer.range, `cannot assign '${(0, types_1.typeToString)(initializerType)}' value to '${(0, types_1.typeToString)(declaredType)}' variable`);
            }
        }
        if (statement.constructorArgs) {
            this.analyzeConstructorArguments(statement, declaredType);
        }
        this.declare(statement.name, declaredType, kind, statement.range);
    }
    analyzeConstructorArguments(statement, declaredType) {
        if (declaredType.kind === 'qualified' && declaredType.moduleName === 'json' && declaredType.name === 'Value') {
            const constructor = this.stdlib.getModuleFunction('json', 'Value');
            if (constructor)
                this.checkArgumentList(statement.constructorArgs ?? [], constructor, statement.range);
            return;
        }
        if (declaredType.kind !== 'class') {
            this.diagnostics.error(statement.range, `constructor-style declaration requires a class type, got '${(0, types_1.typeToString)(declaredType)}'`);
            for (const arg of statement.constructorArgs ?? [])
                this.expressionType(arg.value);
            return;
        }
        const info = this.classes.get(declaredType.name);
        if (!info)
            return;
        const constructor = info.constructorSpec;
        if (!constructor) {
            this.diagnostics.error(statement.range, `class '${declaredType.name}' has no constructor`);
            for (const arg of statement.constructorArgs ?? [])
                this.expressionType(arg.value);
            return;
        }
        if (info.constructorDeclaration?.access === 'private'
            && this.currentClassName() !== declaredType.name) {
            this.diagnostics.error(statement.range, `constructor '${declaredType.name}' is private and can only be used inside class '${declaredType.name}'`);
        }
        this.checkArgumentList(statement.constructorArgs ?? [], constructor, statement.range);
    }
    resolveTypeName(typeName) {
        if (typeName.kind === 'PrimitiveTypeName') {
            return (0, types_1.primitive)(typeName.name);
        }
        if (typeName.kind === 'ArrayTypeName') {
            const elementType = this.resolveTypeName(typeName.elementType);
            if ((0, types_1.sameType)(elementType, types_1.VOID)) {
                this.diagnostics.error(typeName.elementType.range, "array element type cannot be 'void'");
            }
            if (!typeName.dynamic && (typeName.size === null || typeName.size < 0)) {
                this.diagnostics.error(typeName.range, 'array size must be a non-negative integer');
            }
            return (0, types_1.arrayType)(elementType, typeName.size, typeName.dynamic);
        }
        if (typeName.kind === 'ClassTypeName') {
            if (!this.classes.has(typeName.name)) {
                this.diagnostics.error(typeName.range, `unknown class '${typeName.name}'`);
                return types_1.ERROR_TYPE;
            }
            return (0, types_1.classType)(typeName.name);
        }
        if (!this.imports.has(typeName.moduleName)) {
            this.diagnostics.error(typeName.range, `'${typeName.moduleName}' is not imported (use 'use ${typeName.moduleName};')`);
            return types_1.ERROR_TYPE;
        }
        if (!this.stdlib.hasModule(typeName.moduleName)) {
            const module = this.userModuleRegistry.getModule(typeName.moduleName);
            if (!module) {
                if (this.userModules.has(typeName.moduleName))
                    return types_1.ANY_TYPE;
                this.diagnostics.error(typeName.range, `unknown type '${typeName.moduleName}.${typeName.name}'`);
                return types_1.ERROR_TYPE;
            }
            const classSpec = module.classes.get(typeName.name);
            if (!classSpec) {
                this.diagnostics.error(typeName.range, `module '${typeName.moduleName}' has no type '${typeName.name}'`);
                return types_1.ERROR_TYPE;
            }
            return (0, types_1.classType)(classSpec.qualifiedName);
        }
        if (!this.stdlib.hasQualifiedType(typeName.moduleName, typeName.name)) {
            this.diagnostics.error(typeName.range, `unknown type '${typeName.moduleName}.${typeName.name}'`);
            return types_1.ERROR_TYPE;
        }
        return (0, types_1.qualified)(typeName.moduleName, typeName.name);
    }
    analyzeAssignment(statement) {
        const target = this.assignmentTargetInfo(statement.target);
        const targetType = target.type;
        const valueType = this.expressionType(statement.value);
        const assignedType = statement.operator === '='
            ? valueType
            : this.compoundAssignmentType(statement.operator, targetType, valueType, statement.range);
        if (statement.operator === '=' && target.property?.callbacks) {
            this.checkCallbackAssignment(target.property, valueType, statement.value.range);
        }
        if (!this.canAssign(targetType, assignedType)) {
            this.diagnostics.error(statement.value.range, `cannot assign '${(0, types_1.typeToString)(assignedType)}' value to '${(0, types_1.typeToString)(targetType)}' variable`);
        }
    }
    compoundAssignmentType(operator, targetType, valueType, range) {
        const binaryOperator = operator.slice(0, 1);
        const result = this.binaryOperatorType(binaryOperator, targetType, valueType);
        if (result.kind === 'error') {
            this.diagnostics.error(range, `operator '${operator}' cannot be applied to '${(0, types_1.typeToString)(targetType)}' and '${(0, types_1.typeToString)(valueType)}'`);
        }
        return result;
    }
    assignmentTargetInfo(target) {
        if (target.kind === 'IdentifierExpression') {
            const symbol = this.lookup(target.name);
            if (!symbol) {
                this.diagnostics.error(target.range, `variable '${target.name}' was not declared in this scope`);
                return { type: types_1.ERROR_TYPE };
            }
            if (symbol.kind === 'function') {
                this.diagnostics.error(target.range, `cannot assign to function '${target.name}'`);
                return { type: types_1.ERROR_TYPE };
            }
            return { type: symbol.type };
        }
        if (target.kind === 'MemberExpression') {
            const objectType = this.expressionType(target.object);
            if (objectType.kind === 'class') {
                const field = this.getClassField(objectType.name, target.name);
                if (!field) {
                    this.diagnostics.error(target.range, `type '${(0, types_1.typeToString)(objectType)}' has no field '${target.name}'`);
                    return { type: types_1.ERROR_TYPE };
                }
                this.checkClassMemberAccess(field, target.range);
                return { type: field.type };
            }
            const property = this.stdlib.getTypeProperty(objectType, target.name);
            if (!property) {
                this.diagnostics.error(target.range, `type '${(0, types_1.typeToString)(objectType)}' has no property '${target.name}'`);
                return { type: types_1.ERROR_TYPE };
            }
            if (property.readonly) {
                this.diagnostics.error(target.range, `property '${target.name}' is read-only`);
            }
            return { type: property.type, property };
        }
        if (target.kind === 'IndexExpression') {
            const objectType = this.expressionType(target.object);
            if (this.isStringType(objectType)) {
                this.expressionType(target.index);
                this.diagnostics.error(target.range, 'string characters are read-only');
                return { type: types_1.ERROR_TYPE };
            }
            return { type: this.indexExpressionType(target) };
        }
        this.diagnostics.error(target.range, 'assignment target must be a variable, object property, or array element');
        return { type: types_1.ERROR_TYPE };
    }
    checkCallbackAssignment(property, valueType, range) {
        if (!property.callbacks || valueType.kind === 'error' || valueType.kind === 'any')
            return;
        if (valueType.kind !== 'function') {
            this.diagnostics.error(range, `callback property '${property.name}' expects a function, got '${(0, types_1.typeToString)(valueType)}'`);
            return;
        }
        const matches = property.callbacks.some((signature) => {
            if (signature.parameters.length !== valueType.parameters.length)
                return false;
            if (!this.canAssign(signature.returnType, valueType.returnType))
                return false;
            return signature.parameters.every((runtimeArgType, index) => (this.canAssign(valueType.parameters[index], runtimeArgType)));
        });
        if (!matches) {
            const expected = property.callbacks.map((signature) => this.callbackSignatureText(signature.parameters, signature.returnType)).join(' or ');
            this.diagnostics.error(range, `callback property '${property.name}' expects ${expected}, got ${(0, types_1.typeToString)(valueType)}`);
        }
    }
    callbackSignatureText(parameters, returnType) {
        return `function(${parameters.map(types_1.typeToString).join(', ')}): ${(0, types_1.typeToString)(returnType)}`;
    }
    expressionType(expression) {
        switch (expression.kind) {
            case 'LiteralExpression':
                return (0, types_1.primitive)(expression.valueType);
            case 'IdentifierExpression':
                return this.identifierType(expression.name, expression.range);
            case 'UnaryExpression':
                return this.unaryType(expression);
            case 'BinaryExpression':
                return this.binaryType(expression);
            case 'ArrayLiteralExpression':
                return this.arrayLiteralType(expression);
            case 'IndexExpression':
                return this.indexExpressionType(expression);
            case 'FunctionExpression':
                return this.functionExpressionType(expression);
            case 'CallExpression':
                return this.callType(expression);
            case 'MemberExpression':
                return this.memberType(expression);
        }
    }
    functionExpressionType(expression) {
        const returnType = this.resolveTypeName(expression.returnType);
        const parameters = expression.parameters.map((parameter) => this.resolveTypeName(parameter.paramType));
        this.returnTypes.push(returnType);
        this.pushScope();
        this.analyzeParameters(expression.parameters);
        this.analyzeStatement(expression.body);
        this.reportMissingReturn(returnType, expression.body, expression.range);
        this.popScope();
        this.returnTypes.pop();
        return (0, types_1.functionType)(parameters, returnType, requiredParameterCount(expression.parameters));
    }
    reportMissingReturn(returnType, body, range) {
        if ((0, types_1.sameType)(returnType, types_1.VOID) || returnType.kind === 'error')
            return;
        if (this.statementAlwaysReturns(body))
            return;
        this.diagnostics.error(range, `function with return type '${(0, types_1.typeToString)(returnType)}' must return a value`);
    }
    statementAlwaysReturns(statement) {
        switch (statement.kind) {
            case 'ReturnStatement':
                return true;
            case 'BlockStatement':
                return statement.statements.some((child) => this.statementAlwaysReturns(child));
            case 'IfStatement':
                return statement.elseBranch !== null
                    && this.statementAlwaysReturns(statement.thenBranch)
                    && this.statementAlwaysReturns(statement.elseBranch);
            default:
                return false;
        }
    }
    arrayLiteralType(expression) {
        let elementType = types_1.ANY_TYPE;
        for (const element of expression.elements) {
            const currentType = this.expressionType(element);
            if (elementType.kind === 'any') {
                elementType = currentType;
                continue;
            }
            const merged = this.mergeArrayElementTypes(elementType, currentType);
            if (merged.kind === 'error') {
                this.diagnostics.error(element.range, `array element type '${(0, types_1.typeToString)(currentType)}' does not match '${(0, types_1.typeToString)(elementType)}'`);
            }
            else {
                elementType = merged;
            }
        }
        return (0, types_1.arrayType)(elementType, expression.elements.length, false);
    }
    mergeArrayElementTypes(left, right) {
        if (this.canAssign(left, right))
            return left;
        if (this.canAssign(right, left))
            return right;
        if ((0, types_1.isNumeric)(left) && (0, types_1.isNumeric)(right)) {
            return (0, types_1.sameType)(left, types_1.FLOAT) || (0, types_1.sameType)(right, types_1.FLOAT) ? types_1.FLOAT : types_1.INT;
        }
        return types_1.ERROR_TYPE;
    }
    indexExpressionType(expression) {
        const objectType = this.expressionType(expression.object);
        const indexType = this.expressionType(expression.index);
        if (!(0, types_1.isIntegerLike)(indexType)) {
            this.diagnostics.error(expression.index.range, `array index must be integer, got '${(0, types_1.typeToString)(indexType)}'`);
        }
        if (this.isStringType(objectType)) {
            return types_1.CHAR;
        }
        if (objectType.kind !== 'array') {
            this.diagnostics.error(expression.object.range, `indexing requires an array or string, got '${(0, types_1.typeToString)(objectType)}'`);
            return types_1.ERROR_TYPE;
        }
        return objectType.elementType;
    }
    identifierType(name, range) {
        const symbol = this.lookup(name);
        if (symbol)
            return symbol.type;
        if (this.stdlib.hasModule(name)) {
            if (!this.imports.has(name)) {
                this.diagnostics.error(range, `'${name}' is not imported (use 'use ${name};')`);
            }
            return types_1.ERROR_TYPE;
        }
        if (this.userModules.has(name)) {
            return types_1.ANY_TYPE;
        }
        if (this.classes.has(name)) {
            this.diagnostics.error(range, `class '${name}' cannot be used as a value`);
            return types_1.ERROR_TYPE;
        }
        if (name === 'this') {
            if (this.currentClassContext()?.isStatic) {
                this.diagnostics.error(range, "'this' cannot be used in a static method");
                return types_1.ERROR_TYPE;
            }
            this.diagnostics.error(range, "'this' can only be used inside a class");
            return types_1.ERROR_TYPE;
        }
        this.diagnostics.error(range, `'${name}' was not declared in this scope`);
        return types_1.ERROR_TYPE;
    }
    unaryType(expression) {
        const operandType = this.expressionType(expression.operand);
        if (expression.operator === 'not') {
            if (!(0, types_1.sameType)(operandType, types_1.BOOL)) {
                this.diagnostics.error(expression.operand.range, `operator 'not' requires 'bool', got '${(0, types_1.typeToString)(operandType)}'`);
                return types_1.ERROR_TYPE;
            }
            return types_1.BOOL;
        }
        if (!(0, types_1.isNumeric)(operandType)) {
            this.diagnostics.error(expression.operand.range, `unary '-' requires numeric operand, got '${(0, types_1.typeToString)(operandType)}'`);
            return types_1.ERROR_TYPE;
        }
        return operandType;
    }
    binaryType(expression) {
        const left = this.expressionType(expression.left);
        const right = this.expressionType(expression.right);
        if (['and', 'or'].includes(expression.operator)) {
            if (!(0, types_1.sameType)(left, types_1.BOOL) || !(0, types_1.sameType)(right, types_1.BOOL)) {
                this.diagnostics.error(expression.range, `operator '${expression.operator}' requires bool operands`);
            }
            return types_1.BOOL;
        }
        if (['==', '!='].includes(expression.operator)) {
            if (!(0, types_1.sameType)(left, right) && !this.canAssign(left, right) && !this.canAssign(right, left)) {
                this.diagnostics.error(expression.range, `cannot compare '${(0, types_1.typeToString)(left)}' and '${(0, types_1.typeToString)(right)}'`);
            }
            return types_1.BOOL;
        }
        if (['<', '<=', '>', '>='].includes(expression.operator)) {
            if (!(0, types_1.isNumeric)(left) || !(0, types_1.isNumeric)(right)) {
                this.diagnostics.error(expression.range, `comparison '${expression.operator}' requires numeric operands`);
            }
            return types_1.BOOL;
        }
        const result = this.binaryOperatorType(expression.operator, left, right);
        if (result.kind === 'error') {
            this.diagnostics.error(expression.range, `operator '${expression.operator}' cannot be applied to '${(0, types_1.typeToString)(left)}' and '${(0, types_1.typeToString)(right)}'`);
        }
        return result;
    }
    binaryOperatorType(operator, left, right) {
        if (operator === '+' && left.kind === 'primitive' && right.kind === 'primitive') {
            if (left.name === 'string' && (right.name === 'string' || right.name === 'char')) {
                return types_1.STRING;
            }
            if (left.name === 'char' && right.name === 'string') {
                return types_1.STRING;
            }
        }
        return (0, types_1.numericBinaryResult)(operator, left, right);
    }
    callType(expression) {
        if (expression.callee.kind === 'IdentifierExpression' && this.isArrayGlobalFunction(expression.callee.name)) {
            return this.arrayGlobalFunctionType(expression.callee.name, expression);
        }
        const specialMathType = this.specialMathCallType(expression);
        if (specialMathType)
            return specialMathType;
        const resolved = this.resolveCall(expression);
        if (!resolved) {
            for (const arg of expression.args)
                this.expressionType(arg.value);
            return types_1.ERROR_TYPE;
        }
        this.checkArguments(expression, resolved);
        return resolved.returnType;
    }
    isArrayGlobalFunction(name) {
        return name === 'max' || name === 'min' || name === 'sum' || name === 'avg';
    }
    arrayGlobalFunctionType(name, expression) {
        const fn = { name, parameters: [{ name: 'array', type: types_1.ANY_TYPE }], returnType: types_1.ANY_TYPE };
        this.checkArgumentList(expression.args, fn, expression.range);
        const ordered = this.orderedArguments(expression.args, fn);
        const arg = ordered[0];
        if (!arg) {
            return types_1.ERROR_TYPE;
        }
        const argType = this.expressionType(arg.value);
        if (argType.kind !== 'array') {
            this.diagnostics.error(arg.range, `'${name}' expects an array, got '${(0, types_1.typeToString)(argType)}'`);
            return types_1.ERROR_TYPE;
        }
        if (!(0, types_1.isNumeric)(argType.elementType)) {
            this.diagnostics.error(arg.range, `'${name}' expects a numeric array, got '${(0, types_1.typeToString)(argType)}'`);
            return types_1.ERROR_TYPE;
        }
        return name === 'avg' ? types_1.FLOAT : argType.elementType;
    }
    specialMathCallType(expression) {
        const callee = expression.callee;
        if (callee.kind !== 'MemberExpression' || callee.object.kind !== 'IdentifierExpression') {
            return null;
        }
        if (callee.object.name !== 'math')
            return null;
        if (callee.name === 'round' || callee.name === 'floor' || callee.name === 'ceil') {
            if (!this.imports.has('math')) {
                this.diagnostics.error(callee.object.range, "'math' is not imported (use 'use math;')");
                return types_1.ERROR_TYPE;
            }
            const fn = {
                name: callee.name,
                parameters: [
                    { name: 'value', type: types_1.FLOAT },
                    { name: 'digits', type: types_1.INT },
                ],
                returnType: types_1.FLOAT,
                minArguments: 1,
            };
            this.checkArgumentList(expression.args, fn, expression.range);
            if (expression.args.length === 2) {
                return types_1.FLOAT;
            }
            return types_1.INT;
        }
        if (callee.name === 'clamp') {
            if (!this.imports.has('math')) {
                this.diagnostics.error(callee.object.range, "'math' is not imported (use 'use math;')");
                return types_1.ERROR_TYPE;
            }
            const fn = {
                name: 'clamp',
                parameters: [
                    { name: 'min', type: types_1.FLOAT },
                    { name: 'value', type: types_1.FLOAT },
                    { name: 'max', type: types_1.FLOAT },
                ],
                returnType: types_1.FLOAT,
            };
            this.checkArgumentList(expression.args, fn, expression.range);
            const argTypes = expression.args.map((arg) => this.expressionType(arg.value));
            for (let i = 0; i < argTypes.length; i++) {
                if (!(0, types_1.isNumeric)(argTypes[i])) {
                    this.diagnostics.error(expression.args[i].range, `'clamp' argument ${i + 1} expects numeric value, got '${(0, types_1.typeToString)(argTypes[i])}'`);
                }
            }
            return argTypes.every((type) => (0, types_1.isIntegerLike)(type)) ? types_1.INT : types_1.FLOAT;
        }
        return null;
    }
    resolveCall(expression) {
        const callee = expression.callee;
        if (callee.kind === 'IdentifierExpression') {
            if (this.classes.has(callee.name)) {
                this.diagnostics.error(callee.range, `class '${callee.name}' cannot be called as a function; declare an object with '${callee.name} name(...)'`);
                return null;
            }
            const global = this.stdlib.getGlobalFunction(callee.name);
            if (global)
                return global;
            const localFunction = this.functions.get(callee.name);
            if (localFunction) {
                const parameters = localFunction.parameters.map((parameter) => ({
                    name: parameter.name,
                    type: this.resolveTypeName(parameter.paramType),
                }));
                return {
                    name: callee.name,
                    parameters,
                    returnType: this.resolveTypeName(localFunction.returnType),
                    minArguments: requiredParameterCount(localFunction.parameters),
                };
            }
            const symbol = this.lookup(callee.name);
            if (symbol?.type.kind === 'function') {
                return {
                    name: callee.name,
                    parameters: symbol.type.parameters.map((type, index) => ({ name: `arg${index + 1}`, type })),
                    returnType: symbol.type.returnType,
                    minArguments: symbol.type.minArguments,
                };
            }
            this.diagnostics.error(callee.range, `function '${callee.name}' was not declared in this scope`);
            return null;
        }
        if (callee.kind === 'MemberExpression' && callee.object.kind === 'IdentifierExpression') {
            const moduleName = callee.object.name;
            const module = this.stdlib.getModule(moduleName);
            if (module) {
                if (!this.imports.has(moduleName)) {
                    this.diagnostics.error(callee.object.range, `'${moduleName}' is not imported (use 'use ${moduleName};')`);
                    return null;
                }
                const fn = this.stdlib.getModuleFunction(moduleName, callee.name);
                if (!fn) {
                    this.diagnostics.error(callee.range, `'${moduleName}' has no function '${callee.name}'`);
                    return null;
                }
                return fn;
            }
            const userModule = this.userModuleRegistry.getModule(moduleName);
            if (userModule) {
                if (!this.imports.has(moduleName)) {
                    this.diagnostics.error(callee.object.range, `'${moduleName}' is not imported (use 'use ${moduleName};')`);
                    return null;
                }
                const fn = userModule.functions.get(callee.name);
                if (!fn) {
                    this.diagnostics.error(callee.range, `module '${moduleName}' has no function '${callee.name}'`);
                    return null;
                }
                return fn;
            }
            if (this.userModules.has(moduleName)) {
                return { name: callee.name, parameters: [], returnType: types_1.ANY_TYPE, variadic: true, variadicTypes: [types_1.ANY_TYPE] };
            }
            const classInfo = this.classes.get(moduleName);
            if (classInfo) {
                const method = classInfo.methods.get(callee.name);
                if (method) {
                    const access = classInfo.methodAccess.get(callee.name);
                    if (access?.isStatic) {
                        this.checkClassMemberAccess(access, callee.range);
                        return method;
                    }
                    this.diagnostics.error(callee.range, `instance method '${moduleName}.${callee.name}' must be called on an object`);
                    return null;
                }
                if (classInfo.fields.has(callee.name)) {
                    this.diagnostics.error(callee.range, `instance field '${moduleName}.${callee.name}' must be accessed through an object`);
                    return null;
                }
                this.diagnostics.error(callee.range, `class '${moduleName}' has no static method '${callee.name}'`);
                return null;
            }
        }
        if (callee.kind === 'MemberExpression') {
            const objectType = this.expressionType(callee.object);
            if (this.isStringType(objectType)) {
                const method = this.stringMethodSpec(callee.name);
                if (method)
                    return method;
                this.diagnostics.error(callee.range, `type 'string' has no method '${callee.name}'`);
                return null;
            }
            if (objectType.kind === 'array') {
                const method = this.arrayMethodSpec(objectType, callee.name);
                if (method)
                    return method;
                this.reportUnknownArrayMethod(objectType, callee.name, callee.range);
                return null;
            }
            if (objectType.kind === 'class') {
                const method = this.getClassMethodInfo(objectType.name, callee.name);
                if (method) {
                    this.checkClassMemberAccess(method.access, callee.range);
                    return method.spec;
                }
                const staticMethod = this.getClassStaticMethodInfo(objectType.name, callee.name);
                if (staticMethod) {
                    this.diagnostics.error(callee.range, `static method '${staticMethod.access.owner}.${staticMethod.access.name}' must be called on class '${staticMethod.access.owner}'`);
                    return null;
                }
                this.diagnostics.error(callee.range, `type '${(0, types_1.typeToString)(objectType)}' has no method '${callee.name}'`);
                return null;
            }
            const method = this.stdlib.getTypeMethod(objectType, callee.name);
            if (method)
                return method;
            this.diagnostics.error(callee.range, `type '${(0, types_1.typeToString)(objectType)}' has no method '${callee.name}'`);
            return null;
        }
        this.diagnostics.error(callee.range, 'only function and method calls are supported in this compiler slice');
        return null;
    }
    checkArguments(expression, fn) {
        this.checkArgumentList(expression.args, fn, expression.range);
    }
    checkArgumentList(args, fn, range) {
        const minArguments = fn.minArguments ?? fn.parameters.length;
        const maxArguments = fn.parameters.length;
        const { resolved, providedCount, positionalCount } = this.resolveArguments(args, fn, range);
        const hasNamedArguments = args.some((arg) => arg.name !== null);
        if (!fn.variadic && positionalCount > maxArguments) {
            this.diagnostics.error(range, `'${fn.name}' expects ${argumentCountText(minArguments, maxArguments)} arguments, got ${args.length}`);
        }
        else if (!fn.variadic && !hasNamedArguments && args.length < minArguments) {
            this.diagnostics.error(range, `'${fn.name}' expects ${argumentCountText(minArguments, maxArguments)} arguments, got ${args.length}`);
        }
        else if (!fn.variadic) {
            for (let i = 0; i < minArguments; i++) {
                if (!resolved.some((item) => item.parameterIndex === i)) {
                    this.diagnostics.error(range, `'${fn.name}' missing required argument '${fn.parameters[i].name}'`);
                }
            }
            if (providedCount > maxArguments) {
                this.diagnostics.error(range, `'${fn.name}' expects ${argumentCountText(minArguments, maxArguments)} arguments, got ${args.length}`);
            }
        }
        if (fn.variadic && args.length < minArguments) {
            this.diagnostics.error(range, `'${fn.name}' expects at least ${minArguments} arguments, got ${args.length}`);
        }
        for (const item of resolved) {
            const argType = this.expressionType(item.arg.value);
            const parameter = item.parameter;
            if (parameter) {
                if (parameter.acceptedTypes) {
                    const accepts = parameter.acceptedTypes.some((candidate) => this.canAssign(candidate, argType));
                    if (!accepts) {
                        this.diagnostics.error(item.arg.range, this.argumentTypeError(fn, item, parameter.acceptedDescription ?? parameter.acceptedTypes.map(types_1.typeToString).join(' or '), argType));
                    }
                    continue;
                }
                if (!this.canAssign(parameter.type, argType)) {
                    this.diagnostics.error(item.arg.range, this.argumentTypeError(fn, item, `'${(0, types_1.typeToString)(parameter.type)}'`, argType));
                }
                continue;
            }
            if (fn.variadicTypes && !fn.variadicTypes.some((candidate) => this.canAssign(candidate, argType))) {
                this.diagnostics.error(item.arg.range, `'${fn.name}' does not accept argument of type '${(0, types_1.typeToString)(argType)}'`);
            }
        }
    }
    resolveArguments(args, fn, range) {
        const resolved = [];
        const assigned = new Map();
        let sawNamed = false;
        let positionalCount = 0;
        for (let argumentIndex = 0; argumentIndex < args.length; argumentIndex++) {
            const arg = args[argumentIndex];
            if (arg.name !== null) {
                sawNamed = true;
                if (fn.variadic) {
                    this.diagnostics.error(arg.range, `'${fn.name}' does not support named arguments`);
                    resolved.push({ arg, parameter: null, parameterIndex: null, argumentIndex });
                    continue;
                }
                const parameterIndex = fn.parameters.findIndex((parameter) => parameter.name === arg.name);
                if (parameterIndex < 0) {
                    this.diagnostics.error(arg.range, `'${fn.name}' has no argument named '${arg.name}'`);
                    resolved.push({ arg, parameter: null, parameterIndex: null, argumentIndex });
                    continue;
                }
                if (assigned.has(parameterIndex)) {
                    this.diagnostics.error(arg.range, `'${fn.name}' argument '${arg.name}' was already provided`);
                }
                assigned.set(parameterIndex, arg);
                resolved.push({ arg, parameter: fn.parameters[parameterIndex], parameterIndex, argumentIndex });
                continue;
            }
            if (sawNamed) {
                this.diagnostics.error(arg.range, 'positional argument cannot follow named argument');
            }
            const parameterIndex = positionalCount;
            positionalCount += 1;
            const parameter = fn.parameters[parameterIndex] ?? null;
            if (parameter && assigned.has(parameterIndex)) {
                this.diagnostics.error(arg.range, `'${fn.name}' argument '${parameter.name}' was already provided`);
            }
            if (parameter)
                assigned.set(parameterIndex, arg);
            resolved.push({ arg, parameter, parameterIndex: parameter ? parameterIndex : null, argumentIndex });
        }
        return {
            resolved,
            providedCount: assigned.size,
            positionalCount,
        };
    }
    orderedArguments(args, fn) {
        if (!args.some((arg) => arg.name !== null))
            return [...args];
        const ordered = new Array(fn.parameters.length).fill(null);
        let positionalIndex = 0;
        for (const arg of args) {
            if (arg.name !== null) {
                const parameterIndex = fn.parameters.findIndex((parameter) => parameter.name === arg.name);
                if (parameterIndex >= 0)
                    ordered[parameterIndex] = arg;
                continue;
            }
            if (positionalIndex < ordered.length)
                ordered[positionalIndex] = arg;
            positionalIndex += 1;
        }
        return ordered.filter((arg) => arg !== null);
    }
    argumentTypeError(fn, item, expected, actual) {
        const label = item.arg.name ? `argument '${item.arg.name}'` : `argument ${item.argumentIndex + 1}`;
        return `'${fn.name}' ${label} expects ${expected}, got '${(0, types_1.typeToString)(actual)}'`;
    }
    memberType(expression) {
        if (expression.object.kind === 'IdentifierExpression') {
            const moduleName = expression.object.name;
            const module = this.stdlib.getModule(moduleName);
            if (module) {
                if (!this.imports.has(moduleName)) {
                    this.diagnostics.error(expression.object.range, `'${moduleName}' is not imported (use 'use ${moduleName};')`);
                    return types_1.ERROR_TYPE;
                }
                const constant = module.constants.get(expression.name);
                if (constant)
                    return constant.type;
                const fn = module.functions.get(expression.name);
                if (fn)
                    return (0, types_1.functionType)(fn.parameters.map((param) => param.type), fn.returnType);
                if (module.types.has(expression.name))
                    return types_1.ERROR_TYPE;
                this.diagnostics.error(expression.range, `'${moduleName}' has no member '${expression.name}'`);
                return types_1.ERROR_TYPE;
            }
            const userModule = this.userModuleRegistry.getModule(moduleName);
            if (userModule) {
                if (!this.imports.has(moduleName)) {
                    this.diagnostics.error(expression.object.range, `'${moduleName}' is not imported (use 'use ${moduleName};')`);
                    return types_1.ERROR_TYPE;
                }
                const fn = userModule.functions.get(expression.name);
                if (fn)
                    return (0, types_1.functionType)(fn.parameters.map((param) => param.type), fn.returnType);
                if (userModule.classes.has(expression.name))
                    return types_1.ERROR_TYPE;
                this.diagnostics.error(expression.range, `module '${moduleName}' has no member '${expression.name}'`);
                return types_1.ERROR_TYPE;
            }
            if (this.userModules.has(moduleName)) {
                return types_1.ANY_TYPE;
            }
            const classInfo = this.classes.get(moduleName);
            if (classInfo) {
                const method = classInfo.methods.get(expression.name);
                if (method) {
                    const access = classInfo.methodAccess.get(expression.name);
                    if (access?.isStatic) {
                        this.checkClassMemberAccess(access, expression.range);
                        return (0, types_1.functionType)(method.parameters.map((param) => param.type), method.returnType);
                    }
                    this.diagnostics.error(expression.range, `instance method '${moduleName}.${expression.name}' must be called on an object`);
                    return types_1.ERROR_TYPE;
                }
                if (classInfo.fields.has(expression.name)) {
                    this.diagnostics.error(expression.range, `instance field '${moduleName}.${expression.name}' must be accessed through an object`);
                    return types_1.ERROR_TYPE;
                }
                this.diagnostics.error(expression.range, `class '${moduleName}' has no member '${expression.name}'`);
                return types_1.ERROR_TYPE;
            }
        }
        const objectType = this.expressionType(expression.object);
        if (this.isStringType(objectType)) {
            const method = this.stringMethodSpec(expression.name);
            if (method)
                return (0, types_1.functionType)(method.parameters.map((param) => param.type), method.returnType);
            this.diagnostics.error(expression.range, `type 'string' has no member '${expression.name}'`);
            return types_1.ERROR_TYPE;
        }
        if (objectType.kind === 'array') {
            const method = this.arrayMethodSpec(objectType, expression.name);
            if (method)
                return (0, types_1.functionType)(method.parameters.map((param) => param.type), method.returnType);
            this.reportUnknownArrayMethod(objectType, expression.name, expression.range);
            return types_1.ERROR_TYPE;
        }
        if (objectType.kind === 'class') {
            const field = this.getClassField(objectType.name, expression.name);
            if (field) {
                this.checkClassMemberAccess(field, expression.range);
                return field.type;
            }
            const method = this.getClassMethodInfo(objectType.name, expression.name);
            if (method) {
                this.checkClassMemberAccess(method.access, expression.range);
                return (0, types_1.functionType)(method.spec.parameters.map((param) => param.type), method.spec.returnType);
            }
            const staticMethod = this.getClassStaticMethodInfo(objectType.name, expression.name);
            if (staticMethod) {
                this.diagnostics.error(expression.range, `static method '${staticMethod.access.owner}.${staticMethod.access.name}' must be called on class '${staticMethod.access.owner}'`);
                return types_1.ERROR_TYPE;
            }
            this.diagnostics.error(expression.range, `type '${(0, types_1.typeToString)(objectType)}' has no member '${expression.name}'`);
            return types_1.ERROR_TYPE;
        }
        const property = this.stdlib.getTypeProperty(objectType, expression.name);
        if (property)
            return property.type;
        const method = this.stdlib.getTypeMethod(objectType, expression.name);
        if (method)
            return (0, types_1.functionType)(method.parameters.map((param) => param.type), method.returnType);
        this.diagnostics.error(expression.range, `type '${(0, types_1.typeToString)(objectType)}' has no member '${expression.name}'`);
        return types_1.ERROR_TYPE;
    }
    isStringType(type) {
        return type.kind === 'primitive' && type.name === 'string';
    }
    stringMethodSpec(name) {
        switch (name) {
            case 'length':
                return { name, parameters: [], returnType: types_1.INT };
            case 'contains':
            case 'find':
            case 'count':
                return { name, parameters: [{ name: 'text', type: types_1.ANY_TYPE }], returnType: name === 'contains' ? types_1.BOOL : types_1.INT };
            case 'is_int':
            case 'is_float':
                return { name, parameters: [], returnType: types_1.BOOL };
            case 'to_upper':
            case 'to_lower':
            case 'trim':
                return { name, parameters: [], returnType: types_1.STRING };
            case 'substring':
                return { name, parameters: [{ name: 'start', type: types_1.INT }, { name: 'length', type: types_1.INT }], returnType: types_1.STRING };
            case 'replace':
                return { name, parameters: [{ name: 'old_text', type: types_1.STRING }, { name: 'new_text', type: types_1.STRING }], returnType: types_1.STRING };
            case 'split':
                return { name, parameters: [{ name: 'separator', type: types_1.STRING }], returnType: (0, types_1.arrayType)(types_1.STRING, null, true) };
            default:
                return null;
        }
    }
    arrayMethodSpec(type, name) {
        const value = { name: 'value', type: type.elementType };
        const index = { name: 'index', type: types_1.INT };
        switch (name) {
            case 'length':
                return { name, parameters: [], returnType: types_1.INT };
            case 'contains':
                return { name, parameters: [value], returnType: types_1.BOOL };
            case 'find':
                return { name, parameters: [value], returnType: types_1.INT };
            case 'count':
                return { name, parameters: [value], returnType: types_1.INT };
            case 'reverse':
            case 'sort':
                return { name, parameters: [], returnType: types_1.VOID };
            case 'add':
                return type.dynamic ? { name, parameters: [value], returnType: types_1.VOID } : null;
            case 'remove_at':
            case 'resize':
                return type.dynamic ? { name, parameters: [{ name: 'size', type: types_1.INT }], returnType: types_1.VOID } : null;
            case 'insert':
                return type.dynamic ? { name, parameters: [index, value], returnType: types_1.VOID } : null;
            case 'join':
                return type.dynamic
                    ? { name, parameters: [{ name: 'other', type: (0, types_1.arrayType)(type.elementType, null, true) }], returnType: types_1.VOID }
                    : null;
            case 'clear':
                return type.dynamic ? { name, parameters: [], returnType: types_1.VOID } : null;
            case 'pop':
                return type.dynamic ? { name, parameters: [], returnType: type.elementType } : null;
            default:
                return null;
        }
    }
    reportUnknownArrayMethod(type, name, range) {
        if (!type.dynamic && this.isDynamicArrayOnlyMethod(name)) {
            this.diagnostics.error(range, `method '${name}' is only available on 'dyn_array'`);
            return;
        }
        this.diagnostics.error(range, `type '${(0, types_1.typeToString)(type)}' has no method '${name}'`);
    }
    isDynamicArrayOnlyMethod(name) {
        return name === 'add'
            || name === 'remove_at'
            || name === 'resize'
            || name === 'insert'
            || name === 'join'
            || name === 'clear'
            || name === 'pop';
    }
    getClassField(className, fieldName) {
        return this.classes.get(className)?.fields.get(fieldName) ?? null;
    }
    getClassMethodInfo(className, methodName) {
        const info = this.classes.get(className);
        if (!info)
            return null;
        const method = info.methods.get(methodName);
        if (!method)
            return null;
        const access = info.methodAccess.get(methodName);
        if (!access)
            return null;
        if (access.isStatic)
            return null;
        return { spec: method, access };
    }
    getClassStaticMethodInfo(className, methodName) {
        const info = this.classes.get(className);
        if (!info)
            return null;
        const method = info.methods.get(methodName);
        if (!method)
            return null;
        const access = info.methodAccess.get(methodName);
        if (!access)
            return null;
        if (!access.isStatic)
            return null;
        return { spec: method, access };
    }
    checkClassMemberAccess(member, range) {
        if (member.access === 'public')
            return;
        if (this.currentClassName() === member.owner)
            return;
        this.diagnostics.error(range, `member '${member.owner}.${member.name}' is private and can only be used inside class '${member.owner}'`);
    }
    canAssign(target, value) {
        if ((0, types_1.isAssignable)(target, value))
            return true;
        if (target.kind === 'class' && value.kind === 'class') {
            return this.classExtends(value.name, target.name);
        }
        if (target.kind === 'qualified' && value.kind === 'qualified') {
            return this.stdlib.typeExtends(value, target);
        }
        if (target.kind === 'array' && value.kind === 'array') {
            const sizeMatches = target.dynamic || (!value.dynamic && target.size === value.size);
            return sizeMatches && this.canAssign(target.elementType, value.elementType);
        }
        return false;
    }
    classExtends(childName, parentName) {
        let current = this.classes.get(childName);
        const seen = new Set();
        while (current?.declaration.baseName) {
            const baseName = current.declaration.baseName;
            if (baseName === parentName)
                return true;
            if (seen.has(baseName))
                return false;
            seen.add(baseName);
            current = this.classes.get(baseName);
        }
        return false;
    }
    declare(name, type, kind, range) {
        const scope = this.currentScope();
        if (scope.has(name)) {
            this.diagnostics.error(range, `'${name}' is already declared in this scope`);
            return;
        }
        scope.set(name, { type, kind, range });
    }
    lookup(name) {
        for (let i = this.scopes.length - 1; i >= 0; i--) {
            const symbol = this.scopes[i].get(name);
            if (symbol)
                return symbol;
        }
        return null;
    }
    pushScope() {
        this.scopes.push(new Map());
    }
    popScope() {
        this.scopes.pop();
    }
    pushClassContext(className, isStatic) {
        this.classContexts.push({ className, isStatic });
    }
    popClassContext() {
        this.classContexts.pop();
    }
    currentClassName() {
        return this.currentClassContext()?.className ?? null;
    }
    currentClassContext() {
        return this.classContexts[this.classContexts.length - 1] ?? null;
    }
    currentScope() {
        return this.scopes[this.scopes.length - 1];
    }
}
exports.SemanticAnalyzer = SemanticAnalyzer;
function argumentCountText(minArguments, maxArguments) {
    if (minArguments === maxArguments)
        return String(maxArguments);
    if (maxArguments === minArguments + 1)
        return `${minArguments} or ${maxArguments}`;
    return `${minArguments}-${maxArguments}`;
}
function requiredParameterCount(parameters) {
    const firstDefault = parameters.findIndex((parameter) => parameter.defaultValue !== null);
    return firstDefault < 0 ? parameters.length : firstDefault;
}
//# sourceMappingURL=semantics.js.map
},
"src/core/stdlib/registry.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StandardLibraryRegistry = void 0;
exports.createDefaultStandardLibrary = createDefaultStandardLibrary;
const types_1 = require("../types");
class StandardLibraryRegistry {
    modules = new Map();
    globals = new Map();
    registerModule(module) {
        this.modules.set(module.name, module);
    }
    registerGlobalFunction(spec) {
        this.globals.set(spec.name, spec);
    }
    hasModule(name) {
        return this.modules.has(name);
    }
    getModule(name) {
        return this.modules.get(name);
    }
    getModuleFunction(moduleName, functionName) {
        return this.modules.get(moduleName)?.functions.get(functionName);
    }
    hasQualifiedType(moduleName, typeName) {
        return this.modules.get(moduleName)?.types.has(typeName) ?? false;
    }
    getQualifiedType(moduleName, typeName) {
        return this.modules.get(moduleName)?.types.get(typeName);
    }
    getTypeProperty(type, propertyName) {
        if (type.kind !== 'qualified')
            return undefined;
        return this.findTypeProperty(type, propertyName, new Set());
    }
    getTypeMethod(type, methodName) {
        if (type.kind !== 'qualified')
            return undefined;
        return this.findTypeMethod(type, methodName, new Set());
    }
    listTypeMembers(type) {
        if (type.kind !== 'qualified')
            return [];
        const members = new Map();
        this.collectTypeMembers(type, members, new Set());
        return [...members.values()].sort((left, right) => left.name.localeCompare(right.name));
    }
    typeExtends(child, parent) {
        if (child.kind !== 'qualified' || parent.kind !== 'qualified')
            return false;
        return this.qualifiedTypeExtends(child, parent, new Set());
    }
    getGlobalFunction(name) {
        return this.globals.get(name);
    }
    listModules() {
        return [...this.modules.values()].map((module) => ({
            name: module.name,
            kind: 'module',
            detail: `module ${module.name}`,
        }));
    }
    listModuleMembers(moduleName) {
        const module = this.modules.get(moduleName);
        if (!module)
            return [];
        const functions = [...module.functions.values()].map((fn) => ({
            name: fn.name,
            kind: 'function',
            detail: signatureDetail(fn),
        }));
        const constants = [...module.constants.values()].map((constant) => ({
            name: constant.name,
            kind: 'constant',
            detail: `${constant.name}: ${(0, types_1.typeToString)(constant.type)}`,
        }));
        const types = [...module.types.values()].map((typeSpec) => ({
            name: typeSpec.name,
            kind: 'type',
            detail: `type ${moduleName}.${typeSpec.name}`,
        }));
        return [...types, ...constants, ...functions].sort((left, right) => left.name.localeCompare(right.name));
    }
    findTypeProperty(type, propertyName, seen) {
        const typeSpec = this.getQualifiedType(type.moduleName, type.name);
        if (!typeSpec)
            return undefined;
        const own = typeSpec.properties.get(propertyName);
        if (own)
            return own;
        if (!typeSpec.baseType)
            return undefined;
        const key = qualifiedTypeKey(typeSpec.baseType);
        if (seen.has(key))
            return undefined;
        seen.add(key);
        return this.findTypeProperty(typeSpec.baseType, propertyName, seen);
    }
    findTypeMethod(type, methodName, seen) {
        const typeSpec = this.getQualifiedType(type.moduleName, type.name);
        if (!typeSpec)
            return undefined;
        const own = typeSpec.methods.get(methodName);
        if (own)
            return own;
        if (!typeSpec.baseType)
            return undefined;
        const key = qualifiedTypeKey(typeSpec.baseType);
        if (seen.has(key))
            return undefined;
        seen.add(key);
        return this.findTypeMethod(typeSpec.baseType, methodName, seen);
    }
    qualifiedTypeExtends(child, parent, seen) {
        const typeSpec = this.getQualifiedType(child.moduleName, child.name);
        if (!typeSpec?.baseType)
            return false;
        const base = typeSpec.baseType;
        if (base.moduleName === parent.moduleName && base.name === parent.name)
            return true;
        const key = qualifiedTypeKey(base);
        if (seen.has(key))
            return false;
        seen.add(key);
        return this.qualifiedTypeExtends(base, parent, seen);
    }
    collectTypeMembers(type, members, seen) {
        const key = qualifiedTypeKey(type);
        if (seen.has(key))
            return;
        seen.add(key);
        const typeSpec = this.getQualifiedType(type.moduleName, type.name);
        if (!typeSpec)
            return;
        if (typeSpec.baseType) {
            this.collectTypeMembers(typeSpec.baseType, members, seen);
        }
        for (const property of typeSpec.properties.values()) {
            members.set(property.name, {
                name: property.name,
                kind: 'property',
                detail: `${property.name}: ${(0, types_1.typeToString)(property.type)}`,
            });
        }
        for (const method of typeSpec.methods.values()) {
            members.set(method.name, {
                name: method.name,
                kind: 'method',
                detail: signatureDetail(method),
            });
        }
    }
}
exports.StandardLibraryRegistry = StandardLibraryRegistry;
function createDefaultStandardLibrary() {
    const registry = new StandardLibraryRegistry();
    const guiWidget = (0, types_1.qualified)('gui', 'Widget');
    const guiCanvas = (0, types_1.qualified)('gui', 'Canvas');
    const guiLabel = (0, types_1.qualified)('gui', 'Label');
    const guiButton = (0, types_1.qualified)('gui', 'Button');
    const guiFrame = (0, types_1.qualified)('gui', 'Frame');
    const guiLineEdit = (0, types_1.qualified)('gui', 'LineEdit');
    const guiTextEdit = (0, types_1.qualified)('gui', 'TextEdit');
    const guiProgressBar = (0, types_1.qualified)('gui', 'ProgressBar');
    const guiSpinBox = (0, types_1.qualified)('gui', 'SpinBox');
    const guiFloatSpinBox = (0, types_1.qualified)('gui', 'FloatSpinBox');
    const guiSlider = (0, types_1.qualified)('gui', 'Slider');
    const guiCheckBox = (0, types_1.qualified)('gui', 'CheckBox');
    const guiRadioButton = (0, types_1.qualified)('gui', 'RadioButton');
    const guiComboBox = (0, types_1.qualified)('gui', 'ComboBox');
    const guiModal = (0, types_1.qualified)('gui', 'Modal');
    const guiTimer = (0, types_1.qualified)('gui', 'Timer');
    const guiKeyboardEvent = (0, types_1.qualified)('gui', 'KeyboardEvent');
    const guiMouseEvent = (0, types_1.qualified)('gui', 'MouseEvent');
    const guiMouseScrollEvent = (0, types_1.qualified)('gui', 'MouseScrollEvent');
    const guiChildParameter = {
        name: 'child',
        type: guiWidget,
        acceptedTypes: [guiWidget],
        acceptedDescription: 'gui widget',
    };
    const drawableMoveMethod = functionSpec('move', [
        { name: 'dx', type: types_1.FLOAT },
        { name: 'dy', type: types_1.FLOAT },
    ], types_1.VOID);
    const positioned = [
        propertySpec('x', types_1.INT),
        propertySpec('y', types_1.INT),
        propertySpec('width', types_1.INT),
        propertySpec('height', types_1.INT),
    ];
    const visible = [
        propertySpec('visible', types_1.BOOL),
    ];
    const changeable = [
        propertySpec('on_change', types_1.ANY_TYPE),
    ];
    const inheritableColorRoles = [
        propertySpec('text_color', types_1.COLOR),
        propertySpec('background_color', types_1.COLOR),
    ];
    const colorRoles = [
        ...inheritableColorRoles,
        propertySpec('border_color', types_1.COLOR),
    ];
    const buttonClickable = [
        callbackPropertySpec('on_click', [
            callbackSpec([]),
            callbackSpec([guiButton]),
        ]),
    ];
    const drawableDrawable = (0, types_1.qualified)('drawable', 'Drawable');
    const timeStamp = (0, types_1.qualified)('time', 'stamp');
    const fileIStream = (0, types_1.qualified)('file', 'istream');
    const fileOStream = (0, types_1.qualified)('file', 'ostream');
    const jsonValue = (0, types_1.qualified)('json', 'Value');
    const jsonObject = (0, types_1.qualified)('json', 'Object');
    const jsonArray = (0, types_1.qualified)('json', 'Array');
    const audioMusic = (0, types_1.qualified)('audio', 'Music');
    registry.registerModule(moduleSpec('console', [
        functionSpec('write', [], types_1.VOID, {
            variadic: true,
            variadicTypes: [types_1.ANY_TYPE],
            documentation: 'Writes values exactly as passed, without automatic spaces or line breaks.',
        }),
        functionSpec('writeln', [], types_1.VOID, {
            variadic: true,
            variadicTypes: [types_1.ANY_TYPE],
            documentation: 'Writes values and then writes a line break.',
        }),
        functionSpec('clear', [], types_1.VOID, {
            documentation: 'Clears the console output.',
        }),
        functionSpec('get_int', [], types_1.INT),
        functionSpec('get_float', [], types_1.FLOAT),
        functionSpec('get_string', [], types_1.STRING),
        functionSpec('set_precision', [{ name: 'digits', type: types_1.INT }], types_1.VOID),
    ]));
    registry.registerModule(moduleSpec('math', [
        functionSpec('abs', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('sqrt', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('round', [{ name: 'value', type: types_1.FLOAT }], types_1.INT),
        functionSpec('floor', [{ name: 'value', type: types_1.FLOAT }], types_1.INT),
        functionSpec('ceil', [{ name: 'value', type: types_1.FLOAT }], types_1.INT),
        functionSpec('pow', [{ name: 'value', type: types_1.FLOAT }, { name: 'power', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('clamp', [
            { name: 'min', type: types_1.FLOAT },
            { name: 'value', type: types_1.FLOAT },
            { name: 'max', type: types_1.FLOAT },
        ], types_1.FLOAT),
        functionSpec('sin', [{ name: 'radians', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('cos', [{ name: 'radians', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('tan', [{ name: 'radians', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('asin', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('acos', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('log', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('log10', [{ name: 'value', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('to_radians', [{ name: 'degrees', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('to_degrees', [{ name: 'radians', type: types_1.FLOAT }], types_1.FLOAT),
    ], [
        { name: 'pi', type: types_1.FLOAT, documentation: 'Pi constant.' },
        { name: 'e', type: types_1.FLOAT, documentation: 'Euler number.' },
    ]));
    registry.registerModule(moduleSpec('random', [
        functionSpec('create_int', [{ name: 'min', type: types_1.INT }, { name: 'max', type: types_1.INT }], types_1.INT),
        functionSpec('create_float', [{ name: 'min', type: types_1.FLOAT }, { name: 'max', type: types_1.FLOAT }], types_1.FLOAT),
        functionSpec('set_seed', [{ name: 'seed', type: types_1.INT }], types_1.VOID),
    ]));
    registry.registerModule(moduleSpec('time', [
        functionSpec('sleep', [{ name: 'seconds', type: types_1.FLOAT }], types_1.VOID),
        functionSpec('now', [], timeStamp),
        functionSpec('from_unix', [{ name: 'seconds', type: types_1.INT }], timeStamp),
    ], [], [
        typeSpec('stamp', [], [
            functionSpec('year', [], types_1.INT),
            functionSpec('month', [], types_1.INT),
            functionSpec('day', [], types_1.INT),
            functionSpec('hour', [], types_1.INT),
            functionSpec('minute', [], types_1.INT),
            functionSpec('second', [], types_1.INT),
            functionSpec('week_day', [], types_1.INT),
            functionSpec('unix', [], types_1.INT),
            functionSpec('to_string', [], types_1.STRING),
        ]),
    ]));
    registry.registerModule(moduleSpec('file', [
        functionSpec('exists', [{ name: 'path', type: types_1.STRING }], types_1.BOOL),
        functionSpec('open', [
            { name: 'path', type: types_1.STRING },
            { name: 'mode', type: types_1.STRING },
        ], types_1.ANY_TYPE),
    ], [], [
        typeSpec('istream', [], [
            functionSpec('read_line', [], types_1.STRING),
            functionSpec('read_all', [], types_1.STRING),
            functionSpec('has_next_line', [], types_1.BOOL),
            functionSpec('close', [], types_1.VOID),
        ]),
        typeSpec('ostream', [], [
            functionSpec('write_line', [], types_1.VOID, {
                variadic: true,
                variadicTypes: [types_1.ANY_TYPE],
            }),
            functionSpec('close', [], types_1.VOID),
        ]),
    ]));
    registry.registerModule(moduleSpec('encoding', [
        functionSpec('list_encodings', [], (0, types_1.arrayType)(types_1.STRING, null, true)),
        functionSpec('char_to_int', [
            { name: 'character', type: types_1.CHAR },
            { name: 'encoding', type: types_1.STRING },
        ], types_1.INT),
        functionSpec('int_to_char', [
            { name: 'code', type: types_1.INT },
            { name: 'encoding', type: types_1.STRING },
        ], types_1.CHAR),
        functionSpec('encode', [
            { name: 'text', type: types_1.STRING },
            { name: 'encoding', type: types_1.STRING },
        ], (0, types_1.arrayType)(types_1.INT, null, true)),
        functionSpec('decode', [
            { name: 'codes', type: (0, types_1.arrayType)(types_1.INT, null, true) },
            { name: 'encoding', type: types_1.STRING },
        ], types_1.STRING),
    ]));
    registry.registerModule(moduleSpec('json', [
        functionSpec('is_valid', [{ name: 'text', type: types_1.STRING }], types_1.BOOL),
        functionSpec('parse', [{ name: 'text', type: types_1.STRING }], jsonValue),
        functionSpec('Value', [{ name: 'value', type: types_1.ANY_TYPE }], jsonValue, {
            minArguments: 0,
            documentation: 'Creates a JSON value. Without arguments creates JSON null.',
        }),
    ], [
        { name: 'NULL', type: jsonValue },
    ], [
        typeSpec('Value', [], [
            functionSpec('is_null', [], types_1.BOOL),
            functionSpec('is_string', [], types_1.BOOL),
            functionSpec('is_int', [], types_1.BOOL),
            functionSpec('is_float', [], types_1.BOOL),
            functionSpec('is_bool', [], types_1.BOOL),
            functionSpec('is_object', [], types_1.BOOL),
            functionSpec('is_array', [], types_1.BOOL),
            functionSpec('to_string', [], types_1.STRING),
            functionSpec('to_int', [], types_1.INT),
            functionSpec('to_float', [], types_1.FLOAT),
            functionSpec('to_bool', [], types_1.BOOL),
            functionSpec('to_object', [], jsonObject),
            functionSpec('to_array', [], jsonArray),
            functionSpec('set_null', [], types_1.VOID),
            functionSpec('set_string', [{ name: 'value', type: types_1.STRING }], types_1.VOID),
            functionSpec('set_int', [{ name: 'value', type: types_1.INT }], types_1.VOID),
            functionSpec('set_float', [{ name: 'value', type: types_1.FLOAT }], types_1.VOID),
            functionSpec('set_bool', [{ name: 'value', type: types_1.BOOL }], types_1.VOID),
            functionSpec('set_object', [{ name: 'value', type: jsonObject }], types_1.VOID),
            functionSpec('set_array', [{ name: 'value', type: jsonArray }], types_1.VOID),
            functionSpec('to_json', [], types_1.STRING),
            functionSpec('to_pretty_json', [{ name: 'indent', type: types_1.INT }], types_1.STRING, {
                minArguments: 0,
            }),
        ]),
        typeSpec('Object', [], [
            functionSpec('length', [], types_1.INT),
            functionSpec('has', [{ name: 'key', type: types_1.STRING }], types_1.BOOL),
            functionSpec('get', [{ name: 'key', type: types_1.STRING }], jsonValue),
            functionSpec('add', [{ name: 'key', type: types_1.STRING }, { name: 'value', type: jsonValue }], types_1.VOID),
            functionSpec('set', [{ name: 'key', type: types_1.STRING }, { name: 'value', type: jsonValue }], types_1.VOID),
            functionSpec('remove', [{ name: 'key', type: types_1.STRING }], types_1.VOID),
            functionSpec('keys', [], (0, types_1.arrayType)(types_1.STRING, null, true)),
        ], jsonValue),
        typeSpec('Array', [], [
            functionSpec('length', [], types_1.INT),
            functionSpec('at', [{ name: 'index', type: types_1.INT }], jsonValue),
            functionSpec('set', [{ name: 'index', type: types_1.INT }, { name: 'value', type: jsonValue }], types_1.VOID),
            functionSpec('add', [{ name: 'value', type: jsonValue }], types_1.VOID),
            functionSpec('insert', [{ name: 'index', type: types_1.INT }, { name: 'value', type: jsonValue }], types_1.VOID),
            functionSpec('pop', [], jsonValue),
            functionSpec('remove', [{ name: 'index', type: types_1.INT }], types_1.VOID),
            functionSpec('clear', [], types_1.VOID),
        ], jsonValue),
    ]));
    registry.registerModule(moduleSpec('audio', [], [], [
        typeSpec('Sound', [
            propertySpec('src', types_1.STRING, true),
            propertySpec('duration', types_1.FLOAT, true),
            propertySpec('volume', types_1.FLOAT),
            propertySpec('is_playing', types_1.BOOL, true),
        ], [
            functionSpec('load_from_file', [{ name: 'path', type: types_1.STRING }], types_1.VOID, {
                documentation: 'Loads an audio file or raises a clear runtime error if loading fails.',
            }),
            functionSpec('play', [], types_1.VOID),
            functionSpec('pause', [], types_1.VOID),
            functionSpec('resume', [], types_1.VOID),
            functionSpec('stop', [], types_1.VOID),
        ]),
        typeSpec('Music', [
            propertySpec('src', types_1.STRING, true),
            propertySpec('duration', types_1.FLOAT, true),
            propertySpec('position', types_1.FLOAT),
            propertySpec('volume', types_1.FLOAT),
            propertySpec('loop', types_1.BOOL),
            propertySpec('is_playing', types_1.BOOL, true),
            callbackPropertySpec('on_finished', [
                callbackSpec([]),
                callbackSpec([audioMusic]),
            ]),
        ], [
            functionSpec('load_from_file', [{ name: 'path', type: types_1.STRING }], types_1.VOID, {
                documentation: 'Loads an audio file or raises a clear runtime error if loading fails.',
            }),
            functionSpec('play', [], types_1.VOID),
            functionSpec('pause', [], types_1.VOID),
            functionSpec('resume', [], types_1.VOID),
            functionSpec('stop', [], types_1.VOID),
        ]),
    ]));
    const drawableTexture = (0, types_1.qualified)('drawable', 'Texture');
    const drawableFont = (0, types_1.qualified)('drawable', 'Font');
    registry.registerModule(moduleSpec('gui', [], [], [
        typeSpec('Window', [
            ...positioned,
            ...inheritableColorRoles,
            propertySpec('title', types_1.STRING),
        ], [
            functionSpec('add_child', [guiChildParameter], types_1.VOID),
            functionSpec('show', [], types_1.VOID),
        ]),
        typeSpec('Widget', [
            ...positioned,
            ...visible,
            ...inheritableColorRoles,
        ]),
        typeSpec('Canvas', [
            ...positioned,
            ...visible,
            propertySpec('framerate_limit', types_1.INT),
            callbackPropertySpec('on_init', [callbackSpec([guiCanvas])]),
            callbackPropertySpec('on_key_pressed', [callbackSpec([guiCanvas, guiKeyboardEvent])]),
            callbackPropertySpec('on_key_released', [callbackSpec([guiCanvas, guiKeyboardEvent])]),
            callbackPropertySpec('on_mouse_pressed', [callbackSpec([guiCanvas, guiMouseEvent])]),
            callbackPropertySpec('on_mouse_released', [callbackSpec([guiCanvas, guiMouseEvent])]),
            callbackPropertySpec('on_mouse_move', [callbackSpec([guiCanvas, guiMouseEvent])]),
            callbackPropertySpec('on_mouse_scroll', [callbackSpec([guiCanvas, guiMouseScrollEvent])]),
            callbackPropertySpec('on_update', [callbackSpec([guiCanvas, types_1.FLOAT])]),
        ], [
            functionSpec('clear', [], types_1.VOID),
            functionSpec('fill', [{ name: 'color', type: types_1.COLOR }], types_1.VOID),
            functionSpec('draw', [{
                    name: 'object',
                    type: drawableDrawable,
                    acceptedTypes: [drawableDrawable],
                    acceptedDescription: 'drawable object',
                }], types_1.VOID),
        ], guiWidget),
        typeSpec('Label', [
            ...positioned,
            ...visible,
            ...colorRoles,
            callbackPropertySpec('on_click', [
                callbackSpec([]),
                callbackSpec([guiLabel]),
            ]),
            propertySpec('text', types_1.STRING),
            propertySpec('font_size', types_1.INT),
            propertySpec('color', types_1.STRING, false, 'Legacy text color shortcut. Prefer text_color with colors.Color.'),
        ], [], guiWidget),
        typeSpec('Button', [
            ...positioned,
            ...visible,
            ...colorRoles,
            ...buttonClickable,
            propertySpec('text', types_1.STRING),
        ], [], guiWidget),
        typeSpec('Frame', [
            ...positioned,
            ...visible,
            propertySpec('background_color', types_1.COLOR),
            propertySpec('border_color', types_1.COLOR),
            propertySpec('border_width', types_1.INT),
            propertySpec('title', types_1.STRING),
        ], [
            functionSpec('add_child', [guiChildParameter], types_1.VOID),
        ], guiWidget),
        typeSpec('Image', [
            ...positioned,
            ...visible,
            propertySpec('resize_mode', types_1.STRING),
        ], [
            functionSpec('load_from_file', [{ name: 'path', type: types_1.STRING }], types_1.VOID, {
                documentation: 'Loads an image file into the widget or raises a clear runtime error if loading fails.',
            }),
        ], guiWidget),
        typeSpec('LineEdit', [
            ...positioned,
            ...visible,
            ...changeable,
            ...colorRoles,
            propertySpec('text', types_1.STRING),
            propertySpec('placeholder', types_1.STRING),
            propertySpec('font_size', types_1.INT),
            propertySpec('echo_mode', types_1.STRING),
        ], [], guiWidget),
        typeSpec('TextEdit', [
            ...positioned,
            ...visible,
            ...colorRoles,
            propertySpec('text', types_1.STRING),
            propertySpec('placeholder', types_1.STRING),
        ], [], guiWidget),
        typeSpec('ProgressBar', [
            ...positioned,
            ...visible,
            propertySpec('value', types_1.INT),
            propertySpec('min', types_1.INT),
            propertySpec('max', types_1.INT),
            propertySpec('text_color', types_1.COLOR),
            propertySpec('background_color', types_1.COLOR),
            propertySpec('foreground_color', types_1.COLOR),
            propertySpec('fill_color', types_1.COLOR),
            propertySpec('border_color', types_1.COLOR),
        ], [], guiWidget),
        typeSpec('SpinBox', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('value', types_1.INT),
            propertySpec('min', types_1.INT),
            propertySpec('max', types_1.INT),
            propertySpec('step', types_1.INT),
        ], [], guiWidget),
        typeSpec('FloatSpinBox', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('value', types_1.FLOAT),
            propertySpec('min', types_1.FLOAT),
            propertySpec('max', types_1.FLOAT),
            propertySpec('step', types_1.FLOAT),
        ], [], guiWidget),
        typeSpec('Slider', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('value', types_1.INT),
            propertySpec('min', types_1.INT),
            propertySpec('max', types_1.INT),
            propertySpec('step', types_1.INT),
        ], [], guiWidget),
        typeSpec('CheckBox', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('text', types_1.STRING),
            propertySpec('is_checked', types_1.BOOL),
        ], [], guiWidget),
        typeSpec('RadioButton', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('text', types_1.STRING),
            propertySpec('is_selected', types_1.BOOL),
            propertySpec('group', types_1.STRING),
        ], [], guiWidget),
        typeSpec('ComboBox', [
            ...positioned,
            ...visible,
            ...changeable,
            propertySpec('selected_index', types_1.INT),
            propertySpec('selected_text', types_1.STRING),
        ], [
            functionSpec('add_item', [{ name: 'text', type: types_1.STRING }], types_1.VOID),
            functionSpec('clear_items', [], types_1.VOID),
        ], guiWidget),
        typeSpec('Modal', [
            propertySpec('title', types_1.STRING),
            propertySpec('message', types_1.STRING),
            propertySpec('confirm_text', types_1.STRING),
            propertySpec('cancel_text', types_1.STRING),
            callbackPropertySpec('on_confirm', [
                callbackSpec([]),
                callbackSpec([guiModal]),
            ]),
            callbackPropertySpec('on_cancel', [
                callbackSpec([]),
                callbackSpec([guiModal]),
            ]),
        ], [
            functionSpec('show_alert', [], types_1.VOID),
            functionSpec('show_confirm', [], types_1.VOID),
            functionSpec('show_input', [], types_1.VOID),
            functionSpec('get_input_value', [], types_1.STRING),
        ]),
        typeSpec('Timer', [
            propertySpec('interval', types_1.INT),
            callbackPropertySpec('on_tick', [
                callbackSpec([]),
                callbackSpec([guiTimer]),
            ]),
        ], [
            functionSpec('start', [], types_1.VOID),
            functionSpec('stop', [], types_1.VOID),
        ]),
        typeSpec('KeyboardEvent', [
            propertySpec('key', types_1.STRING, true),
        ]),
        typeSpec('MouseEvent', [
            propertySpec('x', types_1.INT, true),
            propertySpec('y', types_1.INT, true),
            propertySpec('mouse_button', types_1.STRING, true),
        ]),
        typeSpec('MouseScrollEvent', [
            propertySpec('x', types_1.INT, true),
            propertySpec('y', types_1.INT, true),
            propertySpec('delta', types_1.INT, true),
        ]),
    ]));
    registry.registerModule(moduleSpec('drawable', [], [], [
        typeSpec('Drawable'),
        typeSpec('Rectangle', [
            propertySpec('x', types_1.INT),
            propertySpec('y', types_1.INT),
            propertySpec('width', types_1.INT),
            propertySpec('height', types_1.INT),
            propertySpec('rotation', types_1.FLOAT),
            propertySpec('fill_color', types_1.COLOR),
            propertySpec('border_width', types_1.INT),
            propertySpec('border_color', types_1.COLOR),
        ], [
            drawableMoveMethod,
            functionSpec('rotate', [{ name: 'angle', type: types_1.FLOAT }], types_1.VOID),
        ], drawableDrawable),
        typeSpec('Circle', [
            propertySpec('x', types_1.INT),
            propertySpec('y', types_1.INT),
            propertySpec('radius', types_1.INT),
            propertySpec('rotation', types_1.FLOAT),
            propertySpec('fill_color', types_1.COLOR),
            propertySpec('border_width', types_1.INT),
            propertySpec('border_color', types_1.COLOR),
        ], [
            drawableMoveMethod,
            functionSpec('rotate', [{ name: 'angle', type: types_1.FLOAT }], types_1.VOID),
        ], drawableDrawable),
        typeSpec('Line', [
            propertySpec('x1', types_1.INT),
            propertySpec('y1', types_1.INT),
            propertySpec('x2', types_1.INT),
            propertySpec('y2', types_1.INT),
            propertySpec('color', types_1.COLOR),
            propertySpec('thickness', types_1.INT),
        ], [
            drawableMoveMethod,
        ], drawableDrawable),
        typeSpec('Sprite', [
            propertySpec('texture', drawableTexture),
            propertySpec('x', types_1.INT),
            propertySpec('y', types_1.INT),
        ], [
            functionSpec('set_scale', [{ name: 'x', type: types_1.FLOAT }, { name: 'y', type: types_1.FLOAT }], types_1.VOID),
            drawableMoveMethod,
        ], drawableDrawable),
        typeSpec('Texture', [], [
            functionSpec('load_from_file', [{ name: 'path', type: types_1.STRING }], types_1.VOID, {
                documentation: 'Loads an image file or raises a clear runtime error if loading fails.',
            }),
        ]),
        typeSpec('Font', [], [
            functionSpec('load_from_file', [{ name: 'path', type: types_1.STRING }], types_1.VOID, {
                documentation: 'Loads a font file or raises a clear runtime error if loading fails.',
            }),
        ]),
        typeSpec('Text', [
            propertySpec('font', drawableFont),
            propertySpec('text', types_1.STRING),
            propertySpec('x', types_1.INT),
            propertySpec('y', types_1.INT),
            propertySpec('font_size', types_1.INT),
            propertySpec('text_color', types_1.COLOR),
        ], [
            drawableMoveMethod,
        ], drawableDrawable),
    ]));
    registry.registerModule(moduleSpec('colors', [
        functionSpec('RGB', [
            { name: 'red', type: types_1.INT },
            { name: 'green', type: types_1.INT },
            { name: 'blue', type: types_1.INT },
        ], types_1.COLOR, {
            documentation: 'Creates an opaque color from red, green, and blue channels in the 0..255 range.',
        }),
        functionSpec('RGBA', [
            { name: 'red', type: types_1.INT },
            { name: 'green', type: types_1.INT },
            { name: 'blue', type: types_1.INT },
            { name: 'alpha', type: types_1.FLOAT },
        ], types_1.COLOR, {
            documentation: 'Creates a color from red, green, blue, and alpha channels. Alpha is in the 0.0..1.0 range.',
        }),
        functionSpec('HEX', [
            { name: 'value', type: types_1.STRING },
        ], types_1.COLOR, {
            documentation: 'Creates a color from a CSS-style #RRGGBB or #RRGGBBAA string.',
        }),
        functionSpec('HSL', [
            { name: 'hue', type: types_1.INT },
            { name: 'saturation', type: types_1.INT },
            { name: 'lightness', type: types_1.INT },
        ], types_1.COLOR, {
            documentation: 'Creates a color from hue, saturation, and lightness. Saturation and lightness are percentages.',
        }),
    ], [
        { name: 'BLACK', type: types_1.COLOR },
        { name: 'WHITE', type: types_1.COLOR },
        { name: 'RED', type: types_1.COLOR },
        { name: 'GREEN', type: types_1.COLOR },
        { name: 'BLUE', type: types_1.COLOR },
        { name: 'TRANSPARENT', type: types_1.COLOR },
    ], [typeSpec('Color')]));
    const typesNumericMethods = [
        functionSpec('to_bin', [], types_1.STRING),
        functionSpec('to_hex', [], types_1.STRING),
    ];
    registry.registerModule(moduleSpec('types', [
        functionSpec('from_bin', [
            { name: 'bits', type: types_1.STRING },
            { name: 'type_name', type: types_1.STRING },
        ], types_1.ANY_TYPE),
        functionSpec('from_hex', [
            { name: 'hex', type: types_1.STRING },
            { name: 'type_name', type: types_1.STRING },
        ], types_1.ANY_TYPE),
    ], [], [
        typeSpec('int8', [], typesNumericMethods),
        typeSpec('uint8', [], typesNumericMethods),
        typeSpec('int16', [], typesNumericMethods),
        typeSpec('uint16', [], typesNumericMethods),
        typeSpec('int32', [], typesNumericMethods),
        typeSpec('uint32', [], typesNumericMethods),
        typeSpec('float32', [], typesNumericMethods),
        typeSpec('float64', [], typesNumericMethods),
    ]));
    registry.registerGlobalFunction(functionSpec('div', [
        { name: 'left', type: types_1.INT },
        { name: 'right', type: types_1.INT },
    ], types_1.INT));
    registry.registerGlobalFunction(functionSpec('mod', [
        { name: 'left', type: types_1.INT },
        { name: 'right', type: types_1.INT },
    ], types_1.INT));
    registry.registerGlobalFunction(functionSpec('to_int', [
        {
            name: 'value',
            type: types_1.ANY_TYPE,
            acceptedTypes: [types_1.STRING, types_1.FLOAT, types_1.INT],
            acceptedDescription: 'string or numeric value',
        },
    ], types_1.INT));
    registry.registerGlobalFunction(functionSpec('to_float', [
        {
            name: 'value',
            type: types_1.ANY_TYPE,
            acceptedTypes: [types_1.STRING, types_1.FLOAT, types_1.INT],
            acceptedDescription: 'string or numeric value',
        },
    ], types_1.FLOAT));
    registry.registerGlobalFunction(functionSpec('to_string', [
        { name: 'value', type: types_1.ANY_TYPE },
    ], types_1.STRING));
    return registry;
}
function moduleSpec(name, functions, constants = [], types = []) {
    return {
        name,
        functions: new Map(functions.map((fn) => [fn.name, fn])),
        constants: new Map(constants.map((constant) => [constant.name, constant])),
        types: new Map(types.map((type) => [type.name, type])),
    };
}
function functionSpec(name, parameters, returnType, extras = {}) {
    return {
        name,
        parameters,
        returnType,
        ...extras,
    };
}
function signatureDetail(fn) {
    const minArguments = fn.minArguments ?? fn.parameters.length;
    const params = fn.parameters.map((param, index) => {
        const text = `${param.name}: ${typeName(param.type)}`;
        return index >= minArguments ? `[${text}]` : text;
    });
    const suffix = fn.variadic ? (params.length > 0 ? ', ...values' : '...values') : '';
    return `${fn.name}(${params.join(', ')}${suffix}): ${typeName(fn.returnType)}`;
}
function typeName(type) {
    return (0, types_1.typeToString)(type);
}
function propertySpec(name, type, readonly = false, documentation, callbacks) {
    return { name, type, readonly, documentation, callbacks };
}
function callbackPropertySpec(name, callbacks, documentation) {
    return propertySpec(name, types_1.ANY_TYPE, false, documentation, callbacks);
}
function callbackSpec(parameters, returnType = types_1.VOID) {
    return { parameters, returnType };
}
function typeSpec(name, properties = [], methods = [], baseType) {
    return {
        name,
        baseType,
        properties: new Map(properties.map((property) => [property.name, property])),
        methods: new Map(methods.map((method) => [method.name, method])),
    };
}
function qualifiedTypeKey(type) {
    return `${type.moduleName}.${type.name}`;
}
//# sourceMappingURL=registry.js.map
},
"src/core/tokens.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.KEYWORDS = exports.TokenKind = void 0;
exports.tokenDisplay = tokenDisplay;
var TokenKind;
(function (TokenKind) {
    TokenKind["IntLiteral"] = "IntLiteral";
    TokenKind["FloatLiteral"] = "FloatLiteral";
    TokenKind["StringLiteral"] = "StringLiteral";
    TokenKind["CharLiteral"] = "CharLiteral";
    TokenKind["Identifier"] = "Identifier";
    TokenKind["KwUse"] = "KwUse";
    TokenKind["KwMain"] = "KwMain";
    TokenKind["KwFunction"] = "KwFunction";
    TokenKind["KwInt"] = "KwInt";
    TokenKind["KwFloat"] = "KwFloat";
    TokenKind["KwString"] = "KwString";
    TokenKind["KwChar"] = "KwChar";
    TokenKind["KwBool"] = "KwBool";
    TokenKind["KwVoid"] = "KwVoid";
    TokenKind["KwIf"] = "KwIf";
    TokenKind["KwElse"] = "KwElse";
    TokenKind["KwWhile"] = "KwWhile";
    TokenKind["KwDo"] = "KwDo";
    TokenKind["KwFor"] = "KwFor";
    TokenKind["KwBreak"] = "KwBreak";
    TokenKind["KwContinue"] = "KwContinue";
    TokenKind["KwReturn"] = "KwReturn";
    TokenKind["KwAnd"] = "KwAnd";
    TokenKind["KwOr"] = "KwOr";
    TokenKind["KwNot"] = "KwNot";
    TokenKind["KwTrue"] = "KwTrue";
    TokenKind["KwFalse"] = "KwFalse";
    TokenKind["KwDiv"] = "KwDiv";
    TokenKind["KwMod"] = "KwMod";
    TokenKind["KwArray"] = "KwArray";
    TokenKind["KwDynArray"] = "KwDynArray";
    TokenKind["KwClass"] = "KwClass";
    TokenKind["KwConstructor"] = "KwConstructor";
    TokenKind["KwThis"] = "KwThis";
    TokenKind["KwStatic"] = "KwStatic";
    TokenKind["KwExtends"] = "KwExtends";
    TokenKind["KwPrivate"] = "KwPrivate";
    TokenKind["KwPublic"] = "KwPublic";
    TokenKind["KwDestructor"] = "KwDestructor";
    TokenKind["LeftParen"] = "LeftParen";
    TokenKind["RightParen"] = "RightParen";
    TokenKind["LeftBrace"] = "LeftBrace";
    TokenKind["RightBrace"] = "RightBrace";
    TokenKind["LeftBracket"] = "LeftBracket";
    TokenKind["RightBracket"] = "RightBracket";
    TokenKind["Comma"] = "Comma";
    TokenKind["Semicolon"] = "Semicolon";
    TokenKind["Dot"] = "Dot";
    TokenKind["Colon"] = "Colon";
    TokenKind["Tilde"] = "Tilde";
    TokenKind["Plus"] = "Plus";
    TokenKind["Minus"] = "Minus";
    TokenKind["Star"] = "Star";
    TokenKind["Slash"] = "Slash";
    TokenKind["Equal"] = "Equal";
    TokenKind["PlusEqual"] = "PlusEqual";
    TokenKind["MinusEqual"] = "MinusEqual";
    TokenKind["StarEqual"] = "StarEqual";
    TokenKind["SlashEqual"] = "SlashEqual";
    TokenKind["EqualEqual"] = "EqualEqual";
    TokenKind["BangEqual"] = "BangEqual";
    TokenKind["Less"] = "Less";
    TokenKind["LessEqual"] = "LessEqual";
    TokenKind["Greater"] = "Greater";
    TokenKind["GreaterEqual"] = "GreaterEqual";
    TokenKind["EndOfFile"] = "EndOfFile";
    TokenKind["Bad"] = "Bad";
})(TokenKind || (exports.TokenKind = TokenKind = {}));
exports.KEYWORDS = {
    use: TokenKind.KwUse,
    main: TokenKind.KwMain,
    function: TokenKind.KwFunction,
    int: TokenKind.KwInt,
    float: TokenKind.KwFloat,
    string: TokenKind.KwString,
    char: TokenKind.KwChar,
    bool: TokenKind.KwBool,
    void: TokenKind.KwVoid,
    if: TokenKind.KwIf,
    else: TokenKind.KwElse,
    while: TokenKind.KwWhile,
    do: TokenKind.KwDo,
    for: TokenKind.KwFor,
    break: TokenKind.KwBreak,
    continue: TokenKind.KwContinue,
    return: TokenKind.KwReturn,
    and: TokenKind.KwAnd,
    or: TokenKind.KwOr,
    not: TokenKind.KwNot,
    true: TokenKind.KwTrue,
    false: TokenKind.KwFalse,
    div: TokenKind.KwDiv,
    mod: TokenKind.KwMod,
    array: TokenKind.KwArray,
    dyn_array: TokenKind.KwDynArray,
    class: TokenKind.KwClass,
    constructor: TokenKind.KwConstructor,
    this: TokenKind.KwThis,
    static: TokenKind.KwStatic,
    extends: TokenKind.KwExtends,
    private: TokenKind.KwPrivate,
    public: TokenKind.KwPublic,
    destructor: TokenKind.KwDestructor,
};
function tokenDisplay(kind) {
    switch (kind) {
        case TokenKind.IntLiteral:
            return 'integer';
        case TokenKind.FloatLiteral:
            return 'float';
        case TokenKind.StringLiteral:
            return 'string';
        case TokenKind.CharLiteral:
            return 'char';
        case TokenKind.Identifier:
            return 'identifier';
        case TokenKind.EndOfFile:
            return 'end of file';
        default:
            return `'${kind}'`;
    }
}
//# sourceMappingURL=tokens.js.map
},
"src/core/types.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ANY_VALUE_TYPES = exports.COLOR = exports.ANY_TYPE = exports.ERROR_TYPE = exports.VOID = exports.BOOL = exports.CHAR = exports.STRING = exports.FLOAT = exports.INT = void 0;
exports.primitive = primitive;
exports.typeToString = typeToString;
exports.sameType = sameType;
exports.isNumeric = isNumeric;
exports.isIntegerLike = isIntegerLike;
exports.isFloatLike = isFloatLike;
exports.isTypesNumeric = isTypesNumeric;
exports.isAssignable = isAssignable;
exports.numericBinaryResult = numericBinaryResult;
exports.qualified = qualified;
exports.classType = classType;
exports.arrayType = arrayType;
exports.functionType = functionType;
exports.INT = { kind: 'primitive', name: 'int' };
exports.FLOAT = { kind: 'primitive', name: 'float' };
exports.STRING = { kind: 'primitive', name: 'string' };
exports.CHAR = { kind: 'primitive', name: 'char' };
exports.BOOL = { kind: 'primitive', name: 'bool' };
exports.VOID = { kind: 'primitive', name: 'void' };
exports.ERROR_TYPE = { kind: 'error' };
exports.ANY_TYPE = { kind: 'any' };
exports.COLOR = { kind: 'qualified', moduleName: 'colors', name: 'Color' };
exports.ANY_VALUE_TYPES = [exports.INT, exports.FLOAT, exports.STRING, exports.CHAR, exports.BOOL, exports.COLOR];
function primitive(name) {
    switch (name) {
        case 'int':
            return exports.INT;
        case 'float':
            return exports.FLOAT;
        case 'string':
            return exports.STRING;
        case 'char':
            return exports.CHAR;
        case 'bool':
            return exports.BOOL;
        case 'void':
            return exports.VOID;
    }
}
function typeToString(type) {
    if (type.kind === 'error')
        return '<error>';
    if (type.kind === 'any')
        return 'any';
    if (type.kind === 'function') {
        return `function(${type.parameters.map(typeToString).join(', ')}): ${typeToString(type.returnType)}`;
    }
    if (type.kind === 'array') {
        if (type.dynamic)
            return `dyn_array<${typeToString(type.elementType)}>`;
        return `array<${typeToString(type.elementType)}, ${type.size ?? '?'}>`;
    }
    if (type.kind === 'class')
        return type.name;
    if (type.kind === 'qualified')
        return `${type.moduleName}.${type.name}`;
    return type.name;
}
function sameType(left, right) {
    if (left.kind === 'error' || right.kind === 'error')
        return true;
    if (left.kind === 'any' || right.kind === 'any')
        return true;
    if (left.kind !== right.kind)
        return false;
    if (left.kind === 'function' && right.kind === 'function') {
        if (left.parameters.length !== right.parameters.length)
            return false;
        return left.parameters.every((param, index) => sameType(param, right.parameters[index]))
            && sameType(left.returnType, right.returnType);
    }
    if (left.kind === 'qualified' && right.kind === 'qualified') {
        return left.moduleName === right.moduleName && left.name === right.name;
    }
    if (left.kind === 'class' && right.kind === 'class') {
        return left.name === right.name;
    }
    if (left.kind === 'array' && right.kind === 'array') {
        return left.dynamic === right.dynamic
            && left.size === right.size
            && sameType(left.elementType, right.elementType);
    }
    return left.kind === 'primitive' && right.kind === 'primitive' && left.name === right.name;
}
function isNumeric(type) {
    return isIntegerLike(type) || isFloatLike(type);
}
function isIntegerLike(type) {
    if (type.kind === 'primitive')
        return type.name === 'int';
    return type.kind === 'qualified'
        && type.moduleName === 'types'
        && ['int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32'].includes(type.name);
}
function isFloatLike(type) {
    if (type.kind === 'primitive')
        return type.name === 'float';
    return type.kind === 'qualified'
        && type.moduleName === 'types'
        && (type.name === 'float32' || type.name === 'float64');
}
function isTypesNumeric(type) {
    return type.kind === 'qualified'
        && type.moduleName === 'types'
        && ['int8', 'uint8', 'int16', 'uint16', 'int32', 'uint32', 'float32', 'float64'].includes(type.name);
}
function isAssignable(target, value) {
    if (target.kind === 'error' || value.kind === 'error')
        return true;
    if (target.kind === 'any' || value.kind === 'any')
        return true;
    if (sameType(target, value))
        return true;
    if (isIntegerLike(target))
        return isIntegerLike(value);
    if (isFloatLike(target))
        return isNumeric(value);
    if (target.kind === 'array' && value.kind === 'array') {
        const sizeMatches = target.dynamic || (!value.dynamic && target.size === value.size);
        return sizeMatches && isAssignable(target.elementType, value.elementType);
    }
    return false;
}
function numericBinaryResult(operator, left, right) {
    if (!isNumeric(left) || !isNumeric(right))
        return exports.ERROR_TYPE;
    if (operator === '/')
        return exports.FLOAT;
    return isFloatLike(left) || isFloatLike(right) ? exports.FLOAT : exports.INT;
}
function qualified(moduleName, name) {
    return { kind: 'qualified', moduleName, name };
}
function classType(name) {
    return { kind: 'class', name };
}
function arrayType(elementType, size, dynamic) {
    return { kind: 'array', elementType, size, dynamic };
}
function functionType(parameters, returnType, minArguments) {
    return { kind: 'function', parameters, returnType, minArguments };
}
//# sourceMappingURL=types.js.map
},
"src/language/formatter.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatIdyllium = formatIdyllium;
function formatIdyllium(source, options = {}) {
    const lineEnding = source.includes('\r\n') ? '\r\n' : '\n';
    const normalized = source.replace(/\r\n?/gu, '\n');
    const hadFinalNewline = normalized.endsWith('\n');
    const rawLines = normalized.split('\n');
    const contentLines = hadFinalNewline ? rawLines.slice(0, -1) : rawLines;
    const indentSize = Math.max(1, Math.trunc(options.indentSize ?? 4));
    const indentText = options.insertSpaces === false ? '\t' : ' '.repeat(indentSize);
    const trimTrailingWhitespace = options.trimTrailingWhitespace ?? true;
    const insertFinalNewline = options.insertFinalNewline ?? hadFinalNewline;
    let indent = 0;
    const state = { inBlockComment: false };
    const formatted = [];
    for (const rawLine of contentLines) {
        const line = trimTrailingWhitespace ? rawLine.replace(/[ \t]+$/u, '') : rawLine;
        const trimmed = line.trim();
        if (trimmed.length === 0) {
            formatted.push('');
            continue;
        }
        const leadingClosers = leadingClosingBraces(trimmed);
        const lineIndent = Math.max(0, indent - leadingClosers);
        formatted.push(`${indentText.repeat(lineIndent)}${trimmed}`);
        const scan = braceBalance(trimmed, state.inBlockComment);
        state.inBlockComment = scan.inBlockComment;
        indent = Math.max(0, indent + scan.balance);
    }
    let result = formatted.join(lineEnding);
    if (insertFinalNewline)
        result += lineEnding;
    return result;
}
function leadingClosingBraces(line) {
    let count = 0;
    while (line[count] === '}')
        count++;
    return count;
}
function braceBalance(line, inBlockComment) {
    let balance = 0;
    let quote = null;
    let escaped = false;
    for (let i = 0; i < line.length; i++) {
        const char = line[i];
        const next = line[i + 1];
        if (inBlockComment) {
            if (char === '*' && next === '/') {
                inBlockComment = false;
                i++;
            }
            continue;
        }
        if (quote) {
            if (escaped) {
                escaped = false;
                continue;
            }
            if (char === '\\') {
                escaped = true;
                continue;
            }
            if (char === quote)
                quote = null;
            continue;
        }
        if (char === '/' && next === '/')
            break;
        if (char === '/' && next === '*') {
            inBlockComment = true;
            i++;
            continue;
        }
        if (char === '"' || char === "'") {
            quote = char;
            continue;
        }
        if (char === '{')
            balance++;
        if (char === '}')
            balance--;
    }
    return { balance, inBlockComment };
}
//# sourceMappingURL=formatter.js.map
},
"src/language/project.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.IdylliumProject = void 0;
exports.compileProject = compileProject;
const diagnostics_1 = require("../core/diagnostics");
const project_1 = require("../core/project");
const registry_1 = require("../core/stdlib/registry");
const types_1 = require("../core/types");
const run_1 = require("../runtime/run");
const path = require('path');
class IdylliumProject {
    entryFile;
    files;
    stdlib;
    constructor(options) {
        this.entryFile = normalizeFile(options.entryFile);
        this.files = normalizeFiles(options.files);
        this.stdlib = options.stdlib ?? (0, registry_1.createDefaultStandardLibrary)();
    }
    compile(entryFile = this.entryFile) {
        const file = normalizeFile(entryFile);
        const source = this.files.get(file) ?? '';
        return (0, run_1.compileIdyllium)(source, {
            file,
            stdlib: this.stdlib,
            sources: this.sourcesRecord(),
            resolveModule: this.resolveModule,
        });
    }
    diagnostics(file) {
        const result = this.compile();
        if (!file)
            return result.diagnostics;
        const normalized = normalizeFile(file);
        return result.diagnostics.filter((diagnostic) => normalizeFile(diagnostic.range.start.file) === normalized);
    }
    completions(request) {
        const file = normalizeFile(request.file);
        const source = this.files.get(file) ?? '';
        const prefix = source.slice(0, request.offset);
        const index = this.index(file);
        const argumentCompletions = this.argumentNameCompletions(index, source, request.offset);
        if (argumentCompletions.length > 0)
            return argumentCompletions;
        const memberMatch = /([A-Za-z_][A-Za-z0-9_]*)\.\s*$/.exec(prefix);
        if (memberMatch) {
            const moduleName = memberMatch[1];
            const stdlibMembers = this.stdlib.listModuleMembers(moduleName);
            if (stdlibMembers.length > 0)
                return stdlibMembers;
            const userModuleMembers = this.listUserModuleMembers(index, moduleName);
            if (userModuleMembers.length > 0)
                return userModuleMembers;
            return this.listVariableMembers(index, moduleName);
        }
        if (/\buse\s+[A-Za-z_0-9]*$/.test(prefix)) {
            return [
                ...this.stdlib.listModules(),
                ...this.listProjectModules(),
            ].sort((left, right) => left.name.localeCompare(right.name));
        }
        return [
            ...this.stdlib.listModules(),
            ...this.listProjectModules(),
            { name: 'main', kind: 'function', detail: 'main()' },
            { name: 'int', kind: 'type', detail: 'type int' },
            { name: 'float', kind: 'type', detail: 'type float' },
            { name: 'string', kind: 'type', detail: 'type string' },
            { name: 'char', kind: 'type', detail: 'type char' },
            { name: 'bool', kind: 'type', detail: 'type bool' },
        ];
    }
    hover(request) {
        const file = normalizeFile(request.file);
        const source = this.files.get(file) ?? '';
        const word = wordAtOffset(source, request.offset);
        if (!word)
            return null;
        const index = this.index(file);
        const member = memberContext(source, word.start);
        if (member) {
            const item = this.memberHoverItem(index, member.objectName, word.text);
            if (item)
                return hoverFromWord(source, file, word, item.detail);
        }
        const variable = index.variables.get(word.text);
        if (variable) {
            return hoverFromWord(source, file, word, `${variable.name}: ${typeNameText(variable.typeName)}`);
        }
        if (this.stdlib.hasModule(word.text)) {
            return hoverFromWord(source, file, word, `module ${word.text}`);
        }
        const projectModule = this.listProjectModules().find((item) => item.name === word.text);
        if (projectModule) {
            return hoverFromWord(source, file, word, projectModule.detail);
        }
        const localClass = index.localClasses.get(word.text);
        if (localClass) {
            return hoverFromWord(source, file, word, `class ${word.text}`);
        }
        const primitive = primitiveTypeHover(word.text);
        if (primitive) {
            return hoverFromWord(source, file, word, primitive);
        }
        return null;
    }
    signatureHelp(request) {
        const file = normalizeFile(request.file);
        const source = this.files.get(file) ?? '';
        const call = activeCallAtOffset(source, request.offset);
        if (!call)
            return null;
        const index = this.index(file);
        const signatures = this.signatureCandidates(index, call.calleeText);
        if (signatures.length === 0)
            return null;
        const maxParameter = Math.max(0, signatures[0].parameters.length - 1);
        return {
            signatures,
            activeSignature: 0,
            activeParameter: Math.min(activeParameterForCall(call, signatures[0]), maxParameter),
        };
    }
    definition(request) {
        const file = normalizeFile(request.file);
        const source = this.files.get(file) ?? '';
        const word = wordAtOffset(source, request.offset);
        if (!word)
            return null;
        const index = this.index(file);
        const member = memberContext(source, word.start);
        if (member) {
            return this.memberDefinition(index, member.objectName, word.text);
        }
        const useModule = useModuleNameAtWord(source, word);
        if (useModule) {
            return this.moduleDefinition(file, useModule);
        }
        const variable = index.variables.get(word.text);
        if (variable)
            return { file: normalizeFile(variable.range.start.file), range: variable.range };
        const localFunction = index.functions.get(word.text);
        if (localFunction)
            return { file: normalizeFile(localFunction.range.start.file), range: localFunction.range };
        const localClass = index.localClasses.get(word.text);
        if (localClass)
            return { file: normalizeFile(localClass.range.start.file), range: localClass.range };
        return null;
    }
    documentSymbols(file) {
        const normalized = normalizeFile(file);
        const source = this.files.get(normalized);
        if (source === undefined)
            return [];
        const diagnostics = new diagnostics_1.DiagnosticBag();
        const parsed = (0, project_1.parseSource)(null, normalized, source, diagnostics);
        if (!parsed.ast)
            return [];
        const symbols = [];
        for (const declaration of parsed.ast.declarations) {
            if (declaration.kind === 'ClassDeclaration') {
                symbols.push({ name: declaration.name, kind: 'class', detail: `class ${declaration.name}`, range: declaration.range });
                symbols.push(...this.classMemberSymbols(declaration));
            }
            if (declaration.kind === 'FunctionDeclaration') {
                symbols.push({ name: declaration.name, kind: 'function', detail: functionDetail(declaration.name, declaration.parameters.length), range: declaration.range });
            }
            if (declaration.kind === 'VariableDeclaration') {
                symbols.push({ name: declaration.name, kind: 'variable', detail: `${declaration.name}: ${typeNameText(declaration.declaredType)}`, range: declaration.range });
            }
        }
        if (parsed.ast.main) {
            symbols.push({
                name: 'main',
                kind: 'function',
                detail: callableDetail('main', [], typeNameText(parsed.ast.main.returnType)),
                range: parsed.ast.main.range,
            });
        }
        return symbols;
    }
    index(file) {
        const diagnostics = new diagnostics_1.DiagnosticBag();
        const source = this.files.get(file) ?? '';
        const root = (0, project_1.parseSource)(null, file, source, diagnostics);
        const modules = [];
        if (root.ast) {
            (0, project_1.loadUserModules)(root.ast, file, this.moduleLoadOptions(), this.stdlib, diagnostics, modules);
        }
        const userModules = (0, project_1.buildUserModuleRegistry)(modules, this.stdlib, diagnostics);
        const allDiagnostics = diagnostics.all();
        return {
            diagnostics: allDiagnostics,
            diagnosticsText: (0, diagnostics_1.formatDiagnostics)(allDiagnostics),
            root: root.ast,
            modules,
            userModules,
            variables: root.ast ? collectVariables(root.ast) : new Map(),
            functions: root.ast ? collectFunctions(root.ast) : new Map(),
            localClasses: root.ast ? collectLocalClasses(root.ast) : new Map(),
            userModuleDeclarations: collectUserModuleDeclarations(modules),
        };
    }
    listUserModuleMembers(index, moduleName) {
        const module = index.userModules.getModule(moduleName);
        if (!module)
            return [];
        const classes = [...module.classes.values()].map((item) => ({
            name: item.name,
            kind: 'type',
            detail: `type ${moduleName}.${item.name}`,
        }));
        const functions = [...module.functions.values()].map((item) => ({
            name: item.name,
            kind: 'function',
            detail: signatureDetail(item),
        }));
        return [...classes, ...functions].sort((left, right) => left.name.localeCompare(right.name));
    }
    listProjectModules() {
        const modules = [];
        for (const file of this.files.keys()) {
            if (file === this.entryFile || !file.endsWith('.idyl'))
                continue;
            modules.push({
                name: moduleNameFromFile(file),
                kind: 'module',
                detail: `module ${moduleNameFromFile(file)}`,
            });
        }
        return modules;
    }
    listVariableMembers(index, variableName) {
        const variable = index.variables.get(variableName);
        if (!variable)
            return [];
        if (variable.typeName.kind === 'QualifiedTypeName') {
            if (this.stdlib.hasQualifiedType(variable.typeName.moduleName, variable.typeName.name)) {
                return this.stdlib.listTypeMembers((0, types_1.qualified)(variable.typeName.moduleName, variable.typeName.name));
            }
            const module = index.userModules.getModule(variable.typeName.moduleName);
            const classSpec = module?.classes.get(variable.typeName.name);
            if (classSpec)
                return userClassMemberCompletions(classSpec);
        }
        if (variable.typeName.kind === 'ClassTypeName') {
            const classDeclaration = index.localClasses.get(variable.typeName.name);
            if (classDeclaration)
                return localClassMemberCompletions(classDeclaration);
        }
        if (variable.typeName.kind === 'ArrayTypeName') {
            return arrayMemberCompletions(variable.typeName);
        }
        if (variable.typeName.kind === 'PrimitiveTypeName' && variable.typeName.name === 'string') {
            return stringMemberCompletions();
        }
        return [];
    }
    memberHoverItem(index, objectName, memberName) {
        const stdlibMember = this.stdlib.listModuleMembers(objectName).find((item) => item.name === memberName);
        if (stdlibMember)
            return stdlibMember;
        const userModuleMember = this.listUserModuleMembers(index, objectName).find((item) => item.name === memberName);
        if (userModuleMember)
            return userModuleMember;
        return this.listVariableMembers(index, objectName).find((item) => item.name === memberName) ?? null;
    }
    signatureCandidates(index, calleeText) {
        const normalized = calleeText.replace(/\s+/gu, '');
        const memberMatch = /^([\p{L}_][\p{L}\p{N}_]*)\.([\p{L}_][\p{L}\p{N}_]*)$/u.exec(normalized);
        if (memberMatch) {
            const [, objectName, memberName] = memberMatch;
            const moduleFunction = this.stdlib.getModuleFunction(objectName, memberName);
            if (moduleFunction)
                return [signatureFromFunctionSpec(moduleFunction)];
            const userModuleFunction = index.userModules.getModule(objectName)?.functions.get(memberName);
            if (userModuleFunction)
                return [signatureFromFunctionSpec(userModuleFunction)];
            const member = this.memberHoverItem(index, objectName, memberName);
            return member && member.kind === 'method' ? [signatureFromDetail(member.detail)] : [];
        }
        const globalFunction = this.stdlib.getGlobalFunction(normalized);
        if (globalFunction)
            return [signatureFromFunctionSpec(globalFunction)];
        const localFunction = index.functions.get(normalized);
        if (localFunction)
            return [signatureFromFunctionDeclaration(localFunction)];
        return [];
    }
    argumentNameCompletions(index, source, offset) {
        const call = activeCallAtOffset(source, offset);
        if (!call)
            return [];
        const signature = this.signatureCandidates(index, call.calleeText)[0];
        if (!signature || signature.allowsNamedArguments === false)
            return [];
        const prefix = argumentNamePrefix(call.activeArgumentText);
        if (prefix === null)
            return [];
        const used = usedNamedArgumentNames(call);
        return signature.parameters
            .filter((parameter) => parameter.name && !used.has(parameter.name))
            .filter((parameter) => parameter.name?.toLowerCase().startsWith(prefix.toLowerCase()))
            .map((parameter) => ({
            name: `${parameter.name}=`,
            kind: 'parameter',
            detail: `argument ${parameter.label}`,
        }));
    }
    memberDefinition(index, objectName, memberName) {
        const module = index.userModuleDeclarations.get(objectName);
        if (module) {
            const functionRange = module.functions.get(memberName);
            if (functionRange)
                return { file: normalizeFile(functionRange.start.file), range: functionRange };
            const classDeclaration = module.classes.get(memberName);
            if (classDeclaration)
                return { file: normalizeFile(classDeclaration.range.start.file), range: classDeclaration.range };
        }
        const variable = index.variables.get(objectName);
        if (!variable)
            return null;
        if (variable.typeName.kind === 'ClassTypeName') {
            const classDeclaration = index.localClasses.get(variable.typeName.name);
            const member = classDeclaration ? localClassMemberDeclaration(classDeclaration, memberName) : null;
            if (member)
                return { file: normalizeFile(member.start.file), range: member };
        }
        if (variable.typeName.kind === 'QualifiedTypeName') {
            const declaration = this.userModuleClassMemberDeclaration(index, variable.typeName.moduleName, variable.typeName.name, memberName);
            if (declaration)
                return { file: normalizeFile(declaration.start.file), range: declaration };
        }
        return null;
    }
    userModuleClassMemberDeclaration(index, moduleName, className, memberName) {
        const module = index.userModuleDeclarations.get(moduleName);
        const classDeclaration = module?.classes.get(className);
        return classDeclaration ? localClassMemberDeclaration(classDeclaration, memberName) : null;
    }
    moduleDefinition(fromFile, moduleName) {
        const resolved = this.resolveModule(moduleName, fromFile);
        if (!resolved)
            return null;
        return {
            file: normalizeFile(resolved.file),
            range: {
                start: { file: normalizeFile(resolved.file), line: 1, column: 1 },
                end: { file: normalizeFile(resolved.file), line: 1, column: 1 },
            },
        };
    }
    classMemberSymbols(declaration) {
        const symbols = [];
        for (const member of declaration.members) {
            if (member.kind === 'ClassFieldDeclaration') {
                for (const field of member.fields) {
                    symbols.push({ name: field.name, kind: 'field', detail: `${field.name}: ${typeNameText(member.declaredType)}`, range: field.range });
                }
            }
            if (member.kind === 'ClassMethodDeclaration') {
                symbols.push({ name: member.name, kind: 'method', detail: functionDetail(member.name, member.parameters.length), range: member.range });
            }
            if (member.kind === 'ConstructorDeclaration') {
                symbols.push({ name: member.name, kind: 'constructor', detail: functionDetail(member.name, member.parameters.length), range: member.range });
            }
        }
        return symbols;
    }
    moduleLoadOptions() {
        return {
            sources: this.sourcesRecord(),
            resolveModule: this.resolveModule,
        };
    }
    resolveModule = (moduleName, fromFile) => {
        const candidate = normalizeFile(path.join(path.dirname(fromFile), `${moduleName}.idyl`));
        const source = this.files.get(candidate);
        if (source === undefined)
            return null;
        return { file: candidate, source };
    };
    sourcesRecord() {
        const result = {};
        for (const [file, source] of this.files) {
            result[file] = source;
        }
        return result;
    }
}
exports.IdylliumProject = IdylliumProject;
function compileProject(options) {
    return new IdylliumProject(options).compile();
}
function collectVariables(program) {
    const variables = new Map();
    for (const declaration of program.declarations) {
        if (declaration.kind === 'VariableDeclaration') {
            variables.set(declaration.name, { name: declaration.name, typeName: declaration.declaredType, range: declaration.range });
        }
        if (declaration.kind === 'FunctionDeclaration') {
            for (const parameter of declaration.parameters) {
                variables.set(parameter.name, { name: parameter.name, typeName: parameter.paramType, range: parameter.range });
            }
            collectStatementVariables(declaration.body, variables);
        }
        if (declaration.kind === 'ClassDeclaration') {
            for (const member of declaration.members) {
                if (member.kind === 'ClassMethodDeclaration') {
                    for (const parameter of member.parameters) {
                        variables.set(parameter.name, { name: parameter.name, typeName: parameter.paramType, range: parameter.range });
                    }
                    collectStatementVariables(member.body, variables);
                }
                if (member.kind === 'ConstructorDeclaration') {
                    for (const parameter of member.parameters) {
                        variables.set(parameter.name, { name: parameter.name, typeName: parameter.paramType, range: parameter.range });
                    }
                    collectStatementVariables(member.body, variables);
                }
            }
        }
    }
    if (program.main) {
        collectStatementVariables(program.main.body, variables);
    }
    return variables;
}
function collectFunctions(program) {
    const functions = new Map();
    for (const declaration of program.declarations) {
        if (declaration.kind === 'FunctionDeclaration') {
            functions.set(declaration.name, declaration);
        }
    }
    return functions;
}
function collectStatementVariables(statement, variables) {
    if (statement.kind === 'VariableDeclaration') {
        variables.set(statement.name, { name: statement.name, typeName: statement.declaredType, range: statement.range });
        return;
    }
    if (statement.kind === 'BlockStatement') {
        for (const child of statement.statements)
            collectStatementVariables(child, variables);
        return;
    }
    if (statement.kind === 'IfStatement') {
        collectStatementVariables(statement.thenBranch, variables);
        if (statement.elseBranch)
            collectStatementVariables(statement.elseBranch, variables);
        return;
    }
    if (statement.kind === 'WhileStatement' || statement.kind === 'DoWhileStatement') {
        collectStatementVariables(statement.body, variables);
        return;
    }
    if (statement.kind === 'ForStatement') {
        if (statement.initializer?.kind === 'VariableDeclaration')
            collectStatementVariables(statement.initializer, variables);
        collectStatementVariables(statement.body, variables);
        if (statement.increment?.kind === 'VariableDeclaration')
            collectStatementVariables(statement.increment, variables);
    }
}
function collectLocalClasses(program) {
    const classes = new Map();
    for (const declaration of program.declarations) {
        if (declaration.kind === 'ClassDeclaration')
            classes.set(declaration.name, declaration);
    }
    return classes;
}
function collectUserModuleDeclarations(modules) {
    const result = new Map();
    for (const module of modules) {
        const functions = new Map();
        const classes = new Map();
        for (const declaration of module.ast.declarations) {
            if (declaration.kind === 'FunctionDeclaration') {
                functions.set(declaration.name, declaration.range);
            }
            if (declaration.kind === 'ClassDeclaration') {
                classes.set(declaration.name, declaration);
            }
        }
        result.set(module.name, {
            file: normalizeFile(module.file),
            functions,
            classes,
        });
    }
    return result;
}
function localClassMemberDeclaration(declaration, memberName) {
    for (const member of declaration.members) {
        if (member.kind === 'ClassFieldDeclaration') {
            const field = member.fields.find((item) => item.name === memberName);
            if (field)
                return field.range;
        }
        if (member.kind === 'ClassMethodDeclaration' && member.name === memberName) {
            return member.range;
        }
        if (member.kind === 'ConstructorDeclaration' && member.name === memberName) {
            return member.range;
        }
    }
    return null;
}
function localClassMemberCompletions(declaration) {
    const items = new Map();
    for (const member of declaration.members) {
        if (member.kind === 'ClassFieldDeclaration' && member.access === 'public') {
            for (const field of member.fields) {
                items.set(field.name, {
                    name: field.name,
                    kind: 'property',
                    detail: `${field.name}: ${typeNameText(member.declaredType)}`,
                });
            }
        }
        if (member.kind === 'ClassMethodDeclaration' && member.access === 'public' && !member.isStatic) {
            items.set(member.name, {
                name: member.name,
                kind: 'method',
                detail: callableDetail(member.name, member.parameters.map(parameterDetail), typeNameText(member.returnType)),
            });
        }
    }
    return [...items.values()].sort((left, right) => left.name.localeCompare(right.name));
}
function userClassMemberCompletions(classSpec) {
    const items = new Map();
    for (const field of classSpec.fields) {
        if (field.access !== 'public')
            continue;
        items.set(field.name, {
            name: field.name,
            kind: 'property',
            detail: `${field.name}: ${(0, types_1.typeToString)(field.type)}`,
        });
    }
    for (const method of classSpec.methods) {
        if (method.access !== 'public' || method.isStatic)
            continue;
        items.set(method.name, {
            name: method.name,
            kind: 'method',
            detail: signatureDetail(method.spec),
        });
    }
    return [...items.values()].sort((left, right) => left.name.localeCompare(right.name));
}
function stringMemberCompletions() {
    return [
        { name: 'length', kind: 'method', detail: 'length(): int' },
        { name: 'contains', kind: 'method', detail: 'contains(text: string): bool' },
        { name: 'find', kind: 'method', detail: 'find(text: string): int' },
        { name: 'count', kind: 'method', detail: 'count(text: string): int' },
        { name: 'is_int', kind: 'method', detail: 'is_int(): bool' },
        { name: 'is_float', kind: 'method', detail: 'is_float(): bool' },
        { name: 'to_upper', kind: 'method', detail: 'to_upper(): string' },
        { name: 'to_lower', kind: 'method', detail: 'to_lower(): string' },
        { name: 'substring', kind: 'method', detail: 'substring(start: int, length: int): string' },
        { name: 'replace', kind: 'method', detail: 'replace(old_text: string, new_text: string): string' },
        { name: 'split', kind: 'method', detail: 'split(separator: string): dyn_array<string>' },
        { name: 'trim', kind: 'method', detail: 'trim(): string' },
    ];
}
function arrayMemberCompletions(typeName) {
    const element = typeNameText(typeName.elementType);
    const items = [
        { name: 'length', kind: 'method', detail: 'length(): int' },
        { name: 'contains', kind: 'method', detail: `contains(value: ${element}): bool` },
        { name: 'find', kind: 'method', detail: `find(value: ${element}): int` },
        { name: 'count', kind: 'method', detail: `count(value: ${element}): int` },
        { name: 'reverse', kind: 'method', detail: 'reverse(): void' },
        { name: 'sort', kind: 'method', detail: 'sort(): void' },
    ];
    if (typeName.dynamic) {
        items.push({ name: 'add', kind: 'method', detail: `add(value: ${element}): void` }, { name: 'remove_at', kind: 'method', detail: 'remove_at(index: int): void' }, { name: 'resize', kind: 'method', detail: 'resize(size: int): void' }, { name: 'insert', kind: 'method', detail: `insert(index: int, value: ${element}): void` }, { name: 'join', kind: 'method', detail: `join(other: dyn_array<${element}>): void` }, { name: 'clear', kind: 'method', detail: 'clear(): void' }, { name: 'pop', kind: 'method', detail: `pop(): ${element}` });
    }
    return items.sort((left, right) => left.name.localeCompare(right.name));
}
function normalizeFiles(files) {
    const result = new Map();
    if (files instanceof Map) {
        for (const [file, source] of files) {
            result.set(normalizeFile(file), source);
        }
        return result;
    }
    for (const [file, source] of Object.entries(files)) {
        result.set(normalizeFile(file), source);
    }
    return result;
}
function normalizeFile(file) {
    return path.normalize(file);
}
function moduleNameFromFile(file) {
    return path.basename(file, '.idyl');
}
function signatureDetail(fn) {
    const parameters = fn.parameters.map((param) => `${param.name}: ${(0, types_1.typeToString)(param.type)}`);
    if (fn.variadic) {
        parameters.push(fn.variadicTypes?.length === 1 ? `...values: ${(0, types_1.typeToString)(fn.variadicTypes[0])}` : '...values');
    }
    return callableDetail(fn.name, parameters, (0, types_1.typeToString)(fn.returnType));
}
function functionDetail(name, arity) {
    const params = Array.from({ length: arity }, (_, index) => `arg${index + 1}`).join(', ');
    return `${name}(${params})`;
}
function callableDetail(name, parameters, returnType) {
    return `${name}(${parameters.join(', ')}): ${returnType}`;
}
function activeCallAtOffset(source, offset) {
    const safeOffset = Math.max(0, Math.min(source.length, offset));
    const frames = [];
    let squareDepth = 0;
    for (let i = 0; i < safeOffset; i++) {
        const char = source[i];
        const next = source[i + 1];
        if (char === '/' && next === '/') {
            i = skipLineComment(source, i + 2);
            continue;
        }
        if (char === '/' && next === '*') {
            i = skipBlockComment(source, i + 2);
            continue;
        }
        if (char === '"' || char === "'") {
            i = skipQuotedText(source, i, char, safeOffset);
            continue;
        }
        if (char === '[')
            squareDepth++;
        if (char === ']')
            squareDepth = Math.max(0, squareDepth - 1);
        if (char === '(') {
            frames.push({
                calleeText: calleeTextBefore(source, i),
                openParenIndex: i,
                currentArgumentStart: i + 1,
                activeParameter: 0,
            });
            continue;
        }
        if (char === ')') {
            frames.pop();
            continue;
        }
        if (char === ',' && squareDepth === 0 && frames.length > 0) {
            const frame = frames[frames.length - 1];
            frame.activeParameter++;
            frame.currentArgumentStart = i + 1;
        }
    }
    for (let i = frames.length - 1; i >= 0; i--) {
        const frame = frames[i];
        if (frame.calleeText) {
            const argumentSource = source.slice(frame.openParenIndex + 1, safeOffset);
            return {
                calleeText: frame.calleeText,
                activeParameter: frame.activeParameter,
                activeArgumentText: source.slice(frame.currentArgumentStart, safeOffset),
                previousArgumentTexts: splitTopLevelArguments(argumentSource).slice(0, -1),
            };
        }
    }
    return null;
}
function splitTopLevelArguments(text) {
    const result = [];
    let start = 0;
    let parenDepth = 0;
    let squareDepth = 0;
    for (let i = 0; i < text.length; i++) {
        const char = text[i];
        const next = text[i + 1];
        if (char === '/' && next === '/') {
            i = skipLineComment(text, i + 2);
            continue;
        }
        if (char === '/' && next === '*') {
            i = skipBlockComment(text, i + 2);
            continue;
        }
        if (char === '"' || char === "'") {
            i = skipQuotedText(text, i, char, text.length);
            continue;
        }
        if (char === '(')
            parenDepth++;
        if (char === ')')
            parenDepth = Math.max(0, parenDepth - 1);
        if (char === '[')
            squareDepth++;
        if (char === ']')
            squareDepth = Math.max(0, squareDepth - 1);
        if (char === ',' && parenDepth === 0 && squareDepth === 0) {
            result.push(text.slice(start, i));
            start = i + 1;
        }
    }
    result.push(text.slice(start));
    return result;
}
function calleeTextBefore(source, openParenIndex) {
    const fragment = source.slice(Math.max(0, openParenIndex - 160), openParenIndex);
    const match = /((?:[\p{L}_][\p{L}\p{N}_]*\s*\.\s*)?[\p{L}_][\p{L}\p{N}_]*)\s*$/u.exec(fragment);
    if (!match)
        return null;
    const text = match[1].trim();
    if (CALL_LIKE_KEYWORDS.has(text))
        return null;
    return text;
}
function skipLineComment(source, index) {
    let i = index;
    while (i < source.length && source[i] !== '\n')
        i++;
    return i;
}
function skipBlockComment(source, index) {
    let i = index;
    while (i + 1 < source.length) {
        if (source[i] === '*' && source[i + 1] === '/')
            return i + 1;
        i++;
    }
    return source.length;
}
function skipQuotedText(source, quoteStart, quote, limit) {
    let i = quoteStart + 1;
    while (i < limit) {
        if (source[i] === '\\') {
            i += 2;
            continue;
        }
        if (source[i] === quote)
            return i;
        i++;
    }
    return limit;
}
function signatureFromFunctionSpec(fn) {
    const minArguments = fn.minArguments ?? fn.parameters.length;
    const parameters = fn.parameters.map((param, index) => {
        const label = `${param.name}: ${(0, types_1.typeToString)(param.type)}`;
        return {
            name: param.name,
            label: index >= minArguments ? `[${label}]` : label,
        };
    });
    if (fn.variadic) {
        parameters.push({
            name: undefined,
            label: fn.variadicTypes?.length === 1 ? `...values: ${(0, types_1.typeToString)(fn.variadicTypes[0])}` : '...values',
        });
    }
    return {
        label: signatureDetail(fn),
        parameters,
        documentation: fn.documentation,
        allowsNamedArguments: !fn.variadic,
    };
}
function signatureFromFunctionDeclaration(declaration) {
    const parameters = declaration.parameters.map((parameter) => ({
        name: parameter.name,
        label: parameterDetail(parameter),
    }));
    return {
        label: callableDetail(declaration.name, parameters.map((parameter) => parameter.label), typeNameText(declaration.returnType)),
        parameters,
        allowsNamedArguments: true,
    };
}
function parameterDetail(parameter) {
    return `${parameter.name}: ${typeNameText(parameter.paramType)}${parameter.defaultValue ? ' = ...' : ''}`;
}
function signatureFromDetail(detail) {
    const open = detail.indexOf('(');
    const close = detail.indexOf(')', open + 1);
    const parametersText = open >= 0 && close >= open ? detail.slice(open + 1, close).trim() : '';
    const parameters = parametersText.length === 0
        ? []
        : splitSignatureParameters(parametersText).map((label) => ({
            name: parameterNameFromLabel(label),
            label,
        }));
    return {
        label: detail,
        parameters,
        allowsNamedArguments: !parameters.some((parameter) => parameter.label.trim().startsWith('...')),
    };
}
function activeParameterForCall(call, signature) {
    const named = namedArgumentName(call.activeArgumentText);
    if (named) {
        const namedIndex = signature.parameters.findIndex((parameter) => parameter.name === named);
        if (namedIndex >= 0)
            return namedIndex;
    }
    const hasNamedArguments = call.previousArgumentTexts.some((text) => namedArgumentName(text) !== null);
    if (hasNamedArguments && argumentNamePrefix(call.activeArgumentText) !== null) {
        const used = usedNamedArgumentNames(call);
        const availableIndex = signature.parameters.findIndex((parameter) => parameter.name && !used.has(parameter.name));
        if (availableIndex >= 0)
            return availableIndex;
    }
    return call.activeParameter;
}
function argumentNamePrefix(text) {
    const trimmed = text.trimStart();
    if (topLevelEqualsIndex(trimmed) >= 0)
        return null;
    const match = /^([\p{L}_][\p{L}\p{N}_]*)?$/u.exec(trimmed);
    return match ? (match[1] ?? '') : null;
}
function namedArgumentName(text) {
    const trimmed = text.trimStart();
    const equals = topLevelEqualsIndex(trimmed);
    if (equals < 0)
        return null;
    const name = trimmed.slice(0, equals).trim();
    return /^[\p{L}_][\p{L}\p{N}_]*$/u.test(name) ? name : null;
}
function usedNamedArgumentNames(call) {
    const used = new Set();
    for (const argumentText of call.previousArgumentTexts) {
        const name = namedArgumentName(argumentText);
        if (name)
            used.add(name);
    }
    const current = namedArgumentName(call.activeArgumentText);
    if (current)
        used.add(current);
    return used;
}
function topLevelEqualsIndex(text) {
    let parenDepth = 0;
    let squareDepth = 0;
    for (let i = 0; i < text.length; i++) {
        const char = text[i];
        const next = text[i + 1];
        if (char === '/' && next === '/') {
            i = skipLineComment(text, i + 2);
            continue;
        }
        if (char === '/' && next === '*') {
            i = skipBlockComment(text, i + 2);
            continue;
        }
        if (char === '"' || char === "'") {
            i = skipQuotedText(text, i, char, text.length);
            continue;
        }
        if (char === '(')
            parenDepth++;
        if (char === ')')
            parenDepth = Math.max(0, parenDepth - 1);
        if (char === '[')
            squareDepth++;
        if (char === ']')
            squareDepth = Math.max(0, squareDepth - 1);
        if (char === '=' && parenDepth === 0 && squareDepth === 0)
            return i;
    }
    return -1;
}
function parameterNameFromLabel(label) {
    const cleaned = label.trim().replace(/^\[/u, '').replace(/\]$/u, '').replace(/^\.\.\./u, '');
    const colon = cleaned.indexOf(':');
    if (colon < 0)
        return undefined;
    const name = cleaned.slice(0, colon).trim();
    return /^[\p{L}_][\p{L}\p{N}_]*$/u.test(name) ? name : undefined;
}
function splitSignatureParameters(text) {
    const result = [];
    let depth = 0;
    let start = 0;
    for (let i = 0; i < text.length; i++) {
        const char = text[i];
        if (char === '<')
            depth++;
        if (char === '>')
            depth = Math.max(0, depth - 1);
        if (char === ',' && depth === 0) {
            result.push(text.slice(start, i).trim());
            start = i + 1;
        }
    }
    result.push(text.slice(start).trim());
    return result.filter(Boolean);
}
const CALL_LIKE_KEYWORDS = new Set([
    'if',
    'while',
    'for',
    'function',
    'main',
    'constructor',
]);
function wordAtOffset(source, offset) {
    const safeOffset = Math.max(0, Math.min(source.length, offset));
    let start = safeOffset;
    let end = safeOffset;
    if (start > 0 && !isIdentifierChar(source[start]) && isIdentifierChar(source[start - 1])) {
        start--;
        end--;
    }
    while (start > 0 && isIdentifierChar(source[start - 1]))
        start--;
    while (end < source.length && isIdentifierChar(source[end]))
        end++;
    if (start === end)
        return null;
    return {
        text: source.slice(start, end),
        start,
        end,
    };
}
function memberContext(source, wordStart) {
    let dot = wordStart - 1;
    while (dot >= 0 && /\s/u.test(source[dot]))
        dot--;
    if (source[dot] !== '.')
        return null;
    let end = dot;
    let start = end;
    while (start > 0 && /\s/u.test(source[start - 1]))
        start--;
    end = start;
    while (start > 0 && isIdentifierChar(source[start - 1]))
        start--;
    if (start === end)
        return null;
    return { objectName: source.slice(start, end) };
}
function useModuleNameAtWord(source, word) {
    const lineStart = source.lastIndexOf('\n', word.start - 1) + 1;
    const beforeWord = source.slice(lineStart, word.start);
    if (!/^\s*use\s+$/u.test(beforeWord))
        return null;
    const afterWord = source.slice(word.end);
    if (!/^\s*(?:;|$)/u.test(afterWord))
        return null;
    return word.text;
}
function hoverFromWord(source, file, word, detail) {
    return {
        detail,
        range: {
            start: sourceLocationAtOffset(source, file, word.start),
            end: sourceLocationAtOffset(source, file, word.end),
        },
    };
}
function sourceLocationAtOffset(source, file, offset) {
    const safeOffset = Math.max(0, Math.min(source.length, offset));
    let line = 1;
    let column = 1;
    for (let i = 0; i < safeOffset; i++) {
        if (source[i] === '\n') {
            line++;
            column = 1;
        }
        else {
            column++;
        }
    }
    return { file, line, column };
}
function isIdentifierChar(char) {
    return char !== undefined && /[\p{L}\p{N}_]/u.test(char);
}
function primitiveTypeHover(text) {
    if (['int', 'float', 'string', 'char', 'bool', 'void'].includes(text)) {
        return `type ${text}`;
    }
    return null;
}
function typeNameText(typeName) {
    if (typeName.kind === 'PrimitiveTypeName')
        return typeName.name;
    if (typeName.kind === 'ClassTypeName')
        return typeName.name;
    if (typeName.kind === 'QualifiedTypeName')
        return `${typeName.moduleName}.${typeName.name}`;
    if (typeName.kind === 'ArrayTypeName') {
        const element = typeNameText(typeName.elementType);
        return typeName.dynamic ? `dyn_array<${element}>` : `array<${element}, ${typeName.size ?? '?'}>`;
    }
    return 'unknown';
}
//# sourceMappingURL=project.js.map
},
"src/runtime/run.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.compileIdyllium = compileIdyllium;
exports.runIdyllium = runIdyllium;
const codegen_1 = require("../core/codegen");
const diagnostics_1 = require("../core/diagnostics");
const project_1 = require("../core/project");
const semantics_1 = require("../core/semantics");
const registry_1 = require("../core/stdlib/registry");
const runtime_1 = require("./runtime");
function compileIdyllium(source, options = {}) {
    const file = options.file ?? 'main.idyl';
    const stdlib = options.stdlib ?? (0, registry_1.createDefaultStandardLibrary)();
    const diagnostics = new diagnostics_1.DiagnosticBag();
    const root = (0, project_1.parseSource)(null, file, source, diagnostics);
    let ast = root.ast;
    let jsCode = null;
    const modules = [];
    if (ast && !diagnostics.hasErrors()) {
        (0, project_1.loadUserModules)(ast, file, options, stdlib, diagnostics, modules);
    }
    const userModuleRegistry = (0, project_1.buildUserModuleRegistry)(modules, stdlib, diagnostics);
    if (ast && !diagnostics.hasErrors()) {
        for (const module of modules) {
            const semantics = new semantics_1.SemanticAnalyzer(stdlib, userModuleRegistry).analyze(module.ast);
            (0, project_1.addDiagnostics)(diagnostics, semantics.diagnostics);
        }
        const semantics = new semantics_1.SemanticAnalyzer(stdlib, userModuleRegistry).analyze(ast);
        (0, project_1.addDiagnostics)(diagnostics, semantics.diagnostics);
    }
    if (ast && !diagnostics.hasErrors()) {
        jsCode = new codegen_1.JavaScriptGenerator({
            userModuleNames: new Set(modules.map((module) => module.name)),
        }).generate(ast, { modules: modules.map((module) => ({ name: module.name, program: module.ast })) }).jsCode;
    }
    if (diagnostics.hasErrors()) {
        ast = root.ast;
    }
    const allDiagnostics = diagnostics.all();
    return {
        success: !diagnostics.hasErrors(),
        jsCode,
        diagnostics: allDiagnostics,
        diagnosticsText: (0, diagnostics_1.formatDiagnostics)(allDiagnostics),
        tokens: root.tokens,
        ast,
    };
}
async function runIdyllium(source, runtimeOptions = {}, compileOptions = {}) {
    const compilation = compileIdyllium(source, compileOptions);
    if (!compilation.success || !compilation.jsCode) {
        return {
            success: false,
            output: '',
            runtimeError: null,
            compilation,
        };
    }
    const runtime = (0, runtime_1.createRuntime)(runtimeOptions);
    try {
        const AsyncFunction = Object.getPrototypeOf(async function idle() { }).constructor;
        const factory = new AsyncFunction(compilation.jsCode);
        const program = await factory();
        await program(runtime);
        return {
            success: true,
            output: runtime.getOutput(),
            runtimeError: null,
            compilation,
        };
    }
    catch (error) {
        return {
            success: false,
            output: runtime.getOutput(),
            runtimeError: error instanceof Error ? error.message : String(error),
            compilation,
        };
    }
}
//# sourceMappingURL=run.js.map
},
"src/runtime/runtime.js": function(require, module, exports) {
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.IdylliumArray = exports.IdylliumTimeStamp = exports.IdylliumColor = exports.IdylliumRuntimeError = void 0;
exports.createMemoryRuntimeFileSystem = createMemoryRuntimeFileSystem;
exports.createRuntime = createRuntime;
const nodeFs = require('fs');
const nodePath = require('path');
const nodeBuffer = require('buffer').Buffer;
class IdylliumRuntimeError extends Error {
    file;
    line;
    constructor(file, line, message) {
        super(`${file}:${line}: runtime error: ${message}`);
        this.file = file;
        this.line = line;
        this.name = 'IdylliumRuntimeError';
    }
}
exports.IdylliumRuntimeError = IdylliumRuntimeError;
class IdylliumColor {
    red;
    green;
    blue;
    alpha;
    constructor(red, green, blue, alpha) {
        this.red = red;
        this.green = green;
        this.blue = blue;
        this.alpha = alpha;
    }
    static RGB(red, green, blue, file = 'colors', line = 0) {
        return new IdylliumColor(channel(red, 'colors.RGB() red', file, line), channel(green, 'colors.RGB() green', file, line), channel(blue, 'colors.RGB() blue', file, line), 1);
    }
    static RGBA(red, green, blue, alpha, file = 'colors', line = 0) {
        return new IdylliumColor(channel(red, 'colors.RGBA() red', file, line), channel(green, 'colors.RGBA() green', file, line), channel(blue, 'colors.RGBA() blue', file, line), opacity(alpha, 'colors.RGBA() alpha', file, line));
    }
    static HEX(value, file = 'colors', line = 0) {
        if (typeof value !== 'string') {
            throw new IdylliumRuntimeError(file, line, `colors.HEX() expects string, got '${String(value)}'`);
        }
        const normalized = value.trim();
        const match = /^#?([0-9a-fA-F]{6}|[0-9a-fA-F]{8})$/u.exec(normalized);
        if (!match) {
            throw new IdylliumRuntimeError(file, line, `invalid HEX color '${value}'`);
        }
        const hex = match[1];
        const red = Number.parseInt(hex.slice(0, 2), 16);
        const green = Number.parseInt(hex.slice(2, 4), 16);
        const blue = Number.parseInt(hex.slice(4, 6), 16);
        const alpha = hex.length === 8 ? Number.parseInt(hex.slice(6, 8), 16) / 255 : 1;
        return IdylliumColor.RGBA(red, green, blue, alpha, file, line);
    }
    static HSL(hue, saturation, lightness, file = 'colors', line = 0) {
        const h = percentRange(hue, 'colors.HSL() hue', 0, 360, file, line);
        const s = percent(saturation, 'colors.HSL() saturation', file, line);
        const l = percent(lightness, 'colors.HSL() lightness', file, line);
        const c = (1 - Math.abs(2 * l - 1)) * s;
        const x = c * (1 - Math.abs((h / 60) % 2 - 1));
        const m = l - c / 2;
        let red = 0;
        let green = 0;
        let blue = 0;
        if (h < 60) {
            red = c;
            green = x;
        }
        else if (h < 120) {
            red = x;
            green = c;
        }
        else if (h < 180) {
            green = c;
            blue = x;
        }
        else if (h < 240) {
            green = x;
            blue = c;
        }
        else if (h < 300) {
            red = x;
            blue = c;
        }
        else {
            red = c;
            blue = x;
        }
        return IdylliumColor.RGB(Math.round((red + m) * 255), Math.round((green + m) * 255), Math.round((blue + m) * 255), file, line);
    }
    toHex() {
        return `#${hex(this.red)}${hex(this.green)}${hex(this.blue)}`;
    }
    toCss() {
        if (this.alpha === 1)
            return this.toHex();
        return `rgba(${this.red}, ${this.green}, ${this.blue}, ${trimFloat(this.alpha)})`;
    }
    toString() {
        return this.toCss();
    }
}
exports.IdylliumColor = IdylliumColor;
class IdylliumTimeStamp {
    unixSeconds;
    constructor(unixSeconds) {
        this.unixSeconds = unixSeconds;
    }
    static now() {
        return new IdylliumTimeStamp(Math.floor(Date.now() / 1000));
    }
    static fromUnix(seconds, file, line) {
        return new IdylliumTimeStamp(integerNumber(seconds, 'time.from_unix() seconds', file, line));
    }
    year() {
        return this.date().getUTCFullYear();
    }
    month() {
        return this.date().getUTCMonth() + 1;
    }
    day() {
        return this.date().getUTCDate();
    }
    hour() {
        return this.date().getUTCHours();
    }
    minute() {
        return this.date().getUTCMinutes();
    }
    second() {
        return this.date().getUTCSeconds();
    }
    week_day() {
        return this.date().getUTCDay();
    }
    unix() {
        return this.unixSeconds;
    }
    to_string() {
        return [
            this.year().toString().padStart(4, '0'),
            '-',
            this.month().toString().padStart(2, '0'),
            '-',
            this.day().toString().padStart(2, '0'),
            ' ',
            this.hour().toString().padStart(2, '0'),
            ':',
            this.minute().toString().padStart(2, '0'),
            ':',
            this.second().toString().padStart(2, '0'),
        ].join('');
    }
    toString() {
        return this.to_string();
    }
    date() {
        return new Date(this.unixSeconds * 1000);
    }
}
exports.IdylliumTimeStamp = IdylliumTimeStamp;
class IdylliumArray {
    items;
    dynamic;
    staticSize;
    defaultFactory;
    constructor(items, dynamic, staticSize, defaultFactory) {
        this.items = items;
        this.dynamic = dynamic;
        this.staticSize = staticSize;
        this.defaultFactory = defaultFactory;
        if (!dynamic && staticSize !== null && items.length !== staticSize) {
            throw new IdylliumRuntimeError('array', 0, `array initializer has ${items.length} elements, but static array requires ${staticSize}`);
        }
    }
    static create(size, defaultFactory, dynamic) {
        const normalizedSize = Math.max(0, Math.trunc(size));
        return new IdylliumArray(Array.from({ length: normalizedSize }, () => defaultFactory()), dynamic, dynamic ? null : normalizedSize, defaultFactory);
    }
    static from(values, dynamic, staticSize, defaultFactory) {
        return new IdylliumArray([...values], dynamic, dynamic ? null : staticSize, defaultFactory);
    }
    get(index, file, line) {
        return this.items[this.validIndex(index, file, line)];
    }
    set(index, value, file, line) {
        this.items[this.validIndex(index, file, line)] = value;
    }
    length() {
        return this.items.length;
    }
    contains(value) {
        return this.items.includes(value);
    }
    find(value) {
        return this.items.indexOf(value);
    }
    count(value) {
        return this.items.filter((item) => Object.is(item, value) || item === value).length;
    }
    reverse() {
        this.items.reverse();
    }
    sort() {
        if (this.items.every((item) => typeof item === 'number')) {
            this.items.sort((left, right) => left - right);
            return;
        }
        this.items.sort((left, right) => formatForInspect(left).localeCompare(formatForInspect(right)));
    }
    add(value, file, line) {
        this.expectDynamic('add', file, line);
        this.items.push(value);
    }
    remove_at(index, file, line) {
        this.expectDynamic('remove_at', file, line);
        this.items.splice(this.validIndex(index, file, line), 1);
    }
    resize(size, file, line) {
        this.expectDynamic('resize', file, line);
        const normalizedSize = this.validSize(size, file, line);
        while (this.items.length < normalizedSize) {
            this.items.push(this.defaultFactory());
        }
        this.items.length = normalizedSize;
    }
    insert(index, value, file, line) {
        this.expectDynamic('insert', file, line);
        const normalizedIndex = this.validInsertIndex(index, file, line);
        this.items.splice(normalizedIndex, 0, value);
    }
    join(other, file, line) {
        this.expectDynamic('join', file, line);
        if (!(other instanceof IdylliumArray)) {
            throw new IdylliumRuntimeError(file, line, `join() expects an array, got '${String(other)}'`);
        }
        this.items.push(...other.items);
    }
    clear(file, line) {
        this.expectDynamic('clear', file, line);
        this.items.length = 0;
    }
    pop(file, line) {
        this.expectDynamic('pop', file, line);
        if (this.items.length === 0) {
            throw new IdylliumRuntimeError(file, line, 'cannot pop from empty array');
        }
        return this.items.pop();
    }
    callMethod(name, args, file, line) {
        switch (name) {
            case 'length':
                return this.length();
            case 'contains':
                return this.contains(args[0]);
            case 'find':
                return this.find(args[0]);
            case 'count':
                return this.count(args[0]);
            case 'reverse':
                return this.reverse();
            case 'sort':
                return this.sort();
            case 'add':
                return this.add(args[0], file, line);
            case 'remove_at':
                return this.remove_at(args[0], file, line);
            case 'resize':
                return this.resize(args[0], file, line);
            case 'insert':
                return this.insert(args[0], args[1], file, line);
            case 'join':
                return this.join(args[0], file, line);
            case 'clear':
                return this.clear(file, line);
            case 'pop':
                return this.pop(file, line);
            default:
                throw new IdylliumRuntimeError(file, line, `array has no method '${name}'`);
        }
    }
    values() {
        return this.items;
    }
    toString() {
        return formatForInspect(this);
    }
    toInspectString() {
        return `[${this.items.map(formatForInspect).join(', ')}]`;
    }
    validIndex(index, file, line) {
        if (!Number.isInteger(index)) {
            throw new IdylliumRuntimeError(file, line, `array index must be int, got '${String(index)}'`);
        }
        if (index < 0 || index >= this.items.length) {
            throw new IdylliumRuntimeError(file, line, `array index ${index} out of bounds (size ${this.items.length}, valid indices ${this.validIndexRange()})`);
        }
        return index;
    }
    validInsertIndex(index, file, line) {
        if (!Number.isInteger(index)) {
            throw new IdylliumRuntimeError(file, line, `array index must be int, got '${String(index)}'`);
        }
        if (index < 0 || index > this.items.length) {
            throw new IdylliumRuntimeError(file, line, `array insert index ${index} out of bounds (size ${this.items.length}, valid indices 0-${this.items.length})`);
        }
        return index;
    }
    validSize(size, file, line) {
        if (!Number.isInteger(size)) {
            throw new IdylliumRuntimeError(file, line, `array size must be int, got '${String(size)}'`);
        }
        if (size < 0) {
            throw new IdylliumRuntimeError(file, line, `array size must be non-negative, got ${size}`);
        }
        return size;
    }
    validIndexRange() {
        if (this.items.length === 0)
            return 'none';
        return `0-${this.items.length - 1}`;
    }
    expectDynamic(methodName, file, line) {
        if (!this.dynamic) {
            throw new IdylliumRuntimeError(file, line, `array method '${methodName}' is only available on dyn_array`);
        }
    }
}
exports.IdylliumArray = IdylliumArray;
function createJsonValue(value, file = 'json', line = 0) {
    if (value === undefined || value === null)
        return createJsonPrimitive('null', null);
    if (isJsonRuntimeValue(value))
        return value;
    if (typeof value === 'string')
        return createJsonPrimitive('string', value);
    if (typeof value === 'boolean')
        return createJsonPrimitive('bool', value);
    if (typeof value === 'number') {
        const number = finiteNumber(value, 'json.Value() value', file, line);
        return createJsonPrimitive(Number.isInteger(number) ? 'int' : 'float', number);
    }
    throw new IdylliumRuntimeError(file, line, `json.Value() cannot convert '${runtimeTypeName(value)}' to json.Value`);
}
function createJsonObject(entries = new Map()) {
    const obj = createJsonBase('json.Object', 'object');
    Object.defineProperty(obj, '__jsonEntries', {
        value: entries,
        enumerable: false,
        configurable: true,
    });
    obj.length = () => entries.size;
    obj.has = contextFunction((key, file, line) => entries.has(stringArgument(key, 'json.Object.has() key', file, line)));
    obj.get = contextFunction((key, file, line) => {
        const name = stringArgument(key, 'json.Object.get() key', file, line);
        if (!entries.has(name)) {
            throw new IdylliumRuntimeError(file, line, `json object has no key '${name}'`);
        }
        return entries.get(name);
    });
    obj.add = contextFunction((key, value, file, line) => {
        const name = stringArgument(key, 'json.Object.add() key', file, line);
        if (entries.has(name)) {
            throw new IdylliumRuntimeError(file, line, `json object already has key '${name}'`);
        }
        entries.set(name, expectJsonValue(value, 'json.Object.add() value', file, line));
    });
    obj.set = contextFunction((key, value, file, line) => {
        const name = stringArgument(key, 'json.Object.set() key', file, line);
        if (!entries.has(name)) {
            throw new IdylliumRuntimeError(file, line, `json object has no key '${name}'`);
        }
        entries.set(name, expectJsonValue(value, 'json.Object.set() value', file, line));
    });
    obj.remove = contextFunction((key, file, line) => {
        const name = stringArgument(key, 'json.Object.remove() key', file, line);
        if (!entries.delete(name)) {
            throw new IdylliumRuntimeError(file, line, `json object has no key '${name}'`);
        }
    });
    obj.keys = () => IdylliumArray.from([...entries.keys()], true, null, () => '');
    return obj;
}
function createJsonArray(items = []) {
    const obj = createJsonBase('json.Array', 'array');
    Object.defineProperty(obj, '__jsonItems', {
        value: items,
        enumerable: false,
        configurable: true,
    });
    obj.length = () => items.length;
    obj.at = contextFunction((index, file, line) => items[jsonArrayIndex(items, index, 'json.Array.at()', file, line)]);
    obj.set = contextFunction((index, value, file, line) => {
        items[jsonArrayIndex(items, index, 'json.Array.set()', file, line)] = expectJsonValue(value, 'json.Array.set() value', file, line);
    });
    obj.add = contextFunction((value, file, line) => {
        items.push(expectJsonValue(value, 'json.Array.add() value', file, line));
    });
    obj.insert = contextFunction((index, value, file, line) => {
        items.splice(jsonArrayInsertIndex(items, index, file, line), 0, expectJsonValue(value, 'json.Array.insert() value', file, line));
    });
    obj.pop = contextFunction((file, line) => {
        if (items.length === 0)
            throw new IdylliumRuntimeError(file, line, 'json.Array.pop() cannot pop from empty array');
        return items.pop();
    });
    obj.remove = contextFunction((index, file, line) => {
        items.splice(jsonArrayIndex(items, index, 'json.Array.remove()', file, line), 1);
    });
    obj.clear = () => {
        items.length = 0;
    };
    return obj;
}
function createJsonPrimitive(kind, value) {
    const obj = createJsonBase('json.Value', kind);
    obj.__jsonValue = value;
    return obj;
}
function createJsonBase(typeName, kind) {
    const obj = {
        __idylliumType: typeName,
        __jsonKind: kind,
    };
    obj.is_null = () => obj.__jsonKind === 'null';
    obj.is_string = () => obj.__jsonKind === 'string';
    obj.is_int = () => obj.__jsonKind === 'int';
    obj.is_float = () => obj.__jsonKind === 'int' || obj.__jsonKind === 'float';
    obj.is_bool = () => obj.__jsonKind === 'bool';
    obj.is_object = () => obj.__jsonKind === 'object';
    obj.is_array = () => obj.__jsonKind === 'array';
    obj.to_string = contextFunction((file, line) => {
        if (obj.__jsonKind === 'string')
            return obj.__jsonValue;
        throwJsonExpected(obj, 'string', file, line);
    });
    obj.to_int = contextFunction((file, line) => {
        if (obj.__jsonKind === 'int')
            return obj.__jsonValue;
        throwJsonExpected(obj, 'int', file, line);
    });
    obj.to_float = contextFunction((file, line) => {
        if (obj.__jsonKind === 'int' || obj.__jsonKind === 'float')
            return obj.__jsonValue;
        throwJsonExpected(obj, 'number', file, line);
    });
    obj.to_bool = contextFunction((file, line) => {
        if (obj.__jsonKind === 'bool')
            return obj.__jsonValue;
        throwJsonExpected(obj, 'bool', file, line);
    });
    obj.to_object = contextFunction((file, line) => {
        if (obj.__jsonKind === 'object')
            return jsonObjectValue(obj);
        throwJsonExpected(obj, 'object', file, line);
    });
    obj.to_array = contextFunction((file, line) => {
        if (obj.__jsonKind === 'array')
            return jsonArrayValue(obj);
        throwJsonExpected(obj, 'array', file, line);
    });
    obj.set_null = () => setJsonPrimitiveValue(obj, 'null', null);
    obj.set_string = contextFunction((value, file, line) => {
        setJsonPrimitiveValue(obj, 'string', stringArgument(value, 'json.Value.set_string() value', file, line));
    });
    obj.set_int = contextFunction((value, file, line) => {
        setJsonPrimitiveValue(obj, 'int', integerNumber(value, 'json.Value.set_int() value', file, line));
    });
    obj.set_float = contextFunction((value, file, line) => {
        setJsonPrimitiveValue(obj, 'float', finiteNumber(value, 'json.Value.set_float() value', file, line));
    });
    obj.set_bool = contextFunction((value, file, line) => {
        if (typeof value !== 'boolean') {
            throw new IdylliumRuntimeError(file, line, `json.Value.set_bool() value must be bool, got '${runtimeTypeName(value)}'`);
        }
        setJsonPrimitiveValue(obj, 'bool', value);
    });
    obj.set_object = contextFunction((value, file, line) => {
        setJsonNestedValue(obj, 'object', expectJsonKind(value, 'object', 'json.Value.set_object() value', file, line));
    });
    obj.set_array = contextFunction((value, file, line) => {
        setJsonNestedValue(obj, 'array', expectJsonKind(value, 'array', 'json.Value.set_array() value', file, line));
    });
    obj.to_json = () => jsonSerialize(obj, 0);
    obj.to_pretty_json = contextFunction((indentOrFile, fileOrLine, maybeLine) => {
        const context = optionalNumberContext(indentOrFile, fileOrLine, maybeLine);
        const indent = context.value === undefined ? 2 : jsonIndent(context.value, context.file, context.line);
        return jsonSerialize(obj, indent);
    });
    obj.toString = () => jsonSerialize(obj, 0);
    return obj;
}
function parseJsonValue(text, file, line) {
    try {
        return nativeToJsonRuntime(JSON.parse(text));
    }
    catch (error) {
        throw new IdylliumRuntimeError(file, line, `json.parse() invalid JSON: ${errorMessage(error)}`);
    }
}
function nativeToJsonRuntime(value) {
    if (value === null)
        return createJsonValue();
    if (typeof value === 'string' || typeof value === 'boolean' || typeof value === 'number')
        return createJsonValue(value);
    if (Array.isArray(value))
        return createJsonArray(value.map(nativeToJsonRuntime));
    if (isPlainObject(value)) {
        const entries = new Map();
        for (const [key, item] of Object.entries(value)) {
            entries.set(key, nativeToJsonRuntime(item));
        }
        return createJsonObject(entries);
    }
    return createJsonValue();
}
function jsonRuntimeToNative(value) {
    switch (value.__jsonKind) {
        case 'object': {
            if (isJsonRuntimeValue(value.__jsonValue))
                return jsonRuntimeToNative(value.__jsonValue);
            const result = {};
            for (const [key, item] of jsonEntries(value)) {
                result[key] = jsonRuntimeToNative(item);
            }
            return result;
        }
        case 'array':
            if (isJsonRuntimeValue(value.__jsonValue))
                return jsonRuntimeToNative(value.__jsonValue);
            return jsonItems(value).map(jsonRuntimeToNative);
        case 'null':
            return null;
        default:
            return value.__jsonValue;
    }
}
function jsonSerialize(value, indent) {
    return JSON.stringify(jsonRuntimeToNative(value), null, indent);
}
function isJsonRuntimeValue(value) {
    return isPlainObject(value)
        && typeof value.__idylliumType === 'string'
        && ['json.Value', 'json.Object', 'json.Array'].includes(value.__idylliumType)
        && typeof value.__jsonKind === 'string';
}
function expectJsonValue(value, argumentName, file, line) {
    if (isJsonRuntimeValue(value))
        return value;
    throw new IdylliumRuntimeError(file, line, `${argumentName} expects json.Value, got '${runtimeTypeName(value)}'`);
}
function expectJsonKind(value, kind, argumentName, file, line) {
    const jsonValue = expectJsonValue(value, argumentName, file, line);
    if (jsonValue.__jsonKind === kind)
        return jsonValue;
    throw new IdylliumRuntimeError(file, line, `${argumentName} expects json.${kind === 'object' ? 'Object' : 'Array'}, got json.${jsonKindText(jsonValue)}`);
}
function jsonObjectValue(value) {
    return isJsonRuntimeValue(value.__jsonValue) ? value.__jsonValue : value;
}
function jsonArrayValue(value) {
    return isJsonRuntimeValue(value.__jsonValue) ? value.__jsonValue : value;
}
function setJsonPrimitiveValue(target, kind, value) {
    target.__jsonKind = kind;
    target.__jsonValue = value;
}
function setJsonNestedValue(target, kind, value) {
    target.__jsonKind = kind;
    target.__jsonValue = value;
}
function jsonEntries(value) {
    return value.__jsonEntries instanceof Map ? value.__jsonEntries : new Map();
}
function jsonItems(value) {
    return Array.isArray(value.__jsonItems) ? value.__jsonItems : [];
}
function jsonArrayIndex(items, value, operationName, file, line) {
    const index = integerNumber(value, `${operationName} index`, file, line);
    if (index < 0 || index >= items.length) {
        throw new IdylliumRuntimeError(file, line, `json array index ${index} out of bounds (size ${items.length}, valid indices ${validRange(items.length)})`);
    }
    return index;
}
function jsonArrayInsertIndex(items, value, file, line) {
    const index = integerNumber(value, 'json.Array.insert() index', file, line);
    if (index < 0 || index > items.length) {
        throw new IdylliumRuntimeError(file, line, `json array insert index ${index} out of bounds (size ${items.length}, valid indices 0-${items.length})`);
    }
    return index;
}
function jsonIndent(value, file, line) {
    const indent = integerNumber(value, 'json.to_pretty_json() indent', file, line);
    if (indent < 0 || indent > 16) {
        throw new IdylliumRuntimeError(file, line, `json.to_pretty_json() indent must be between 0 and 16, got ${indent}`);
    }
    return indent;
}
function throwJsonExpected(value, expected, file, line) {
    throw new IdylliumRuntimeError(file, line, `json value is ${jsonKindText(value)}, expected ${expected}`);
}
function jsonKindText(value) {
    if (value.__jsonKind === 'int' || value.__jsonKind === 'float')
        return 'number';
    return value.__jsonKind;
}
function createMemoryRuntimeFileSystem(entries = {}, cwd = '/workspace') {
    const files = new Map();
    const directories = new Set();
    const writtenFiles = new Set();
    const normalizedCwd = normalizeMemoryPath(cwd);
    addMemoryDirectory(directories, '/');
    addMemoryDirectory(directories, normalizedCwd);
    for (const [rawPath, rawEntry] of Object.entries(entries)) {
        const filePath = normalizeMemoryPath(rawPath, normalizedCwd);
        if (typeof rawEntry !== 'string' && rawEntry.kind === 'directory') {
            addMemoryDirectory(directories, filePath);
            continue;
        }
        addMemoryDirectory(directories, memoryDirname(filePath));
        files.set(filePath, {
            content: typeof rawEntry === 'string' ? rawEntry : rawEntry.content ?? '',
            resourceUri: typeof rawEntry === 'string' ? null : rawEntry.resourceUri ?? null,
        });
    }
    return {
        resolvePath(requestedPath, sourceFile) {
            const sourceDirectory = sourceFile.trim() === '' ? normalizedCwd : memoryDirname(normalizeMemoryPath(sourceFile, normalizedCwd));
            return normalizeMemoryPath(requestedPath, sourceDirectory);
        },
        exists(filePath) {
            const normalized = normalizeMemoryPath(filePath, normalizedCwd);
            return files.has(normalized) || directories.has(normalized);
        },
        isFile(filePath) {
            return files.has(normalizeMemoryPath(filePath, normalizedCwd));
        },
        isDirectory(filePath) {
            return directories.has(normalizeMemoryPath(filePath, normalizedCwd));
        },
        readText(filePath) {
            const normalized = normalizeMemoryPath(filePath, normalizedCwd);
            const file = files.get(normalized);
            if (!file)
                throw new Error(`file does not exist: ${normalized}`);
            return file.content;
        },
        writeText(filePath, text) {
            const normalized = normalizeMemoryPath(filePath, normalizedCwd);
            const parent = memoryDirname(normalized);
            if (!directories.has(parent))
                throw new Error(`directory does not exist: ${parent}`);
            if (directories.has(normalized))
                throw new Error(`path is a directory: ${normalized}`);
            files.set(normalized, { content: text, resourceUri: null });
            writtenFiles.add(normalized);
        },
        appendText(filePath, text) {
            const normalized = normalizeMemoryPath(filePath, normalizedCwd);
            const file = files.get(normalized);
            if (!file)
                throw new Error(`file does not exist: ${normalized}`);
            files.set(normalized, { ...file, content: file.content + text });
            writtenFiles.add(normalized);
        },
        resourceUri(filePath) {
            return files.get(normalizeMemoryPath(filePath, normalizedCwd))?.resourceUri ?? null;
        },
        snapshot() {
            const result = {};
            for (const directory of [...directories].sort()) {
                result[directory] = { kind: 'directory' };
            }
            for (const [filePath, file] of [...files.entries()].sort(([left], [right]) => left.localeCompare(right))) {
                result[filePath] = {
                    kind: 'file',
                    content: file.content,
                    resourceUri: file.resourceUri ?? undefined,
                };
            }
            return result;
        },
        writtenFilesSnapshot() {
            const result = {};
            for (const filePath of [...writtenFiles].sort()) {
                const file = files.get(filePath);
                if (!file)
                    continue;
                result[filePath] = {
                    kind: 'file',
                    content: file.content,
                    resourceUri: file.resourceUri ?? undefined,
                };
            }
            return result;
        },
    };
}
function contextFunction(fn) {
    const callable = fn;
    callable.__idylliumPassContext = true;
    return callable;
}
function createRuntime(options = {}) {
    let output = '';
    let precision = null;
    let randomSeed = null;
    const input = [...(options.input ?? [])];
    const fileSystem = options.fileSystem ?? createNodeRuntimeFileSystem();
    const runtimeObjects = {
        objects: [],
        audio: [],
        canvases: [],
        fileSystem,
        modals: [],
        timers: [],
        windows: [],
        nextAudioCommandId: 1,
        nextObjectId: 1,
    };
    const io = {
        write(text) {
            output += text;
            options.console?.write?.(text);
        },
        clear() {
            output = '';
            options.console?.clear?.();
        },
        async readLine() {
            if (options.console?.readLine) {
                return options.console.readLine();
            }
            return input.shift() ?? '';
        },
    };
    async function formatConsoleValue(value) {
        if (isJsonRuntimeValue(value)) {
            return formatForConsole(value, precision);
        }
        if (value !== null && typeof value === 'object') {
            const method = value.to_string;
            if (typeof method === 'function') {
                const result = await method.apply(value);
                return formatForConsole(result, precision);
            }
        }
        return formatForConsole(value, precision);
    }
    async function formatConsoleValues(values) {
        const parts = [];
        for (const value of values) {
            parts.push(await formatConsoleValue(value));
        }
        return parts.join('');
    }
    function createInputFile(filePath, sourceFile, line) {
        if (!fileSystem.exists(filePath)) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for reading: file does not exist`);
        }
        if (!runtimeIsFile(fileSystem, filePath, sourceFile, line, 'reading')) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for reading: path is not a file`);
        }
        let content;
        let lines;
        try {
            content = fileSystem.readText(filePath);
            lines = splitFileLines(content);
        }
        catch (error) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for reading: ${errorMessage(error)}`);
        }
        let index = 0;
        let closed = false;
        const stream = {
            __idylliumType: 'file.istream',
            read_line: contextFunction((file, callLine) => {
                expectOpen(!closed, 'istream.read_line()', file, callLine);
                if (index >= lines.length) {
                    throw new IdylliumRuntimeError(file, callLine, 'istream.read_line() cannot read past end of file');
                }
                return lines[index++];
            }),
            read_all: contextFunction((file, callLine) => {
                expectOpen(!closed, 'istream.read_all()', file, callLine);
                const rest = lines.slice(index).join('');
                index = lines.length;
                return rest;
            }),
            has_next_line: contextFunction((file, callLine) => {
                expectOpen(!closed, 'istream.has_next_line()', file, callLine);
                return index < lines.length;
            }),
            close: () => {
                closed = true;
            },
        };
        return stream;
    }
    function createOutputFile(filePath, sourceFile, line) {
        const parent = runtimeDirname(filePath);
        if (!fileSystem.exists(parent)) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for writing: directory does not exist`);
        }
        if (!runtimeIsDirectory(fileSystem, parent, sourceFile, line, 'writing')) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for writing: parent path is not a directory`);
        }
        try {
            fileSystem.writeText(filePath, '');
        }
        catch (error) {
            throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for writing: ${errorMessage(error)}`);
        }
        let closed = false;
        const stream = {
            __idylliumType: 'file.ostream',
            write_line: contextFunction(async (...rawArgs) => {
                const { values, file, line: callLine } = splitContextArgs(rawArgs);
                expectOpen(!closed, 'ostream.write_line()', file, callLine);
                fileSystem.appendText(filePath, await formatConsoleValues(values));
            }),
            close: () => {
                closed = true;
            },
        };
        return stream;
    }
    const core = {
        divide(left, right, file, line) {
            if (right === 0)
                throw new IdylliumRuntimeError(file, line, 'division by zero');
            return left / right;
        },
        div(left, right, file, line) {
            if (right === 0)
                throw new IdylliumRuntimeError(file, line, 'division by zero');
            return Math.trunc(left / right);
        },
        mod(left, right, file, line) {
            if (right === 0)
                throw new IdylliumRuntimeError(file, line, 'division by zero');
            return left % right;
        },
        to_int(value, file, line) {
            if (typeof value === 'number') {
                return Math.trunc(finiteNumber(value, "'to_int' value", file, line));
            }
            if (typeof value === 'string') {
                return parseIntegerText(value, "'to_int'", file, line);
            }
            throw new IdylliumRuntimeError(file, line, `'to_int' cannot convert '${String(value)}' to int`);
        },
        to_float(value, file, line) {
            if (typeof value === 'number')
                return finiteNumber(value, "'to_float' value", file, line);
            if (typeof value === 'string') {
                return parseFloatText(value, "'to_float'", file, line);
            }
            throw new IdylliumRuntimeError(file, line, `'to_float' cannot convert '${String(value)}' to float`);
        },
        to_string(value) {
            return formatForConsole(value, precision);
        },
    };
    const array = {
        create(size, defaultFactory, dynamic) {
            return IdylliumArray.create(size, defaultFactory, dynamic);
        },
        from(values, dynamic, staticSize, defaultFactory) {
            return IdylliumArray.from(values, dynamic, staticSize, defaultFactory);
        },
        get(value, index, file, line) {
            if (typeof value === 'string')
                return stringCharAt(value, index, file, line);
            return expectArray(value, file, line).get(index, file, line);
        },
        set(value, index, item, file, line) {
            if (typeof value === 'string') {
                throw new IdylliumRuntimeError(file, line, 'string characters are read-only');
            }
            expectArray(value, file, line).set(index, item, file, line);
        },
        max(value, file, line) {
            const values = numericValues(value, 'max', file, line);
            return Math.max(...values);
        },
        min(value, file, line) {
            const values = numericValues(value, 'min', file, line);
            return Math.min(...values);
        },
        sum(value, file, line) {
            return numericValues(value, 'sum', file, line).reduce((total, item) => total + item, 0);
        },
        avg(value, file, line) {
            const values = numericValues(value, 'avg', file, line);
            return values.reduce((total, item) => total + item, 0) / values.length;
        },
    };
    const types = {
        cast(value, typeName) {
            return castTypesValue(value, typeName);
        },
        to_bin(value, typeName) {
            return typesToBin(value, typeName);
        },
        to_hex(value, typeName) {
            return typesToHex(value, typeName);
        },
    };
    return {
        console: {
            async write(...values) {
                io.write(await formatConsoleValues(values));
            },
            async writeln(...values) {
                io.write(`${await formatConsoleValues(values)}\n`);
            },
            async clear() {
                io.clear();
            },
            async get_int(file = 'console', line = 0) {
                const inputText = await io.readLine();
                const normalized = inputText.trim();
                if (!/^[+-]?\d+$/u.test(normalized)) {
                    throw new IdylliumRuntimeError(file, line, `cannot convert input to 'int' (expected integer, got ${JSON.stringify(inputText)})`);
                }
                return Number.parseInt(normalized, 10);
            },
            async get_float(file = 'console', line = 0) {
                const inputText = await io.readLine();
                const normalized = inputText.trim();
                if (!/^[+-]?(?:(?:\d+(?:\.\d*)?)|(?:\.\d+))$/u.test(normalized)) {
                    throw new IdylliumRuntimeError(file, line, `cannot convert input to 'float' (expected number, got ${JSON.stringify(inputText)})`);
                }
                return Number.parseFloat(normalized);
            },
            async get_string() {
                return io.readLine();
            },
            async set_precision(file, line, digits) {
                precision = precisionDigits(digits, 'console.set_precision()', file, line);
            },
        },
        core,
        array,
        types,
        modules: {
            math: {
                pi: Math.PI,
                e: Math.E,
                abs: contextFunction((value, file, line) => Math.abs(finiteNumber(value, 'math.abs() value', file, line))),
                sqrt: contextFunction((value, file, line) => {
                    const number = finiteNumber(value, 'math.sqrt() value', file, line);
                    if (number < 0)
                        throw new IdylliumRuntimeError(file, line, `math.sqrt() expects a non-negative number, got ${number}`);
                    return Math.sqrt(number);
                }),
                round: contextFunction((value, digitsOrFile, fileOrLine, maybeLine) => {
                    const context = optionalNumberContext(digitsOrFile, fileOrLine, maybeLine);
                    return roundWithPrecision(value, context.value, context.file, context.line);
                }),
                floor: contextFunction((value, digitsOrFile, fileOrLine, maybeLine) => {
                    const context = optionalNumberContext(digitsOrFile, fileOrLine, maybeLine);
                    return floorWithPrecision(value, context.value, context.file, context.line);
                }),
                ceil: contextFunction((value, digitsOrFile, fileOrLine, maybeLine) => {
                    const context = optionalNumberContext(digitsOrFile, fileOrLine, maybeLine);
                    return ceilWithPrecision(value, context.value, context.file, context.line);
                }),
                pow: contextFunction((value, power, file, line) => {
                    const result = Math.pow(finiteNumber(value, 'math.pow() value', file, line), finiteNumber(power, 'math.pow() power', file, line));
                    return finiteMathResult(result, 'math.pow()', file, line);
                }),
                clamp: contextFunction((min, value, max, file, line) => {
                    const lower = finiteNumber(min, 'math.clamp() min', file, line);
                    const current = finiteNumber(value, 'math.clamp() value', file, line);
                    const upper = finiteNumber(max, 'math.clamp() max', file, line);
                    if (lower > upper) {
                        throw new IdylliumRuntimeError(file, line, `math.clamp() min must be less than or equal to max (got min ${lower}, max ${upper})`);
                    }
                    return Math.min(upper, Math.max(lower, current));
                }),
                sin: contextFunction((radians, file, line) => Math.sin(finiteNumber(radians, 'math.sin() radians', file, line))),
                cos: contextFunction((radians, file, line) => Math.cos(finiteNumber(radians, 'math.cos() radians', file, line))),
                tan: contextFunction((radians, file, line) => finiteMathResult(Math.tan(finiteNumber(radians, 'math.tan() radians', file, line)), 'math.tan()', file, line)),
                asin: contextFunction((value, file, line) => {
                    const number = rangeNumber(value, 'math.asin() value', -1, 1, file, line);
                    return Math.asin(number);
                }),
                acos: contextFunction((value, file, line) => {
                    const number = rangeNumber(value, 'math.acos() value', -1, 1, file, line);
                    return Math.acos(number);
                }),
                log: contextFunction((value, file, line) => {
                    const number = finiteNumber(value, 'math.log() value', file, line);
                    if (number <= 0)
                        throw new IdylliumRuntimeError(file, line, `math.log() expects a positive number, got ${number}`);
                    return Math.log(number);
                }),
                log10: contextFunction((value, file, line) => {
                    const number = finiteNumber(value, 'math.log10() value', file, line);
                    if (number <= 0)
                        throw new IdylliumRuntimeError(file, line, `math.log10() expects a positive number, got ${number}`);
                    return Math.log10(number);
                }),
                to_radians: contextFunction((degrees, file, line) => finiteNumber(degrees, 'math.to_radians() degrees', file, line) * Math.PI / 180),
                to_degrees: contextFunction((radians, file, line) => finiteNumber(radians, 'math.to_degrees() radians', file, line) * 180 / Math.PI),
            },
            random: {
                create_int: contextFunction((min, max, file, line) => {
                    const low = integerNumber(min, 'random.create_int() min', file, line);
                    const high = integerNumber(max, 'random.create_int() max', file, line);
                    if (low > high) {
                        throw new IdylliumRuntimeError(file, line, `random.create_int() min must be less than or equal to max (got min ${low}, max ${high})`);
                    }
                    return Math.floor(randomUnit() * (high - low + 1)) + low;
                }),
                create_float: contextFunction((min, max, file, line) => {
                    const low = finiteNumber(min, 'random.create_float() min', file, line);
                    const high = finiteNumber(max, 'random.create_float() max', file, line);
                    if (low >= high) {
                        throw new IdylliumRuntimeError(file, line, `random.create_float() min must be less than max (got min ${low}, max ${high})`);
                    }
                    return randomUnit() * (high - low) + low;
                }),
                set_seed: contextFunction((seed, file, line) => {
                    const value = integerNumber(seed, 'random.set_seed() seed', file, line);
                    if (value < 0)
                        throw new IdylliumRuntimeError(file, line, `random.set_seed() seed must be non-negative, got ${value}`);
                    randomSeed = value >>> 0;
                }),
            },
            time: {
                sleep: contextFunction((seconds, file, line) => {
                    const duration = finiteNumber(seconds, 'time.sleep() seconds', file, line);
                    if (duration < 0) {
                        throw new IdylliumRuntimeError(file, line, `time.sleep() seconds must be non-negative, got ${duration}`);
                    }
                    return waitForRuntimeDelay(duration * 1000, file, line);
                }),
                now: contextFunction(() => IdylliumTimeStamp.now()),
                from_unix: contextFunction((seconds, file, line) => IdylliumTimeStamp.fromUnix(seconds, file, line)),
            },
            file: {
                exists: contextFunction((targetPath, file, line) => {
                    const requestedPath = stringArgument(targetPath, 'file.exists() path', file, line);
                    return fileSystem.exists(fileSystem.resolvePath(requestedPath, file));
                }),
                open: contextFunction((targetPath, mode, file, line) => {
                    const requestedPath = stringArgument(targetPath, 'file.open() path', file, line);
                    const openMode = stringArgument(mode, 'file.open() mode', file, line);
                    const resolvedPath = fileSystem.resolvePath(requestedPath, file);
                    if (openMode === 'read')
                        return createInputFile(resolvedPath, file, line);
                    if (openMode === 'write')
                        return createOutputFile(resolvedPath, file, line);
                    throw new IdylliumRuntimeError(file, line, `file.open() mode must be 'read' or 'write', got '${openMode}'`);
                }),
            },
            types: {
                from_bin: contextFunction((bits, typeName, file, line) => typesFromBin(bits, typeName, file, line)),
                from_hex: contextFunction((hex, typeName, file, line) => typesFromHex(hex, typeName, file, line)),
            },
            encoding: {
                list_encodings: contextFunction(() => IdylliumArray.from(['ascii', 'utf-8', 'windows-1251', 'koi8-r'], true, null, () => '')),
                char_to_int: contextFunction((character, encoding, file, line) => (encodingCharToInt(character, encoding, file, line))),
                int_to_char: contextFunction((code, encoding, file, line) => (encodingIntToChar(code, encoding, file, line))),
                encode: contextFunction((text, encoding, file, line) => (IdylliumArray.from(encodingEncode(text, encoding, file, line), true, null, () => 0))),
                decode: contextFunction((codes, encoding, file, line) => (encodingDecode(codes, encoding, file, line))),
            },
            json: {
                is_valid: contextFunction((text, file, line) => {
                    const source = stringArgument(text, 'json.is_valid() text', file, line);
                    try {
                        JSON.parse(source);
                        return true;
                    }
                    catch {
                        return false;
                    }
                }),
                parse: contextFunction((text, file, line) => (parseJsonValue(stringArgument(text, 'json.parse() text', file, line), file, line))),
                Value: contextFunction((valueOrFile, fileOrLine, maybeLine) => {
                    if (maybeLine === undefined) {
                        return createJsonValue(undefined, valueOrFile, fileOrLine);
                    }
                    return createJsonValue(valueOrFile, fileOrLine, maybeLine);
                }),
                NULL: createJsonValue(),
            },
            audio: {},
            gui: {},
            colors: {
                RGB: contextFunction((red, green, blue, file, line) => IdylliumColor.RGB(red, green, blue, file, line)),
                RGBA: contextFunction((red, green, blue, alpha, file, line) => IdylliumColor.RGBA(red, green, blue, alpha, file, line)),
                HEX: contextFunction((value, file, line) => IdylliumColor.HEX(value, file, line)),
                HSL: contextFunction((hue, saturation, lightness, file, line) => IdylliumColor.HSL(hue, saturation, lightness, file, line)),
                BLACK: IdylliumColor.RGB(0, 0, 0),
                WHITE: IdylliumColor.RGB(255, 255, 255),
                RED: IdylliumColor.RGB(255, 0, 0),
                GREEN: IdylliumColor.RGB(0, 255, 0),
                BLUE: IdylliumColor.RGB(0, 0, 255),
                TRANSPARENT: IdylliumColor.RGBA(0, 0, 0, 0),
            },
        },
        createObject(moduleName, typeName) {
            throwIfRuntimeStopped('', 0);
            return createPlainRuntimeObject(moduleName, typeName, runtimeObjects);
        },
        setProperty(target, propertyName, value, file, line) {
            throwIfRuntimeStopped(file, line);
            if (target === null || typeof target !== 'object') {
                throw new IdylliumRuntimeError(file, line, `cannot set property '${propertyName}' on '${runtimeTypeName(target)}'`);
            }
            const obj = target;
            const setters = runtimePropertySetters(obj);
            const setter = setters[propertyName];
            if (setter) {
                setter(value, file, line);
                return value;
            }
            obj[propertyName] = value;
            return value;
        },
        callModuleFunction(moduleName, functionName, args, file, line) {
            throwIfRuntimeStopped(file, line);
            const module = this.modules[moduleName];
            if (!module)
                throw new IdylliumRuntimeError(file, line, `module '${moduleName}' was not found`);
            const fn = module[functionName];
            if (typeof fn !== 'function') {
                throw new IdylliumRuntimeError(file, line, `module '${moduleName}' has no function '${functionName}'`);
            }
            const callable = fn;
            return callable.__idylliumPassContext ? callable(...args, file, line) : callable(...args);
        },
        callMethod(target, methodName, args, file, line) {
            throwIfRuntimeStopped(file, line);
            if (target instanceof IdylliumArray) {
                return target.callMethod(methodName, args, file, line);
            }
            if (typeof target === 'string') {
                return callStringMethod(target, methodName, args, file, line);
            }
            if (target !== null && typeof target === 'object') {
                const method = target[methodName];
                if (typeof method === 'function') {
                    const callable = method;
                    return callable.__idylliumPassContext ? callable.apply(target, [...args, file, line]) : callable.apply(target, [...args]);
                }
            }
            throw new IdylliumRuntimeError(file, line, `object has no method '${methodName}'`);
        },
        getOutput() {
            return output;
        },
        getAudio() {
            return runtimeObjects.audio.map(audioSnapshot);
        },
        getCanvases() {
            return runtimeObjects.canvases.map(canvasSnapshot);
        },
        getWindows() {
            return runtimeObjects.windows.map(windowSnapshot);
        },
        getModals() {
            return runtimeObjects.modals.map(modalSnapshot);
        },
        async stepGui(deltaTime = 0) {
            throwIfRuntimeStopped('', 0);
            for (const timer of runtimeObjects.timers) {
                await stepGuiTimer(timer, deltaTime);
            }
            for (const canvas of runtimeObjects.canvases) {
                const onUpdate = canvas.on_update;
                if (typeof onUpdate === 'function') {
                    canvas.__commands = [];
                    await onUpdate(canvas, deltaTime);
                }
            }
        },
        async dispatchGuiEvent(canvasId, eventName, payload) {
            throwIfRuntimeStopped('', 0);
            const target = runtimeObjects.objects.find((item) => item.__idylliumObjectId === canvasId);
            if (!target)
                return;
            applyGuiEventPayload(target, eventName, payload, runtimeObjects);
            const callbackName = guiCallbackName(target, eventName);
            if (!callbackName)
                return;
            const callback = target[callbackName];
            if (typeof callback !== 'function')
                return;
            if (target.__idylliumType === 'gui.Canvas') {
                await callback(target, guiEventObject(eventName, payload));
                return;
            }
            await callback(target);
        },
    };
    function randomUnit() {
        if (randomSeed === null)
            return Math.random();
        randomSeed = (Math.imul(randomSeed, 1664525) + 1013904223) >>> 0;
        return randomSeed / 0x100000000;
    }
    function waitForRuntimeDelay(milliseconds, file, line) {
        const signal = options.abortSignal;
        if (!signal)
            return new Promise((resolve) => setTimeout(resolve, milliseconds));
        if (signal.aborted)
            return Promise.reject(runtimeStoppedError(file, line));
        return new Promise((resolve, reject) => {
            let timer = null;
            let settled = false;
            const cleanup = () => {
                if (timer !== null)
                    clearTimeout(timer);
                signal.removeEventListener?.('abort', onAbort);
            };
            const settle = (error) => {
                if (settled)
                    return;
                settled = true;
                cleanup();
                if (error)
                    reject(error);
                else
                    resolve();
            };
            const onAbort = () => settle(runtimeStoppedError(file, line));
            timer = setTimeout(() => settle(), milliseconds);
            signal.addEventListener?.('abort', onAbort, { once: true });
        });
    }
    function throwIfRuntimeStopped(file, line) {
        if (options.abortSignal?.aborted)
            throw runtimeStoppedError(file, line);
    }
    function runtimeStoppedError(file, line) {
        return new IdylliumRuntimeError(file || 'main.idyl', line || 1, 'program was stopped');
    }
}
function expectArray(value, file, line) {
    if (value instanceof IdylliumArray)
        return value;
    throw new IdylliumRuntimeError(file, line, `expected array, got '${String(value)}'`);
}
function numericValues(value, functionName, file, line) {
    const array = expectArray(value, file, line);
    const values = array.values();
    if (values.length === 0) {
        throw new IdylliumRuntimeError(file, line, `'${functionName}' cannot be used with an empty array`);
    }
    for (const item of values) {
        if (typeof item !== 'number') {
            throw new IdylliumRuntimeError(file, line, `'${functionName}' expects a numeric array`);
        }
    }
    return values;
}
function callStringMethod(value, methodName, args, file, line) {
    switch (methodName) {
        case 'length':
            return Array.from(value).length;
        case 'contains':
            return findInString(value, searchText(args[0], methodName, file, line)) >= 0;
        case 'find':
            return findInString(value, searchText(args[0], methodName, file, line));
        case 'count':
            return countInString(value, searchText(args[0], methodName, file, line));
        case 'is_int':
            return /^[+-]?\d+$/u.test(value.trim());
        case 'is_float':
            return /^[+-]?(?:\d+\.\d+|\d+\.|\.\d+)$/u.test(value.trim());
        case 'to_upper':
            return value.toLocaleUpperCase();
        case 'to_lower':
            return value.toLocaleLowerCase();
        case 'substring':
            return substringByCharacters(value, intArgument(args[0], 'substring start', file, line), intArgument(args[1], 'substring length', file, line), file, line);
        case 'replace':
            return replaceAllText(value, stringArgument(args[0], 'replace old_text', file, line), stringArgument(args[1], 'replace new_text', file, line), file, line);
        case 'split':
            return splitString(value, stringArgument(args[0], 'split separator', file, line));
        case 'trim':
            return value.trim();
        default:
            throw new IdylliumRuntimeError(file, line, `type 'string' has no method '${methodName}'`);
    }
}
function stringCharAt(value, index, file, line) {
    const chars = Array.from(value);
    if (!Number.isInteger(index)) {
        throw new IdylliumRuntimeError(file, line, `string index must be int, got '${String(index)}'`);
    }
    if (index < 0 || index >= chars.length) {
        throw new IdylliumRuntimeError(file, line, `string index ${index} out of bounds (length ${chars.length}, valid indices ${validRange(chars.length)})`);
    }
    return chars[index];
}
function searchText(value, methodName, file, line) {
    if (typeof value === 'string')
        return value;
    throw new IdylliumRuntimeError(file, line, `string method '${methodName}' expects string or char`);
}
function stringArgument(value, argumentName, file, line) {
    if (typeof value === 'string')
        return value;
    throw new IdylliumRuntimeError(file, line, `${argumentName} must be string`);
}
function resolveRuntimePath(requestedPath, sourceFile) {
    if (nodePath.isAbsolute(requestedPath))
        return nodePath.normalize(requestedPath);
    const base = sourceFile.trim() === '' ? process.cwd() : nodePath.dirname(sourceFile);
    return nodePath.resolve(base, requestedPath);
}
function createNodeRuntimeFileSystem() {
    return {
        resolvePath: resolveRuntimePath,
        exists(filePath) {
            return nodeFs.existsSync(filePath);
        },
        isFile(filePath) {
            return nodeFs.statSync(filePath).isFile();
        },
        isDirectory(filePath) {
            return nodeFs.statSync(filePath).isDirectory();
        },
        readText(filePath) {
            return nodeFs.readFileSync(filePath, 'utf8');
        },
        writeText(filePath, text) {
            nodeFs.writeFileSync(filePath, text, 'utf8');
        },
        appendText(filePath, text) {
            nodeFs.appendFileSync(filePath, text, 'utf8');
        },
        resourceUri(filePath) {
            return filePath;
        },
    };
}
function runtimeDirname(filePath) {
    return filePath.includes('\\') ? memoryDirname(filePath) : nodePath.dirname(filePath);
}
function runtimeIsFile(fileSystem, filePath, sourceFile, line, mode) {
    try {
        return fileSystem.isFile(filePath);
    }
    catch (error) {
        throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for ${mode}: ${errorMessage(error)}`);
    }
}
function runtimeIsDirectory(fileSystem, filePath, sourceFile, line, mode) {
    try {
        return fileSystem.isDirectory(filePath);
    }
    catch (error) {
        throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for ${mode}: ${errorMessage(error)}`);
    }
}
function normalizeMemoryPath(value, base = '/workspace') {
    const raw = value.replace(/\\/gu, '/');
    const parts = (raw.startsWith('/') ? raw : `${base}/${raw}`).split('/');
    const normalized = [];
    for (const part of parts) {
        if (part === '' || part === '.')
            continue;
        if (part === '..') {
            normalized.pop();
            continue;
        }
        normalized.push(part);
    }
    return `/${normalized.join('/')}`;
}
function memoryDirname(filePath) {
    const normalized = normalizeMemoryPath(filePath);
    if (normalized === '/')
        return '/';
    const index = normalized.lastIndexOf('/');
    if (index <= 0)
        return '/';
    return normalized.slice(0, index);
}
function addMemoryDirectory(directories, directory) {
    const normalized = normalizeMemoryPath(directory);
    if (directories.has(normalized))
        return;
    const parent = memoryDirname(normalized);
    if (parent !== normalized)
        addMemoryDirectory(directories, parent);
    directories.add(normalized);
}
function splitFileLines(source) {
    if (source.length === 0)
        return [];
    const matches = source.match(/.*(?:\r\n|\n|$)/gu) ?? [];
    return matches.filter((line, index) => line.length > 0 || index < matches.length - 1);
}
function splitContextArgs(args) {
    const file = args[args.length - 2];
    const line = args[args.length - 1];
    return {
        values: args.slice(0, -2),
        file: typeof file === 'string' ? file : 'runtime',
        line: typeof line === 'number' ? line : 0,
    };
}
function expectOpen(isOpen, operationName, file, line) {
    if (!isOpen) {
        throw new IdylliumRuntimeError(file, line, `${operationName} cannot be used after close()`);
    }
}
function errorMessage(error) {
    return error instanceof Error ? error.message : String(error);
}
function intArgument(value, argumentName, file, line) {
    if (typeof value === 'number' && Number.isInteger(value))
        return value;
    throw new IdylliumRuntimeError(file, line, `${argumentName} must be int`);
}
function findInString(value, needle) {
    const chars = Array.from(value);
    const needleChars = Array.from(needle);
    if (needleChars.length === 0)
        return 0;
    for (let i = 0; i <= chars.length - needleChars.length; i++) {
        if (needleChars.every((char, offset) => chars[i + offset] === char)) {
            return i;
        }
    }
    return -1;
}
function countInString(value, needle) {
    const chars = Array.from(value);
    const needleChars = Array.from(needle);
    if (needleChars.length === 0)
        return 0;
    let count = 0;
    let index = 0;
    while (index <= chars.length - needleChars.length) {
        if (needleChars.every((char, offset) => chars[index + offset] === char)) {
            count++;
            index += needleChars.length;
        }
        else {
            index++;
        }
    }
    return count;
}
function substringByCharacters(value, start, length, file, line) {
    const chars = Array.from(value);
    if (start < 0) {
        throw new IdylliumRuntimeError(file, line, `substring start must be non-negative, got ${start}`);
    }
    if (length < 0) {
        throw new IdylliumRuntimeError(file, line, `substring length must be non-negative, got ${length}`);
    }
    if (start > chars.length) {
        throw new IdylliumRuntimeError(file, line, `substring start ${start} out of bounds (length ${chars.length}, valid indices ${validRange(chars.length)})`);
    }
    return chars.slice(start, start + length).join('');
}
function replaceAllText(value, oldText, newText, file, line) {
    if (oldText.length === 0) {
        throw new IdylliumRuntimeError(file, line, 'replace old_text must not be empty');
    }
    return value.split(oldText).join(newText);
}
function splitString(value, separator) {
    const parts = separator.length === 0 ? Array.from(value) : value.split(separator);
    return IdylliumArray.from(parts, true, null, () => '');
}
const WINDOWS_1251_ENCODING = buildSingleByteEncoding([
    [0xA8, '\u0401'],
    [0xB8, '\u0451'],
    ...sequentialEntries(0xC0, '\u0410\u0411\u0412\u0413\u0414\u0415\u0416\u0417\u0418\u0419\u041A\u041B\u041C\u041D\u041E\u041F\u0420\u0421\u0422\u0423\u0424\u0425\u0426\u0427\u0428\u0429\u042A\u042B\u042C\u042D\u042E\u042F'),
    ...sequentialEntries(0xE0, '\u0430\u0431\u0432\u0433\u0434\u0435\u0436\u0437\u0438\u0439\u043A\u043B\u043C\u043D\u043E\u043F\u0440\u0441\u0442\u0443\u0444\u0445\u0446\u0447\u0448\u0449\u044A\u044B\u044C\u044D\u044E\u044F'),
]);
const KOI8_R_ENCODING = buildSingleByteEncoding([
    [0xA3, '\u0451'],
    [0xB3, '\u0401'],
    ...sequentialEntries(0xC0, '\u044E\u0430\u0431\u0446\u0434\u0435\u0444\u0433\u0445\u0438\u0439\u043A\u043B\u043C\u043D\u043E\u043F\u044F\u0440\u0441\u0442\u0443\u0436\u0432\u044C\u044B\u0437\u0448\u044D\u0449\u0447\u044A'),
    ...sequentialEntries(0xE0, '\u042E\u0410\u0411\u0426\u0414\u0415\u0424\u0413\u0425\u0418\u0419\u041A\u041B\u041C\u041D\u041E\u041F\u042F\u0420\u0421\u0422\u0423\u0416\u0412\u042C\u042B\u0417\u0428\u042D\u0429\u0427\u042A'),
]);
function encodingCharToInt(character, encoding, file, line) {
    const char = singleCharacter(character, 'encoding.char_to_int() character', file, line);
    const name = normalizeEncoding(encoding, file, line);
    if (name === 'ascii')
        return asciiCode(char, file, line);
    if (name === 'utf-8')
        return char.codePointAt(0) ?? 0;
    return singleByteCharToInt(char, name === 'windows-1251' ? WINDOWS_1251_ENCODING : KOI8_R_ENCODING, name, file, line);
}
function encodingIntToChar(code, encoding, file, line) {
    const value = integerNumber(code, 'encoding.int_to_char() code', file, line);
    const name = normalizeEncoding(encoding, file, line);
    if (name === 'ascii') {
        byteRange(value, 'encoding.int_to_char() code', 0, 127, file, line);
        return String.fromCodePoint(value);
    }
    if (name === 'utf-8') {
        if (value < 0 || value > 0x10FFFF) {
            throw new IdylliumRuntimeError(file, line, `encoding.int_to_char() code must be between 0 and 1114111, got ${value}`);
        }
        return String.fromCodePoint(value);
    }
    return singleByteIntToChar(value, name === 'windows-1251' ? WINDOWS_1251_ENCODING : KOI8_R_ENCODING, name, file, line);
}
function encodingEncode(text, encoding, file, line) {
    const value = stringArgument(text, 'encoding.encode() text', file, line);
    const name = normalizeEncoding(encoding, file, line);
    if (name === 'ascii')
        return Array.from(value).map((char) => asciiCode(char, file, line));
    if (name === 'utf-8')
        return [...nodeBuffer.from(value, 'utf8')];
    const table = name === 'windows-1251' ? WINDOWS_1251_ENCODING : KOI8_R_ENCODING;
    return Array.from(value).map((char) => singleByteCharToInt(char, table, name, file, line));
}
function encodingDecode(codes, encoding, file, line) {
    const array = expectArray(codes, file, line);
    const name = normalizeEncoding(encoding, file, line);
    const bytes = array.values().map((code) => integerNumber(code, 'encoding.decode() code', file, line));
    if (name === 'ascii') {
        return bytes.map((code) => {
            byteRange(code, 'encoding.decode() code', 0, 127, file, line);
            return String.fromCodePoint(code);
        }).join('');
    }
    if (name === 'utf-8') {
        for (const code of bytes) {
            byteRange(code, 'encoding.decode() code', 0, 255, file, line);
        }
        return nodeBuffer.from(bytes).toString('utf8');
    }
    const table = name === 'windows-1251' ? WINDOWS_1251_ENCODING : KOI8_R_ENCODING;
    return bytes.map((code) => singleByteIntToChar(code, table, name, file, line)).join('');
}
function normalizeEncoding(value, file, line) {
    const name = stringArgument(value, 'encoding name', file, line).toLowerCase();
    if (name === 'ascii')
        return 'ascii';
    if (name === 'utf-8' || name === 'utf8')
        return 'utf-8';
    if (name === 'windows-1251' || name === 'cp1251' || name === 'win1251')
        return 'windows-1251';
    if (name === 'koi8-r' || name === 'koi8r')
        return 'koi8-r';
    throw new IdylliumRuntimeError(file, line, `unknown encoding '${value}'`);
}
function singleCharacter(value, argumentName, file, line) {
    const text = stringArgument(value, argumentName, file, line);
    const chars = Array.from(text);
    if (chars.length !== 1) {
        throw new IdylliumRuntimeError(file, line, `${argumentName} must contain exactly one character, got ${JSON.stringify(text)}`);
    }
    return chars[0];
}
function asciiCode(char, file, line) {
    const code = char.codePointAt(0) ?? 0;
    if (code <= 127)
        return code;
    throw new IdylliumRuntimeError(file, line, `character '${char}' is not valid ASCII`);
}
function singleByteCharToInt(char, table, encoding, file, line) {
    const code = char.codePointAt(0) ?? 0;
    if (code <= 127)
        return code;
    const byte = table.charToByte.get(char);
    if (byte !== undefined)
        return byte;
    throw new IdylliumRuntimeError(file, line, `character '${char}' is not valid ${encoding}`);
}
function singleByteIntToChar(code, table, encoding, file, line) {
    byteRange(code, 'encoding code', 0, 255, file, line);
    if (code <= 127)
        return String.fromCodePoint(code);
    const char = table.byteToChar.get(code);
    if (char !== undefined)
        return char;
    throw new IdylliumRuntimeError(file, line, `byte ${code} is not valid ${encoding}`);
}
function byteRange(value, argumentName, min, max, file, line) {
    if (value < min || value > max) {
        throw new IdylliumRuntimeError(file, line, `${argumentName} must be between ${min} and ${max}, got ${value}`);
    }
    return value;
}
function buildSingleByteEncoding(entries) {
    const charToByte = new Map();
    const byteToChar = new Map();
    for (const [byte, char] of entries) {
        charToByte.set(char, byte);
        byteToChar.set(byte, char);
    }
    return { charToByte, byteToChar };
}
function sequentialEntries(start, chars) {
    return Array.from(chars).map((char, index) => [start + index, char]);
}
const RUNTIME_TYPES = {
    int8: { kind: 'integer', bits: 8, signed: true },
    uint8: { kind: 'integer', bits: 8, signed: false },
    int16: { kind: 'integer', bits: 16, signed: true },
    uint16: { kind: 'integer', bits: 16, signed: false },
    int32: { kind: 'integer', bits: 32, signed: true },
    uint32: { kind: 'integer', bits: 32, signed: false },
    float32: { kind: 'float', bits: 32, signed: true },
    float64: { kind: 'float', bits: 64, signed: true },
};
function castTypesValue(value, typeName) {
    const name = normalizeRuntimeTypesName(typeName, 'types', 0);
    const spec = RUNTIME_TYPES[name];
    const number = finiteNumber(value, `types.${name} value`, 'types', 0);
    if (spec.kind === 'float') {
        return name === 'float32' ? Math.fround(number) : number;
    }
    return wrapInteger(number, spec);
}
function typesToBin(value, typeName) {
    const name = normalizeRuntimeTypesName(typeName, 'types', 0);
    const spec = RUNTIME_TYPES[name];
    if (spec.kind === 'float') {
        return bytesToBinary(floatBytes(castTypesValue(value, name), name));
    }
    return integerToUnsigned(value, name).toString(2).padStart(spec.bits, '0');
}
function typesToHex(value, typeName) {
    const name = normalizeRuntimeTypesName(typeName, 'types', 0);
    const spec = RUNTIME_TYPES[name];
    if (spec.kind === 'float') {
        return bytesToHex(floatBytes(castTypesValue(value, name), name));
    }
    return integerToUnsigned(value, name).toString(16).padStart(spec.bits / 4, '0').toUpperCase();
}
function typesFromBin(bits, typeName, file, line) {
    const name = normalizeTypesName(typeName, file, line);
    const spec = RUNTIME_TYPES[name];
    const normalized = normalizedBinary(bits, spec.bits, file, line);
    if (spec.kind === 'float') {
        return floatFromBytes(binaryToBytes(normalized), name);
    }
    return castTypesValue(Number.parseInt(normalized, 2), name);
}
function typesFromHex(hex, typeName, file, line) {
    const name = normalizeTypesName(typeName, file, line);
    const spec = RUNTIME_TYPES[name];
    const normalized = normalizedHex(hex, spec.bits, file, line);
    if (spec.kind === 'float') {
        return floatFromBytes(hexToBytes(normalized), name);
    }
    return castTypesValue(Number.parseInt(normalized, 16), name);
}
function normalizeTypesName(value, file, line) {
    return normalizeRuntimeTypesName(stringArgument(value, 'types type name', file, line), file, line);
}
function normalizeRuntimeTypesName(value, file, line) {
    const name = value.trim().toLowerCase().replace(/^types\./u, '');
    if (Object.prototype.hasOwnProperty.call(RUNTIME_TYPES, name)) {
        return name;
    }
    throw new IdylliumRuntimeError(file, line, `unknown types numeric type '${value}'`);
}
function wrapInteger(value, spec) {
    const modulo = 2 ** spec.bits;
    let wrapped = ((Math.trunc(value) % modulo) + modulo) % modulo;
    if (spec.signed && wrapped >= 2 ** (spec.bits - 1)) {
        wrapped -= modulo;
    }
    return wrapped;
}
function integerToUnsigned(value, typeName) {
    const spec = RUNTIME_TYPES[typeName];
    const casted = castTypesValue(value, typeName);
    if (spec.kind !== 'integer')
        return casted;
    return casted < 0 ? casted + 2 ** spec.bits : casted;
}
function normalizedBinary(value, bits, file, line) {
    const text = stringArgument(value, 'types.from_bin() bits', file, line).trim();
    if (!/^[01]+$/u.test(text)) {
        throw new IdylliumRuntimeError(file, line, `types.from_bin() expects binary digits, got ${JSON.stringify(text)}`);
    }
    if (text.length > bits) {
        throw new IdylliumRuntimeError(file, line, `types.from_bin() expects at most ${bits} bits, got ${text.length}`);
    }
    return text.padStart(bits, '0');
}
function normalizedHex(value, bits, file, line) {
    const text = stringArgument(value, 'types.from_hex() hex', file, line).trim().replace(/^0x/iu, '');
    if (!/^[0-9a-fA-F]+$/u.test(text)) {
        throw new IdylliumRuntimeError(file, line, `types.from_hex() expects hexadecimal digits, got ${JSON.stringify(text)}`);
    }
    const digits = bits / 4;
    if (text.length > digits) {
        throw new IdylliumRuntimeError(file, line, `types.from_hex() expects at most ${digits} hex digits, got ${text.length}`);
    }
    return text.padStart(digits, '0').toUpperCase();
}
function binaryToBytes(bits) {
    const bytes = [];
    for (let i = 0; i < bits.length; i += 8) {
        bytes.push(Number.parseInt(bits.slice(i, i + 8), 2));
    }
    return bytes;
}
function hexToBytes(hex) {
    const bytes = [];
    for (let i = 0; i < hex.length; i += 2) {
        bytes.push(Number.parseInt(hex.slice(i, i + 2), 16));
    }
    return bytes;
}
function bytesToBinary(bytes) {
    return bytes.map((byte) => byte.toString(2).padStart(8, '0')).join('');
}
function bytesToHex(bytes) {
    return bytes.map((byte) => byte.toString(16).padStart(2, '0')).join('').toUpperCase();
}
function floatBytes(value, typeName) {
    if (typeName === 'float32') {
        const buffer = nodeBuffer.alloc(4);
        buffer.writeFloatBE(Math.fround(value), 0);
        return [...buffer];
    }
    const buffer = nodeBuffer.alloc(8);
    buffer.writeDoubleBE(value, 0);
    return [...buffer];
}
function floatFromBytes(bytes, typeName) {
    const buffer = nodeBuffer.from(bytes);
    if (typeName === 'float32')
        return Math.fround(buffer.readFloatBE(0));
    return buffer.readDoubleBE(0);
}
function roundWithPrecision(value, digits, file, line) {
    const number = finiteNumber(value, 'math.round() value', file, line);
    if (digits === undefined)
        return Math.round(number);
    const safeDigits = precisionDigits(digits, 'math.round() digits', file, line);
    const factor = 10 ** safeDigits;
    return Number((Math.round(number * factor) / factor).toFixed(safeDigits));
}
function floorWithPrecision(value, digits, file, line) {
    const number = finiteNumber(value, 'math.floor() value', file, line);
    if (digits === undefined)
        return Math.floor(number);
    const safeDigits = precisionDigits(digits, 'math.floor() digits', file, line);
    const factor = 10 ** safeDigits;
    return Number((Math.floor(number * factor) / factor).toFixed(safeDigits));
}
function ceilWithPrecision(value, digits, file, line) {
    const number = finiteNumber(value, 'math.ceil() value', file, line);
    if (digits === undefined)
        return Math.ceil(number);
    const safeDigits = precisionDigits(digits, 'math.ceil() digits', file, line);
    const factor = 10 ** safeDigits;
    return Number((Math.ceil(number * factor) / factor).toFixed(safeDigits));
}
function parseIntegerText(value, functionName, file, line) {
    const normalized = value.trim();
    if (!/^[+-]?\d+$/u.test(normalized)) {
        throw new IdylliumRuntimeError(file, line, `${functionName} cannot convert '${value}' to int`);
    }
    return Number.parseInt(normalized, 10);
}
function parseFloatText(value, functionName, file, line) {
    const normalized = value.trim();
    if (!/^[+-]?(?:(?:\d+(?:\.\d*)?)|(?:\.\d+))$/u.test(normalized)) {
        throw new IdylliumRuntimeError(file, line, `${functionName} cannot convert '${value}' to float`);
    }
    return Number.parseFloat(normalized);
}
function finiteNumber(value, argumentName, file, line) {
    if (typeof value === 'number' && Number.isFinite(value))
        return value;
    throw new IdylliumRuntimeError(file, line, `${argumentName} must be a finite number, got '${String(value)}'`);
}
function integerNumber(value, argumentName, file, line) {
    const number = finiteNumber(value, argumentName, file, line);
    if (Number.isInteger(number))
        return number;
    throw new IdylliumRuntimeError(file, line, `${argumentName} must be int, got ${number}`);
}
function precisionDigits(value, argumentName, file, line) {
    const digits = integerNumber(value, argumentName, file, line);
    if (digits < 0 || digits > 25) {
        throw new IdylliumRuntimeError(file, line, `${argumentName} must be between 0 and 25, got ${digits}`);
    }
    return digits;
}
function optionalNumberContext(valueOrFile, fileOrLine, maybeLine) {
    if (maybeLine === undefined) {
        return {
            value: undefined,
            file: valueOrFile,
            line: fileOrLine,
        };
    }
    return {
        value: valueOrFile,
        file: fileOrLine,
        line: maybeLine,
    };
}
function rangeNumber(value, argumentName, min, max, file, line) {
    const number = finiteNumber(value, argumentName, file, line);
    if (number < min || number > max) {
        throw new IdylliumRuntimeError(file, line, `${argumentName} must be between ${min} and ${max}, got ${number}`);
    }
    return number;
}
function finiteMathResult(value, functionName, file, line) {
    if (Number.isFinite(value))
        return value;
    throw new IdylliumRuntimeError(file, line, `${functionName} result is not a finite number`);
}
function validRange(length) {
    if (length === 0)
        return 'none';
    return `0-${length - 1}`;
}
function formatForConsole(value, precision) {
    if (value instanceof IdylliumArray)
        return value.toInspectString();
    if (isJsonRuntimeValue(value))
        return jsonSerialize(value, 0);
    if (typeof value === 'boolean')
        return value ? 'true' : 'false';
    if (typeof value === 'number' && precision !== null) {
        return Number(value.toFixed(precision)).toString();
    }
    return String(value);
}
function formatForInspect(value) {
    if (value instanceof IdylliumArray)
        return value.toInspectString();
    if (isJsonRuntimeValue(value))
        return jsonSerialize(value, 0);
    if (typeof value === 'string')
        return JSON.stringify(value);
    if (typeof value === 'boolean')
        return value ? 'true' : 'false';
    return String(value);
}
function runtimeStat(filePath, sourceFile, line, mode) {
    try {
        return nodeFs.statSync(filePath);
    }
    catch (error) {
        throw new IdylliumRuntimeError(sourceFile, line, `file.open() cannot open '${filePath}' for ${mode}: ${errorMessage(error)}`);
    }
}
function isPlainObject(value) {
    return typeof value === 'object' && value !== null && !Array.isArray(value);
}
function defineTrackedRuntimeProperty(obj, name, defaultValue) {
    const values = trackedRuntimePropertyValues(obj);
    values[name] = defaultValue;
    Object.defineProperty(obj, name, {
        enumerable: true,
        configurable: true,
        get() {
            return values[name];
        },
        set(value) {
            values[name] = value;
            explicitRuntimeProperties(obj).add(name);
        },
    });
}
function setTrackedRuntimePropertyDefault(obj, name, value) {
    const values = obj.__trackedPropertyValues;
    if (isPlainObject(values) && Object.prototype.hasOwnProperty.call(values, name)) {
        values[name] = value;
        return;
    }
    defineTrackedRuntimeProperty(obj, name, value);
}
function trackedRuntimePropertyValues(obj) {
    if (isPlainObject(obj.__trackedPropertyValues))
        return obj.__trackedPropertyValues;
    const values = {};
    Object.defineProperty(obj, '__trackedPropertyValues', {
        value: values,
        enumerable: false,
        configurable: true,
    });
    return values;
}
function explicitRuntimeProperties(obj) {
    if (obj.__explicitProperties instanceof Set)
        return obj.__explicitProperties;
    const properties = new Set();
    Object.defineProperty(obj, '__explicitProperties', {
        value: properties,
        enumerable: false,
        configurable: true,
    });
    return properties;
}
function runtimePropertySetters(obj) {
    if (isPlainObject(obj.__runtimePropertySetters))
        return obj.__runtimePropertySetters;
    const setters = {};
    Object.defineProperty(obj, '__runtimePropertySetters', {
        value: setters,
        enumerable: false,
        configurable: true,
    });
    return setters;
}
function defineValidatedRuntimeProperty(obj, name, defaultValue, validator) {
    const values = trackedRuntimePropertyValues(obj);
    values[name] = defaultValue;
    runtimePropertySetters(obj)[name] = (value, file, line) => {
        values[name] = validator(value, file, line);
        explicitRuntimeProperties(obj).add(name);
    };
    Object.defineProperty(obj, name, {
        enumerable: true,
        configurable: true,
        get() {
            return values[name];
        },
        set(value) {
            values[name] = validator(value, 'runtime', 0);
            explicitRuntimeProperties(obj).add(name);
        },
    });
}
function createPlainRuntimeObject(moduleName, typeName, state) {
    if (moduleName === 'json') {
        if (typeName === 'Value')
            return createJsonValue();
        if (typeName === 'Object')
            return createJsonObject();
        if (typeName === 'Array')
            return createJsonArray();
    }
    const obj = {
        __idylliumObjectId: state.nextObjectId++,
        __idylliumType: `${moduleName}.${typeName}`,
    };
    state.objects.push(obj);
    if (moduleName === 'gui') {
        initializeGuiObject(obj, typeName, state);
    }
    if (moduleName === 'drawable') {
        initializeDrawableObject(obj, typeName, state);
    }
    if (moduleName === 'audio') {
        initializeAudioObject(obj, typeName, state);
    }
    return obj;
}
function initializeGuiObject(obj, typeName, state) {
    if (isGuiWidget(typeName)) {
        obj.x = 0;
        obj.y = 0;
        const size = defaultGuiWidgetSize(typeName);
        obj.width = size.width;
        obj.height = size.height;
        obj.visible = true;
        defineTrackedRuntimeProperty(obj, 'text_color', colorBlack());
        defineTrackedRuntimeProperty(obj, 'background_color', colorTransparent());
    }
    if (typeName === 'Window' || typeName === 'Frame') {
        obj.__children = [];
        obj.add_child = contextFunction((child, file, line) => {
            if (!isRuntimeObject(child)) {
                throw new IdylliumRuntimeError(file, line, `add_child() expects gui widget, got '${String(child)}'`);
            }
            child.__parent = obj;
            obj.__children.push(child);
        });
    }
    if (typeName === 'Window') {
        obj.title = '';
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorWhite());
        obj.show = async () => {
            obj.__shown = true;
            for (const child of obj.__children ?? []) {
                await initializeGuiChild(child);
            }
        };
        state.windows.push(obj);
    }
    if (typeName === 'Canvas') {
        obj.framerate_limit = 60;
        obj.__commands = [];
        obj.clear = contextFunction((_file, _line) => {
            canvasCommands(obj).push({ kind: 'clear', color: '#000000' });
        });
        obj.fill = contextFunction((color, file, line) => {
            canvasCommands(obj).push({ kind: 'fill', color: colorToCss(color, 'Canvas.fill() color', file, line) });
        });
        obj.draw = contextFunction((target, file, line) => {
            if (!isDrawableObject(target)) {
                throw new IdylliumRuntimeError(file, line, `Canvas.draw() expects drawable object, got '${runtimeTypeName(target)}'`);
            }
            canvasCommands(obj).push({ kind: 'draw', object: drawableSnapshot(target) });
        });
        obj.to_string = () => `gui.Canvas(commands: ${canvasCommands(obj).length})`;
        state.canvases.push(obj);
    }
    if (typeName === 'Timer') {
        obj.interval = 1000;
        obj.__running = false;
        obj.__elapsedMs = 0;
        obj.start = () => {
            obj.__running = true;
            obj.__elapsedMs = 0;
        };
        obj.stop = () => {
            obj.__running = false;
        };
        state.timers.push(obj);
    }
    if (typeName === 'Label') {
        obj.text = '';
        obj.font_size = 12;
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorTransparent());
        obj.border_color = colorTransparent();
        obj.color = '';
    }
    if (typeName === 'Button') {
        obj.text = '';
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorLightGray());
        obj.border_color = colorGray();
    }
    if (typeName === 'Frame') {
        obj.title = '';
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorVeryLightGray());
        obj.border_color = colorGray();
        obj.border_width = 1;
    }
    if (typeName === 'Image') {
        obj.path = '';
        obj.resolved_path = '';
        obj.resource_uri = '';
        obj.is_loaded = false;
        obj.resize_mode = 'fit';
        obj.load_from_file = contextFunction((targetPath, file, line) => {
            const requestedPath = stringArgument(targetPath, 'Image.load_from_file() path', file, line);
            const resolvedPath = state.fileSystem.resolvePath(requestedPath, file);
            if (!state.fileSystem.exists(resolvedPath)) {
                throw new IdylliumRuntimeError(file, line, `Image.load_from_file() cannot load '${requestedPath}': file does not exist`);
            }
            if (!runtimeIsFile(state.fileSystem, resolvedPath, file, line, 'reading')) {
                throw new IdylliumRuntimeError(file, line, `Image.load_from_file() cannot load '${requestedPath}': path is not a file`);
            }
            obj.path = requestedPath;
            obj.resolved_path = resolvedPath;
            obj.resource_uri = state.fileSystem.resourceUri?.(resolvedPath) ?? '';
            obj.is_loaded = true;
        });
    }
    if (typeName === 'LineEdit') {
        obj.text = '';
        obj.placeholder = '';
        obj.font_size = 12;
        obj.echo_mode = 'normal';
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorWhite());
        obj.border_color = colorGray();
    }
    if (typeName === 'TextEdit') {
        obj.text = '';
        obj.placeholder = '';
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorWhite());
        obj.border_color = colorGray();
    }
    if (typeName === 'ProgressBar') {
        obj.value = 0;
        obj.min = 0;
        obj.max = 100;
        setTrackedRuntimePropertyDefault(obj, 'text_color', colorBlack());
        setTrackedRuntimePropertyDefault(obj, 'background_color', colorVeryLightGray());
        setTrackedRuntimePropertyDefault(obj, 'foreground_color', colorBlue());
        setTrackedRuntimePropertyDefault(obj, 'fill_color', colorBlue());
        obj.border_color = colorGray();
    }
    if (typeName === 'SpinBox' || typeName === 'Slider') {
        obj.value = 0;
        obj.min = 0;
        obj.max = 100;
        obj.step = 1;
    }
    if (typeName === 'FloatSpinBox') {
        obj.value = 0;
        obj.min = 0;
        obj.max = 100;
        obj.step = 1;
    }
    if (typeName === 'CheckBox') {
        obj.text = '';
        obj.is_checked = false;
    }
    if (typeName === 'RadioButton') {
        obj.text = '';
        obj.group = '';
        obj.is_selected = false;
    }
    if (typeName === 'ComboBox') {
        const items = [];
        obj.__items = items;
        obj.__selected_index = -1;
        defineComboBoxProperties(obj, items);
        obj.add_item = (text) => {
            items.push(text);
            if (obj.selected_index === -1) {
                obj.selected_index = 0;
            }
        };
        obj.clear_items = () => {
            items.length = 0;
            obj.selected_index = -1;
        };
    }
    if (typeName === 'Modal') {
        obj.title = '';
        obj.message = '';
        obj.confirm_text = 'OK';
        obj.cancel_text = 'Cancel';
        obj.__input_value = '';
        obj.show_alert = () => showModal(obj, 'alert', state);
        obj.show_confirm = () => showModal(obj, 'confirm', state);
        obj.show_input = () => {
            obj.__input_value = '';
            showModal(obj, 'input', state);
        };
        obj.get_input_value = () => obj.__input_value;
    }
}
function defaultGuiWidgetSize(typeName) {
    switch (typeName) {
        case 'Window':
            return { width: 640, height: 420 };
        case 'Canvas':
            return { width: 300, height: 150 };
        case 'Label':
            return { width: 120, height: 24 };
        case 'Button':
            return { width: 120, height: 32 };
        case 'Frame':
            return { width: 220, height: 140 };
        case 'Image':
            return { width: 160, height: 120 };
        case 'LineEdit':
            return { width: 180, height: 28 };
        case 'TextEdit':
            return { width: 240, height: 120 };
        case 'ProgressBar':
            return { width: 200, height: 24 };
        case 'SpinBox':
            return { width: 100, height: 28 };
        case 'FloatSpinBox':
            return { width: 120, height: 28 };
        case 'Slider':
            return { width: 200, height: 28 };
        case 'CheckBox':
        case 'RadioButton':
            return { width: 180, height: 24 };
        case 'ComboBox':
            return { width: 180, height: 30 };
        default:
            return { width: 120, height: 32 };
    }
}
function showModal(obj, mode, state) {
    obj.__modalMode = mode;
    if (!state.modals.includes(obj)) {
        state.modals.push(obj);
    }
}
function defineComboBoxProperties(obj, items) {
    Object.defineProperty(obj, 'selected_index', {
        enumerable: true,
        configurable: true,
        get() {
            return obj.__selected_index;
        },
        set(value) {
            obj.__selected_index = typeof value === 'number' && Number.isFinite(value) ? Math.trunc(value) : -1;
        },
    });
    Object.defineProperty(obj, 'selected_text', {
        enumerable: true,
        configurable: true,
        get() {
            const index = obj.__selected_index;
            return typeof index === 'number' && index >= 0 ? items[index] ?? '' : '';
        },
        set(_value) {
            // selected_text is derived from selected_index and ComboBox items.
        },
    });
}
function initializeDrawableObject(obj, typeName, state) {
    if (typeName === 'Rectangle') {
        obj.__idylliumDrawable = true;
        obj.x = 0;
        obj.y = 0;
        obj.width = 0;
        obj.height = 0;
        obj.rotation = 0;
        obj.fill_color = colorTransparent();
        obj.border_width = 0;
        obj.border_color = colorTransparent();
        attachPositionMove(obj, 'Rectangle');
        obj.rotate = contextFunction((angle, file, line) => {
            obj.rotation = finiteNumber(obj.rotation, 'Rectangle.rotate() current rotation', file, line)
                + finiteNumber(angle, 'Rectangle.rotate() angle', file, line);
        });
    }
    if (typeName === 'Circle') {
        obj.__idylliumDrawable = true;
        obj.x = 0;
        obj.y = 0;
        obj.radius = 0;
        obj.rotation = 0;
        obj.fill_color = colorTransparent();
        obj.border_width = 0;
        obj.border_color = colorTransparent();
        attachPositionMove(obj, 'Circle');
        obj.rotate = contextFunction((angle, file, line) => {
            obj.rotation = finiteNumber(obj.rotation, 'Circle.rotate() current rotation', file, line)
                + finiteNumber(angle, 'Circle.rotate() angle', file, line);
        });
    }
    if (typeName === 'Line') {
        obj.__idylliumDrawable = true;
        obj.x1 = 0;
        obj.y1 = 0;
        obj.x2 = 0;
        obj.y2 = 0;
        obj.color = colorWhite();
        obj.thickness = 1;
        attachLineMove(obj);
    }
    if (typeName === 'Texture') {
        obj.path = '';
        obj.resolved_path = '';
        obj.is_loaded = false;
        obj.load_from_file = contextFunction((targetPath, file, line) => {
            const requestedPath = stringArgument(targetPath, 'Texture.load_from_file() path', file, line);
            const resolvedPath = state.fileSystem.resolvePath(requestedPath, file);
            if (!state.fileSystem.exists(resolvedPath)) {
                throw new IdylliumRuntimeError(file, line, `Texture.load_from_file() cannot load '${requestedPath}': file does not exist`);
            }
            if (!runtimeIsFile(state.fileSystem, resolvedPath, file, line, 'reading')) {
                throw new IdylliumRuntimeError(file, line, `Texture.load_from_file() cannot load '${requestedPath}': path is not a file`);
            }
            obj.path = requestedPath;
            obj.resolved_path = resolvedPath;
            obj.resource_uri = state.fileSystem.resourceUri?.(resolvedPath) ?? '';
            obj.is_loaded = true;
        });
    }
    if (typeName === 'Font') {
        obj.path = '';
        obj.resolved_path = '';
        obj.is_loaded = false;
        obj.load_from_file = contextFunction((targetPath, file, line) => {
            const requestedPath = stringArgument(targetPath, 'Font.load_from_file() path', file, line);
            const resolvedPath = state.fileSystem.resolvePath(requestedPath, file);
            if (!state.fileSystem.exists(resolvedPath)) {
                throw new IdylliumRuntimeError(file, line, `Font.load_from_file() cannot load '${requestedPath}': file does not exist`);
            }
            if (!runtimeIsFile(state.fileSystem, resolvedPath, file, line, 'reading')) {
                throw new IdylliumRuntimeError(file, line, `Font.load_from_file() cannot load '${requestedPath}': path is not a file`);
            }
            obj.path = requestedPath;
            obj.resolved_path = resolvedPath;
            obj.resource_uri = state.fileSystem.resourceUri?.(resolvedPath) ?? '';
            obj.is_loaded = true;
        });
    }
    if (typeName === 'Sprite') {
        obj.__idylliumDrawable = true;
        obj.texture = createPlainRuntimeObject('drawable', 'Texture', state);
        obj.x = 0;
        obj.y = 0;
        obj.scale_x = 1;
        obj.scale_y = 1;
        obj.set_scale = contextFunction((x, y, file, line) => {
            obj.scale_x = finiteNumber(x, 'Sprite.set_scale() x', file, line);
            obj.scale_y = finiteNumber(y, 'Sprite.set_scale() y', file, line);
        });
        attachPositionMove(obj, 'Sprite');
    }
    if (typeName === 'Text') {
        obj.__idylliumDrawable = true;
        obj.font = createPlainRuntimeObject('drawable', 'Font', state);
        obj.text = '';
        obj.x = 0;
        obj.y = 0;
        obj.font_size = 16;
        obj.text_color = colorWhite();
        attachPositionMove(obj, 'Text');
    }
}
function attachPositionMove(obj, typeName) {
    obj.move = contextFunction((dx, dy, file, line) => {
        obj.x = finiteNumber(obj.x, `${typeName}.move() current x`, file, line)
            + finiteNumber(dx, `${typeName}.move() dx`, file, line);
        obj.y = finiteNumber(obj.y, `${typeName}.move() current y`, file, line)
            + finiteNumber(dy, `${typeName}.move() dy`, file, line);
    });
}
function attachLineMove(obj) {
    obj.move = contextFunction((dx, dy, file, line) => {
        const deltaX = finiteNumber(dx, 'Line.move() dx', file, line);
        const deltaY = finiteNumber(dy, 'Line.move() dy', file, line);
        obj.x1 = finiteNumber(obj.x1, 'Line.move() current x1', file, line) + deltaX;
        obj.y1 = finiteNumber(obj.y1, 'Line.move() current y1', file, line) + deltaY;
        obj.x2 = finiteNumber(obj.x2, 'Line.move() current x2', file, line) + deltaX;
        obj.y2 = finiteNumber(obj.y2, 'Line.move() current y2', file, line) + deltaY;
    });
}
function initializeAudioObject(obj, typeName, state) {
    if (typeName !== 'Sound' && typeName !== 'Music')
        return;
    obj.src = '';
    obj.resolved_path = '';
    obj.resource_uri = '';
    obj.is_loaded = false;
    obj.duration = 0;
    obj.is_playing = false;
    obj.__audioCommands = [];
    defineValidatedRuntimeProperty(obj, 'volume', 1, (value, file, line) => (rangeNumber(value, `${typeName}.volume`, 0, 1, file, line)));
    obj.load_from_file = contextFunction((targetPath, file, line) => {
        const requestedPath = stringArgument(targetPath, `${typeName}.load_from_file() path`, file, line);
        const resolvedPath = state.fileSystem.resolvePath(requestedPath, file);
        if (!state.fileSystem.exists(resolvedPath)) {
            throw new IdylliumRuntimeError(file, line, `${typeName}.load_from_file() cannot load '${requestedPath}': file does not exist`);
        }
        if (!runtimeIsFile(state.fileSystem, resolvedPath, file, line, 'reading')) {
            throw new IdylliumRuntimeError(file, line, `${typeName}.load_from_file() cannot load '${requestedPath}': path is not a file`);
        }
        obj.src = requestedPath;
        obj.resolved_path = resolvedPath;
        obj.resource_uri = state.fileSystem.resourceUri?.(resolvedPath) ?? '';
        obj.duration = audioDuration(state.fileSystem, resolvedPath);
        obj.is_loaded = true;
    });
    obj.play = contextFunction((file, line) => {
        assertAudioLoaded(obj, typeName, 'play', file, line);
        obj.is_playing = true;
        pushAudioCommand(obj, state, 'play');
    });
    obj.pause = () => {
        obj.is_playing = false;
        pushAudioCommand(obj, state, 'pause');
    };
    obj.resume = contextFunction((file, line) => {
        assertAudioLoaded(obj, typeName, 'resume', file, line);
        obj.is_playing = true;
        pushAudioCommand(obj, state, 'resume');
    });
    obj.stop = () => {
        obj.is_playing = false;
        if (typeName === 'Music')
            obj.position = 0;
        pushAudioCommand(obj, state, 'stop');
    };
    if (typeName === 'Music') {
        obj.loop = false;
        defineValidatedRuntimeProperty(obj, 'position', 0, (value, file, line) => {
            const position = finiteNumber(value, 'Music.position', file, line);
            const duration = typeof obj.duration === 'number' ? obj.duration : 0;
            if (position < 0) {
                throw new IdylliumRuntimeError(file, line, `Music.position must be non-negative, got ${position}`);
            }
            if (duration > 0 && position > duration) {
                throw new IdylliumRuntimeError(file, line, `Music.position must be between 0 and ${duration}, got ${position}`);
            }
            return position;
        });
    }
    state.audio.push(obj);
}
function pushAudioCommand(obj, state, action) {
    audioCommands(obj).push({ id: state.nextAudioCommandId++, action });
}
function assertAudioLoaded(obj, typeName, methodName, file, line) {
    if (obj.is_loaded === true)
        return;
    throw new IdylliumRuntimeError(file, line, `${typeName}.${methodName}() cannot play audio before load_from_file()`);
}
function audioCommands(obj) {
    if (Array.isArray(obj.__audioCommands))
        return obj.__audioCommands;
    const commands = [];
    Object.defineProperty(obj, '__audioCommands', {
        value: commands,
        enumerable: false,
        configurable: true,
    });
    return commands;
}
function audioDuration(fileSystem, filePath) {
    try {
        const text = fileSystem.readText(filePath);
        const dataUrlMatch = /^data:audio\/[^;]+;base64,(.+)$/u.exec(text);
        if (dataUrlMatch)
            return wavDuration(nodeBuffer.from(dataUrlMatch[1], 'base64'));
        return wavDuration(nodeBuffer.from(text, 'binary'));
    }
    catch {
        return 0;
    }
}
function wavDuration(bytes) {
    if (!bytes || typeof bytes.length !== 'number' || bytes.length < 44)
        return 0;
    if (bytes.toString('ascii', 0, 4) !== 'RIFF' || bytes.toString('ascii', 8, 12) !== 'WAVE')
        return 0;
    const channels = bytes.readUInt16LE(22);
    const sampleRate = bytes.readUInt32LE(24);
    const bitsPerSample = bytes.readUInt16LE(34);
    const dataSize = bytes.readUInt32LE(40);
    const bytesPerSecond = sampleRate * Math.max(1, channels) * Math.max(1, bitsPerSample) / 8;
    if (!Number.isFinite(bytesPerSecond) || bytesPerSecond <= 0)
        return 0;
    return dataSize / bytesPerSecond;
}
function isGuiWidget(typeName) {
    return typeName === 'Window'
        || typeName === 'Widget'
        || typeName === 'Canvas'
        || typeName === 'Label'
        || typeName === 'Button'
        || typeName === 'Frame'
        || typeName === 'Image'
        || typeName === 'LineEdit'
        || typeName === 'TextEdit'
        || typeName === 'ProgressBar'
        || typeName === 'SpinBox'
        || typeName === 'FloatSpinBox'
        || typeName === 'Slider'
        || typeName === 'CheckBox'
        || typeName === 'RadioButton'
        || typeName === 'ComboBox';
}
function isRuntimeObject(value) {
    return value !== null && typeof value === 'object';
}
function isDrawableObject(value) {
    return isRuntimeObject(value) && value.__idylliumDrawable === true;
}
function canvasCommands(canvas) {
    const commands = canvas.__commands;
    if (Array.isArray(commands))
        return commands;
    canvas.__commands = [];
    return canvas.__commands;
}
async function initializeGuiChild(child) {
    if (child.__idylliumType === 'gui.Canvas') {
        const onInit = child.on_init;
        if (typeof onInit === 'function') {
            await onInit(child);
        }
        const onUpdate = child.on_update;
        if (typeof onUpdate === 'function') {
            await onUpdate(child, 0);
        }
    }
    for (const nested of child.__children ?? []) {
        await initializeGuiChild(nested);
    }
}
function canvasSnapshot(canvas) {
    return {
        id: runtimeObjectId(canvas),
        type: 'gui.Canvas',
        properties: objectPropertiesSnapshot(canvas),
        commands: canvasCommands(canvas).map((command) => ({ ...command })),
    };
}
function audioSnapshot(audio) {
    return {
        id: runtimeObjectId(audio),
        type: audio.__idylliumType === 'audio.Music' ? 'audio.Music' : 'audio.Sound',
        properties: objectPropertiesSnapshot(audio),
        commands: audioCommands(audio).map((command) => ({ ...command })),
    };
}
function windowSnapshot(window) {
    return {
        id: runtimeObjectId(window),
        type: 'gui.Window',
        properties: objectPropertiesSnapshot(window),
        children: widgetChildrenSnapshot(window),
    };
}
function modalSnapshot(modal) {
    const mode = modal.__modalMode === 'confirm' || modal.__modalMode === 'input' ? modal.__modalMode : 'alert';
    return {
        id: runtimeObjectId(modal),
        type: 'gui.Modal',
        mode,
        properties: objectPropertiesSnapshot(modal),
    };
}
function widgetSnapshot(widget) {
    const type = String(widget.__idylliumType ?? 'gui.Widget');
    return {
        id: runtimeObjectId(widget),
        type,
        properties: objectPropertiesSnapshot(widget),
        children: widgetChildrenSnapshot(widget),
        canvas: type === 'gui.Canvas' ? canvasSnapshot(widget) : undefined,
        items: type === 'gui.ComboBox' && Array.isArray(widget.__items) ? [...widget.__items] : undefined,
    };
}
function widgetChildrenSnapshot(widget) {
    const children = widget.__children;
    if (!Array.isArray(children))
        return [];
    return children.map(widgetSnapshot);
}
function drawableSnapshot(value) {
    return {
        type: String(value.__idylliumType ?? 'drawable.Drawable'),
        properties: objectPropertiesSnapshot(value),
    };
}
function objectPropertiesSnapshot(value) {
    const result = {};
    for (const [key, item] of Object.entries(value)) {
        if (key.startsWith('__') || typeof item === 'function')
            continue;
        result[key] = snapshotValue(item);
    }
    if (value.__explicitProperties instanceof Set && value.__explicitProperties.size > 0) {
        result.__explicit_properties = [...value.__explicitProperties].sort();
    }
    return result;
}
function runtimeObjectId(value) {
    return typeof value.__idylliumObjectId === 'number' ? value.__idylliumObjectId : 0;
}
function snapshotValue(value) {
    if (value instanceof IdylliumColor)
        return value.toCss();
    if (value instanceof IdylliumArray)
        return value.values().map(snapshotValue);
    if (isRuntimeObject(value)) {
        return {
            type: String(value.__idylliumType ?? 'object'),
            properties: objectPropertiesSnapshot(value),
        };
    }
    return value;
}
function colorToCss(value, argumentName, file, line) {
    if (value instanceof IdylliumColor)
        return value.toCss();
    throw new IdylliumRuntimeError(file, line, `${argumentName} must be colors.Color`);
}
function colorBlack() {
    return IdylliumColor.RGB(0, 0, 0);
}
function colorWhite() {
    return IdylliumColor.RGB(255, 255, 255);
}
function colorBlue() {
    return IdylliumColor.RGB(0, 0, 255);
}
function colorGray() {
    return IdylliumColor.RGB(180, 180, 180);
}
function colorLightGray() {
    return IdylliumColor.RGB(239, 239, 239);
}
function colorVeryLightGray() {
    return IdylliumColor.RGB(245, 245, 245);
}
function colorTransparent() {
    return IdylliumColor.RGBA(0, 0, 0, 0);
}
async function stepGuiTimer(timer, deltaTime) {
    if (timer.__running !== true)
        return;
    const callback = timer.on_tick;
    if (typeof callback !== 'function')
        return;
    const interval = typeof timer.interval === 'number' && Number.isFinite(timer.interval)
        ? Math.max(1, Math.trunc(timer.interval))
        : 1000;
    const elapsed = (typeof timer.__elapsedMs === 'number' ? timer.__elapsedMs : 0) + Math.max(0, deltaTime * 1000);
    timer.__elapsedMs = elapsed;
    while (timer.__running === true && typeof timer.__elapsedMs === 'number' && timer.__elapsedMs >= interval) {
        timer.__elapsedMs -= interval;
        await callback(timer);
    }
}
function applyGuiEventPayload(target, eventName, payload, state) {
    if (target.__idylliumType === 'audio.Music' && eventName === 'finished') {
        target.is_playing = false;
        return;
    }
    if (target.__idylliumType === 'audio.Sound' && eventName === 'sound_finished') {
        target.is_playing = false;
        return;
    }
    if (eventName === 'modal_confirm' || eventName === 'modal_cancel') {
        if (typeof payload.input_value === 'string') {
            target.__input_value = payload.input_value;
        }
        closeModal(target, state);
        return;
    }
    if (eventName !== 'change')
        return;
    switch (target.__idylliumType) {
        case 'gui.LineEdit':
        case 'gui.TextEdit':
            target.text = typeof payload.text === 'string' ? payload.text : '';
            return;
        case 'gui.SpinBox':
        case 'gui.Slider':
            target.value = eventNumber(payload.value);
            return;
        case 'gui.FloatSpinBox':
            target.value = eventFloat(payload.value);
            return;
        case 'gui.CheckBox':
            target.is_checked = payload.is_checked === true;
            return;
        case 'gui.RadioButton':
            if (payload.is_selected === true) {
                selectRadioButton(target, state);
            }
            return;
        case 'gui.ComboBox':
            target.selected_index = eventNumber(payload.selected_index);
            return;
        default:
            return;
    }
}
function selectRadioButton(target, state) {
    const group = typeof target.group === 'string' ? target.group : '';
    const parent = target.__parent;
    for (const item of state.objects) {
        if (item === target || item.__idylliumType !== 'gui.RadioButton')
            continue;
        const itemGroup = typeof item.group === 'string' ? item.group : '';
        const sameNamedGroup = group !== '' && itemGroup === group;
        const sameParentDefaultGroup = group === '' && itemGroup === '' && item.__parent === parent;
        if (sameNamedGroup || sameParentDefaultGroup) {
            item.is_selected = false;
        }
    }
    target.is_selected = true;
}
function closeModal(target, state) {
    const index = state.modals.indexOf(target);
    if (index >= 0) {
        state.modals.splice(index, 1);
    }
    target.__modalMode = '';
}
function guiCallbackName(target, eventName) {
    if (eventName === 'click')
        return 'on_click';
    if (eventName === 'change')
        return 'on_change';
    if (target.__idylliumType === 'gui.Modal' && eventName === 'modal_confirm')
        return 'on_confirm';
    if (target.__idylliumType === 'gui.Modal' && eventName === 'modal_cancel')
        return 'on_cancel';
    if (target.__idylliumType === 'audio.Music' && eventName === 'finished')
        return 'on_finished';
    if (target.__idylliumType !== 'gui.Canvas')
        return null;
    switch (eventName) {
        case 'key_pressed':
            return 'on_key_pressed';
        case 'key_released':
            return 'on_key_released';
        case 'mouse_pressed':
            return 'on_mouse_pressed';
        case 'mouse_released':
            return 'on_mouse_released';
        case 'mouse_move':
            return 'on_mouse_move';
        case 'mouse_scroll':
            return 'on_mouse_scroll';
        default:
            return null;
    }
}
function guiEventObject(eventName, payload) {
    if (eventName === 'key_pressed' || eventName === 'key_released') {
        return {
            __idylliumType: 'gui.KeyboardEvent',
            key: typeof payload.key === 'string' ? payload.key : '',
        };
    }
    if (eventName === 'mouse_scroll') {
        return {
            __idylliumType: 'gui.MouseScrollEvent',
            x: eventNumber(payload.x),
            y: eventNumber(payload.y),
            delta: eventNumber(payload.delta),
        };
    }
    return {
        __idylliumType: 'gui.MouseEvent',
        x: eventNumber(payload.x),
        y: eventNumber(payload.y),
        mouse_button: typeof payload.mouse_button === 'string' ? payload.mouse_button : '',
    };
}
function eventNumber(value) {
    return typeof value === 'number' && Number.isFinite(value) ? Math.trunc(value) : 0;
}
function eventFloat(value) {
    return typeof value === 'number' && Number.isFinite(value) ? value : 0;
}
function runtimeTypeName(value) {
    if (isRuntimeObject(value) && typeof value.__idylliumType === 'string')
        return value.__idylliumType;
    return String(value);
}
function channel(value, argumentName, file, line) {
    const number = integerNumber(value, argumentName, file, line);
    if (number < 0 || number > 255) {
        throw new IdylliumRuntimeError(file, line, `${argumentName} must be between 0 and 255, got ${number}`);
    }
    return number;
}
function opacity(value, argumentName, file, line) {
    return rangeNumber(value, argumentName, 0, 1, file, line);
}
function percent(value, argumentName, file, line) {
    return percentRange(value, argumentName, 0, 100, file, line) / 100;
}
function percentRange(value, argumentName, min, max, file, line) {
    return rangeNumber(value, argumentName, min, max, file, line);
}
function hex(value) {
    return value.toString(16).padStart(2, '0');
}
function trimFloat(value) {
    return Number(value.toFixed(4)).toString();
}
//# sourceMappingURL=runtime.js.map
}
  };
  var cache = {};
  var builtins = createBuiltins();

  function load(id) {
    if (cache[id]) return cache[id].exports;
    if (!modules[id]) throw new Error('Module was not bundled: ' + id);
    var module = { exports: {} };
    cache[id] = module;
    modules[id](makeRequire(id), module, module.exports);
    return module.exports;
  }

  function makeRequire(from) {
    return function require(request) {
      if (builtins[request]) return builtins[request];
      if (request.charAt(0) !== '.') throw new Error('Unsupported browser require: ' + request);
      return load(resolveModuleId(from, request));
    };
  }

  function resolveModuleId(from, request) {
    var base = from.slice(0, from.lastIndexOf('/') + 1);
    var id = normalizePath(base + request);
    if (!/\.js$/u.test(id)) id += '.js';
    return id;
  }

  function normalizePath(value) {
    var parts = value.replace(/\\/gu, '/').split('/');
    var result = [];
    for (var i = 0; i < parts.length; i++) {
      var part = parts[i];
      if (!part || part === '.') continue;
      if (part === '..') result.pop();
      else result.push(part);
    }
    return result.join('/');
  }

  function createBuiltins() {
    var pathBuiltin = {
      basename: function basename(value, ext) {
        var name = pathBuiltin.normalize(value).split('/').pop() || '';
        return ext && name.endsWith(ext) ? name.slice(0, -ext.length) : name;
      },
      dirname: function dirname(value) {
        var normalized = pathBuiltin.normalize(value);
        if (normalized === '/') return '/';
        var index = normalized.lastIndexOf('/');
        if (index <= 0) return normalized.charAt(0) === '/' ? '/' : '.';
        return normalized.slice(0, index);
      },
      isAbsolute: function isAbsolute(value) {
        return String(value).charAt(0) === '/';
      },
      join: function join() {
        return pathBuiltin.normalize(Array.prototype.join.call(arguments, '/'));
      },
      normalize: function normalize(value) {
        var raw = String(value).replace(/\\/gu, '/');
        var absolute = raw.charAt(0) === '/';
        var parts = raw.split('/');
        var result = [];
        for (var i = 0; i < parts.length; i++) {
          var part = parts[i];
          if (!part || part === '.') continue;
          if (part === '..') result.pop();
          else result.push(part);
        }
        return (absolute ? '/' : '') + result.join('/');
      },
      resolve: function resolve() {
        var value = '';
        for (var i = 0; i < arguments.length; i++) {
          var part = String(arguments[i]);
          value = part.charAt(0) === '/' ? part : value + '/' + part;
        }
        return pathBuiltin.normalize(value || '/');
      }
    };

    var fsBuiltin = {
      appendFileSync: function appendFileSync() { throw new Error('Node fs is not available in the browser'); },
      existsSync: function existsSync() { return false; },
      readFileSync: function readFileSync() { throw new Error('Node fs is not available in the browser'); },
      statSync: function statSync() { throw new Error('Node fs is not available in the browser'); },
      writeFileSync: function writeFileSync() { throw new Error('Node fs is not available in the browser'); }
    };

    return {
      buffer: { Buffer: BrowserBuffer },
      fs: fsBuiltin,
      path: pathBuiltin
    };
  }

  function BrowserBuffer(length) {
    return new Uint8Array(length);
  }

  BrowserBuffer.alloc = function alloc(length) {
    return enhanceBuffer(new Uint8Array(length));
  };

  BrowserBuffer.from = function from(value, encoding) {
    if (typeof value === 'string') {
      return enhanceBuffer(new TextEncoder().encode(value));
    }
    return enhanceBuffer(new Uint8Array(value));
  };

  function enhanceBuffer(bytes) {
    bytes.toString = function toString(encoding) {
      if (!encoding || String(encoding).toLowerCase() === 'utf8' || String(encoding).toLowerCase() === 'utf-8') {
        return new TextDecoder('utf-8').decode(bytes);
      }
      throw new Error('Unsupported browser buffer encoding: ' + encoding);
    };
    bytes.writeFloatBE = function writeFloatBE(value, offset) {
      new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).setFloat32(offset, value, false);
    };
    bytes.writeDoubleBE = function writeDoubleBE(value, offset) {
      new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).setFloat64(offset, value, false);
    };
    bytes.readFloatBE = function readFloatBE(offset) {
      return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).getFloat32(offset, false);
    };
    bytes.readDoubleBE = function readDoubleBE(offset) {
      return new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength).getFloat64(offset, false);
    };
    return bytes;
  }

  global.process = global.process || { cwd: function cwd() { return '/workspace'; } };
  global.Idyllium = load("src/browser.js");
}(typeof window !== 'undefined' ? window : globalThis));
